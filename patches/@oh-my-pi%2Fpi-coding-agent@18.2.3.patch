diff --git a/dist/types/internal-urls/memory-protocol.d.ts b/dist/types/internal-urls/memory-protocol.d.ts
index b5a4db84f23f88ccfad24c0bb96d07de7efe77b2..178a90dcb2933075053242cdba00bde6b8e4064c 100644
--- a/dist/types/internal-urls/memory-protocol.d.ts
+++ b/dist/types/internal-urls/memory-protocol.d.ts
@@ -1,10 +1,10 @@
 import type { InternalResource, InternalUrl, ProtocolHandler, ResolveContext, UrlCompletion } from "./types.js";
 /**
- * Snapshot of memory roots for every registered session, deduped.
- * Each session has its own cwd (possibly a worktree), so subagents and main
- * may see different roots.
+ * Snapshot of registered sessions' memory roots, deduped.
+ * Each session owns its agent directory and cwd. An explicit agent directory
+ * limits the sweep to sessions from that instance.
  */
-export declare function memoryRootsFromRegistry(): string[];
+export declare function memoryRootsFromRegistry(context?: Pick<ResolveContext, "agentDir" | "agentRegistry">): string[];
 export interface MemoryGlobPattern {
     baseUrl: string;
     globPattern: string;
diff --git a/dist/types/internal-urls/types.d.ts b/dist/types/internal-urls/types.d.ts
index deb47109f71ac8637bf947676986fbcea0cf0bc6..c8c1b0f9b1f1e82f4dcd533488e880d2213ed883 100644
--- a/dist/types/internal-urls/types.d.ts
+++ b/dist/types/internal-urls/types.d.ts
@@ -87,6 +87,8 @@ export interface InternalUrl extends URL {
 export interface ResolveContext {
     /** Working directory of the calling session. */
     cwd?: string;
+    /** Agent directory supplied by the calling SDK session. */
+    agentDir?: string;
     /**
      * Calling session's session file. Handlers that resolve agent ids which may
      * be parked (`history://<id>`, `agent://<id>`) refresh the caller's
diff --git a/dist/types/mcp/loader.d.ts b/dist/types/mcp/loader.d.ts
index 88cb17b3f860d90cffe81d31d47bac132174498f..055bf8ec78ba0c17f37541a8d75deb05b4873421 100644
--- a/dist/types/mcp/loader.d.ts
+++ b/dist/types/mcp/loader.d.ts
@@ -1,7 +1,8 @@
 import type { LoadedCustomTool } from "../extensibility/custom-tools/types.js";
 import { AgentStorage } from "../session/agent-storage.js";
 import type { AuthStorage } from "../session/auth-storage.js";
-import { MCPManager } from "./manager.js";
+import { type MCPConfigLoader, MCPManager } from "./manager.js";
+import type { LoadMCPConfigsOptions } from "./config.js";
 import type { McpConnectionStatusEvent } from "./startup-events.js";
 /** Result from loading MCP tools */
 export interface MCPToolsLoadResult {
@@ -33,6 +34,8 @@ export interface MCPToolsLoadOptions {
     cacheStorage?: AgentStorage | null;
     /** Auth storage used to resolve OAuth credentials before initial MCP connect */
     authStorage?: AuthStorage;
+    configLoader?: MCPConfigLoader;
+    extensionRoots?: LoadMCPConfigsOptions["extensionRoots"];
 }
 /**
  * Discover and load MCP tools from .mcp.json files.
diff --git a/dist/types/modes/internal-url-autocomplete.d.ts b/dist/types/modes/internal-url-autocomplete.d.ts
index 4ae91925cbcd33bb348cc1793f4ca1a0e37280e1..a1fedd8109c84f132bd1f1d8386cf98858782d46 100644
--- a/dist/types/modes/internal-url-autocomplete.d.ts
+++ b/dist/types/modes/internal-url-autocomplete.d.ts
@@ -23,7 +23,7 @@ export interface InternalUrlContext {
  */
 export declare function extractInternalUrlContext(textBeforeCursor: string): InternalUrlContext | null;
 /** Caller binding shared by tool execution and prompt completion. */
-export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "sessionFile" | "sessionId" | "agentRegistry">;
+export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "agentDir" | "sessionFile" | "sessionId" | "agentRegistry">;
 /**
  * Suggestions for the internal-url token ending at the cursor, or `null` when
  * the text is not such a token or no candidate matches the typed query.
diff --git a/dist/types/modes/rpc/rpc-mode.d.ts b/dist/types/modes/rpc/rpc-mode.d.ts
index 225e26b763a2a6467d0bad103fd1bd45f51e0237..23a9a85e8d6bd6ad6fbf31fad050ab592b0e160d 100644
--- a/dist/types/modes/rpc/rpc-mode.d.ts
+++ b/dist/types/modes/rpc/rpc-mode.d.ts
@@ -1,6 +1,7 @@
 import { type ExtensionUIContext, type ExtensionUIDialogOptions, type ExtensionUISelectItem } from "../../extensibility/extensions/index.js";
 import { type BuiltSkillPromptMessage, type Skill, type SkillPromptInput } from "../../extensibility/skills.js";
 import type { AgentSession } from "../../session/agent-session.js";
+import type { PromptDeliveryObserver } from "../../session/agent-session-types.js";
 import type { EventBus } from "../../utils/event-bus.js";
 import { RpcSubagentRegistry } from "./rpc-subagents.js";
 import type { RpcCommand, RpcExtensionUIRequest, RpcExtensionUIResponse, RpcHostToolCallRequest, RpcHostToolCancelRequest, RpcHostToolResult, RpcHostToolUpdate, RpcHostUriCancelRequest, RpcHostUriRequest, RpcHostUriResult, RpcResponse } from "./rpc-types.js";
@@ -69,7 +70,7 @@ export declare function resolveRpcSkillInvocation(session: RpcSkillCommandSessio
  * Must not run on the RPC serial queue's response path — register it with
  * watchAndReportLocalOnlyPromptResult and answer the command first.
  */
-export declare function runRpcSkillCommand(session: RpcSkillCommandSession, invocation: RpcSkillInvocation, streamingBehavior?: "steer" | "followUp", prebuilt?: BuiltSkillPromptMessage): Promise<boolean>;
+export declare function runRpcSkillCommand(session: RpcSkillCommandSession, invocation: RpcSkillInvocation, streamingBehavior?: "steer" | "followUp", prebuilt?: BuiltSkillPromptMessage, deliveryObserver?: PromptDeliveryObserver): Promise<boolean>;
 /**
  * Skill branch of the `prompt` command: resolves the invocation cheaply, then
  * registers the slow dispatch with watchAndReportLocalOnlyPromptResult and
@@ -87,7 +88,7 @@ export declare function dispatchRpcSkillPrompt(input: {
     onError: (error: Error) => void;
     extensionUserMessageTracker: RpcExtensionUserMessageTracker;
 }): Promise<RpcSkillCommandResult | null>;
-export declare function tryRunRpcSkillCommand(session: RpcSkillCommandSession, text: string, streamingBehavior?: "steer" | "followUp"): Promise<RpcSkillCommandResult | false>;
+export declare function tryRunRpcSkillCommand(session: RpcSkillCommandSession, text: string, streamingBehavior?: "steer" | "followUp", deliveryObserver?: PromptDeliveryObserver): Promise<RpcSkillCommandResult | false>;
 export declare function reportLocalOnlyPromptResult(input: {
     id: string | undefined;
     prompt: Promise<boolean>;
diff --git a/dist/types/sdk.d.ts b/dist/types/sdk.d.ts
index 2aace694efb08d3ddc84ff9f1b708bcd754f18f4..0eb171ff99921d25c3627b5b21c347881a838b82 100644
--- a/dist/types/sdk.d.ts
+++ b/dist/types/sdk.d.ts
@@ -16,6 +16,7 @@ import { type FileSlashCommand } from "./extensibility/slash-commands.js";
 import type { HindsightSessionState } from "./hindsight/state.js";
 import { type LocalProtocolOptions } from "./internal-urls/index.js";
 import { MCPManager, type MCPToolsLoadResult } from "./mcp/index.js";
+import type { MCPConfigLoader } from "./mcp/manager.js";
 import type { MnemopiSessionState } from "./mnemopi/state.js";
 import { type AgentRef, AgentRegistry } from "./registry/agent-registry.js";
 import { AgentSession, type PlanYolo, type Prewalk } from "./session/agent-session.js";
@@ -210,6 +211,7 @@ export interface CreateAgentSessionOptions {
     enableMCP?: boolean;
     /** Existing MCP manager to reuse when MCP is enabled (skips discovery, propagates to toolSession). */
     mcpManager?: MCPManager;
+    mcpConfigLoader?: MCPConfigLoader;
     /** Enable LSP integration (tool, formatting, diagnostics, warmup). Default: true */
     enableLsp?: boolean;
     /** Restrict LSP to navigation and diagnostics even when enabled. Defaults to true for restricted sessions. */
diff --git a/dist/types/session/agent-session-types.d.ts b/dist/types/session/agent-session-types.d.ts
index a2580e04885bfe092596e7610417793672338bb5..5977f5e87e0767573c4176ba54de21d35fcbf655 100644
--- a/dist/types/session/agent-session-types.d.ts
+++ b/dist/types/session/agent-session-types.d.ts
@@ -315,8 +315,15 @@ export interface AgentSessionConfig {
     /** System prompt used by automatic session-title generation. */
     titleSystemPrompt?: string;
 }
+/** SDK hosts observe admission and final delivery of one submitted message. */
+export interface PromptDeliveryObserver {
+    onAccepted(delivery: "prompt" | "steer" | "followUp" | "aside"): void;
+    onConsumed(): void;
+    onDiscarded(): void;
+}
 /** Options for AgentSession.prompt(). */
 export interface PromptOptions {
+    deliveryObserver?: PromptDeliveryObserver;
     /** Whether to expand file-based prompt templates (default: true). */
     expandPromptTemplates?: boolean;
     /** Image attachments. */
@@ -361,6 +368,7 @@ export interface SteerOptions {
 }
 /** Options for AgentSession.sendUserMessage(). */
 export interface SendUserMessageOptions {
+    deliveryObserver?: PromptDeliveryObserver;
     /** Queue behavior; omitted starts a turn when idle and steers while streaming. */
     deliverAs?: "steer" | "followUp" | "aside";
     /** Explicit billing/initiator attribution. */
diff --git a/dist/types/session/agent-session.d.ts b/dist/types/session/agent-session.d.ts
index dfc41754a4753f597660aa719e2bf05ea78f00f3..d965df9a8be0aebbb2af69510619470d55738036 100644
--- a/dist/types/session/agent-session.d.ts
+++ b/dist/types/session/agent-session.d.ts
@@ -81,6 +81,7 @@ export declare class AgentSession {
     readonly agent: Agent;
     readonly sessionManager: SessionManager;
     readonly settings: Settings;
+    readonly agentDir: string;
     /** Session-start policy, independent of the selected project memory backend. */
     readonly memoryEnabled: boolean;
     /** Entries of tools mounted under `xd://`; empty when virtual devices are unmounted. */
@@ -250,7 +251,7 @@ export declare class AgentSession {
     markMovedFromEmptySessionFile(sessionFile: string): void;
     /**
      * Synchronously mark the session as disposing so new work is rejected
-     * immediately: eval starts throw, queued asides are dropped, and the
+     * immediately: eval starts throw, queued asides are flushed, and the
      * aside provider is detached. Idempotent; `dispose()` runs it first.
      *
      * Wrappers that await other teardown before delegating to `dispose()` MUST
@@ -573,7 +574,7 @@ export declare class AgentSession {
      * won the generation race), so hosts waiting on a terminal `agent_end` can
      * stop instead of hanging.
      */
-    promptCustomMessage<T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">, options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice"> & {
+    promptCustomMessage<T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">, options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice" | "deliveryObserver"> & {
         queueChipText?: string;
         queueOnly?: boolean;
     }): Promise<boolean>;
@@ -1013,6 +1014,9 @@ export declare class AgentSession {
     navigateTree(targetId: string, options?: {
         summarize?: boolean;
         customInstructions?: string;
+        requireIdle?: boolean;
+        /** Hosts settle and publish replacement before post-navigation hooks can start work. */
+        onHistoryReplaced?: () => void | Promise<void>;
         /**
          * Opts into the two-phase `ask` toolResult re-answer protocol
          * (issue #5642): set only by the interactive `/tree` selector, which
@@ -1039,6 +1043,7 @@ export declare class AgentSession {
         editorImages?: ImageContent[];
         cancelled: boolean;
         aborted?: boolean;
+        busy?: boolean;
         summaryEntry?: BranchSummaryEntry;
         /** Raw session context built during navigation — pass to renderInitialMessages to skip a second O(N) walk. */
         sessionContext?: SessionContext;
diff --git a/dist/types/session/artifacts.d.ts b/dist/types/session/artifacts.d.ts
index 8decbb96f2ff02feb7e5f0a96498e8274f697a56..bb5988f8ed678c90e667ef36b5fc5c4d4a0229b0 100644
--- a/dist/types/session/artifacts.d.ts
+++ b/dist/types/session/artifacts.d.ts
@@ -13,7 +13,7 @@
  *
  * Returns the verified UTF-8 byte count.
  */
-export declare function writeArtifact(path: string, content: string): Promise<number>;
+export declare function writeArtifact(path: string, content: string, signal?: AbortSignal): Promise<number>;
 /**
  * Manages artifact storage for a session.
  *
@@ -56,7 +56,7 @@ export declare class ArtifactManager {
      * @param toolType Tool name for file extension (e.g., "bash", "read")
      * @returns Artifact ID (numeric string)
      */
-    save(content: string, toolType: string): Promise<string>;
+    save(content: string, toolType: string, signal?: AbortSignal): Promise<string>;
     /**
      * Check if an artifact exists.
      * @param id Artifact ID (numeric string)
diff --git a/dist/types/session/session-context.d.ts b/dist/types/session/session-context.d.ts
index 719d6112e7639460efe4c6b2e24ec2e97f2625c4..d301ea10964fbfa2e8b10465db0fd17f6bc6a0fd 100644
--- a/dist/types/session/session-context.d.ts
+++ b/dist/types/session/session-context.d.ts
@@ -1,10 +1,12 @@
 import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
-import { type OpenAIResponsesHistoryPayload, type ServiceTierByFamily } from "@oh-my-pi/pi-ai";
+import { type ImageContent, type OpenAIResponsesHistoryPayload, type ServiceTierByFamily } from "@oh-my-pi/pi-ai";
 import * as snapcompact from "@oh-my-pi/snapcompact";
+import type { TodoPhase } from "../tools/todo.js";
 import { type CustomMessage } from "./messages.js";
 import { type CompactionEntry, type CustomMessageEntry, type SessionEntry, type SessionMessageEntry } from "./session-entries.js";
 export interface SessionContext {
     messages: AgentMessage[];
+    todoPhases?: TodoPhase[] | null;
     thinkingLevel?: string;
     /** Configured thinking selector (`"auto"` or a concrete level) from the latest change. */
     configuredThinkingLevel?: string;
@@ -36,6 +38,8 @@ export interface BuildSessionContextOptions {
      * latest compacted tail.
      */
     transcript?: boolean;
+    /** Read-only snapshot loaders request canonical todos before history compaction. */
+    includeTodoPhases?: boolean;
     /** In transcript mode, elide entries replaced by the latest compaction. */
     collapseCompactedHistory?: boolean;
     /**
@@ -85,4 +89,9 @@ export declare function isUserRequestEntry(entry: SessionEntry): boolean;
  * `undefined` for anything that is not a user request.
  */
 export declare function userTurnDraft(entry: TranscriptEntry): string | undefined;
+/** Resolve plain tree navigation without changing the session or reopening tools. */
+export declare function resolveTreeNavigationTarget(entry: SessionEntry, currentLeafId: string | null): {
+    leafId: string | null;
+    draft: { text: string; images: ImageContent[] } | null;
+};
 export declare function buildSessionContext(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>, options?: BuildSessionContextOptions): SessionContext;
diff --git a/dist/types/session/session-entries.d.ts b/dist/types/session/session-entries.d.ts
index 963cd3ecab5f4581227dd318181dc5b2c0d07fc7..77eaa8bc6f550bf7bc6c9fcb254aa47d182bf408 100644
--- a/dist/types/session/session-entries.d.ts
+++ b/dist/types/session/session-entries.d.ts
@@ -16,6 +16,12 @@ export interface SessionTitleSlotEntry {
     updatedAt: string;
     pad: string;
 }
+/** Physical journal position, excluded from the conversation tree. */
+export interface SessionCursorRecord {
+    type: "session_cursor";
+    leafId: string | null;
+    revision: string;
+}
 export declare const EPHEMERAL_MODEL_CHANGE_ROLE = "fallback";
 export interface SessionHeader {
     type: "session";
@@ -259,10 +265,10 @@ export interface CustomMessageEntry<T = unknown> extends SessionEntryBase {
 }
 /** Session entry - has id/parentId for tree structure (returned by "read" methods in SessionManager) */
 export type SessionEntry = SessionMessageEntry | ModelUsageEntry | ThinkingLevelChangeEntry | ModelChangeEntry | ServiceTierChangeEntry | CompactionEntry | BranchSummaryEntry | CustomEntry | CustomMessageEntry | LabelEntry | TitleChangeEntry | TtsrInjectionEntry | SessionInitEntry | ModeChangeEntry | CredentialPinEntry | ResetBoundaryEntry;
-/** Raw logical file entry after loaders strip any fixed-width title slot. */
+/** Logical file entry after loaders fold physical title and cursor records. */
 export type FileEntry = SessionHeader | SessionEntry;
-/** Physical JSONL entry before slot-aware loaders fold the title slot. */
-export type RawFileEntry = SessionTitleSlotEntry | FileEntry;
+/** Physical JSONL record before loaders fold title and cursor state. */
+export type RawFileEntry = SessionTitleSlotEntry | SessionCursorRecord | FileEntry;
 /** Tree node for getTree() - defensive copy of session structure */
 export interface SessionTreeNode {
     entry: SessionEntry;
diff --git a/dist/types/session/session-loader.d.ts b/dist/types/session/session-loader.d.ts
index f9ef063e38a735e149ad6a174f39df36d55f9f68..e9c1053fe3041f9d3de7ed2aa8c6f3cb8a8fb35e 100644
--- a/dist/types/session/session-loader.d.ts
+++ b/dist/types/session/session-loader.d.ts
@@ -1,6 +1,7 @@
 import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
+import type { TodoPhase } from "../tools/todo.js";
 import { BlobStore } from "./blob-store.js";
-import type { FileEntry } from "./session-entries.js";
+import type { FileEntry, SessionCursorRecord, SessionEntry } from "./session-entries.js";
 import { type SessionStorage } from "./session-storage.js";
 import { type SessionTitleUpdate } from "./session-title-slot.js";
 export interface VisitEntriesFromFileStreamOptions {
@@ -23,6 +24,8 @@ export interface VisitEntriesFromFileStreamOptions {
      * callers derive the exact snapshot size without re-stating the file.
      */
     onBytesConsumed?: (bytes: number) => void;
+    /** Physical cursor records are never passed to the entry visitor. */
+    onCursor?: (cursor: SessionCursorRecord) => void;
 }
 /** Controls how a missing session file is handled. */
 export interface LoadSessionOptions {
@@ -33,13 +36,15 @@ export interface LoadSessionOptions {
 export interface SessionLoadResult {
     entries: FileEntry[];
     titleSlot: SessionTitleUpdate | undefined;
+    cursor?: SessionCursorRecord;
     malformedRecords: number;
     /** Byte length of the snapshot actually parsed, or `null` when the path did not exist. */
     sourceSize?: number | null;
     /** Whether non-empty session data was found without a valid leading session header. */
     invalidHeader: boolean;
 }
-/** Parse session JSONL while stripping and folding the optional fixed title slot. */
+export declare function sessionHistoryVersion(sessionId: string, entries: readonly SessionEntry[], cursor: Pick<SessionCursorRecord, "leafId" | "revision">): string;
+/** Parse session JSONL while folding physical title and cursor records. */
 export declare function parseSessionContent(content: string): SessionLoadResult;
 /** Parse session JSONL and visit each entry without retaining prior entries. */
 export declare function visitEntriesFromFileStream(filePath: string, visit: (entry: FileEntry) => void | boolean, options?: VisitEntriesFromFileStreamOptions): Promise<SessionTitleUpdate | undefined>;
@@ -62,9 +67,22 @@ export declare function resolveBlobRefsInEntries(entries: FileEntry[], blobStore
 /**
  * Read-only transcript view of a session file: load entries, migrate to the
  * current version, resolve blob refs, and build the display transcript along
- * the persisted leaf path (last entry). Uses transcript mode (collapsed to the
+ * the persisted cursor path. Uses transcript mode (collapsed to the
  * latest compaction) so failed/aborted tail turns stay visible, unlike the
  * provider-context builder which drops them. Does NOT create a writer or take
  * the session lock — safe to call against a file another session is writing.
  */
 export declare function loadSessionMessagesReadOnly(filePath: string): Promise<AgentMessage[]>;
+/** Transcript consumers call this to read messages and canonical todos from one journal version. */
+export declare function loadSessionSnapshotReadOnly(filePath: string): Promise<{
+    messages: AgentMessage[];
+    todoPhases: TodoPhase[] | null;
+    historyRevision: string;
+}>;
+export declare function loadSessionHistoryReadOnly(filePath: string): Promise<{
+    sessionId: string;
+    entries: SessionEntry[];
+    activeLeafId: string | null;
+    revision: string;
+    version: string;
+}>;
diff --git a/dist/types/session/session-manager.d.ts b/dist/types/session/session-manager.d.ts
index 7229fb73560793a0364550aa1cff831b2255f7a7..fde7f52d1b402f59bbcdfbff220eb2810425f478 100644
--- a/dist/types/session/session-manager.d.ts
+++ b/dist/types/session/session-manager.d.ts
@@ -5,12 +5,12 @@ import { type BlobPutOptions, type BlobPutResult } from "./blob-store.js";
 import type { CompactionMethod } from "./compaction-methods.js";
 import { type BashExecutionMessage, type CustomMessage, type FileMentionMessage, type HookMessage, type PythonExecutionMessage } from "./messages.js";
 import { type BuildSessionContextOptions, type SessionContext } from "./session-context.js";
-import { type FileEntry, type ModelUsageEntry, type NewSessionOptions, type SessionEntry, type SessionHeader, type SessionTitleSource, type SessionTreeNode, type UsageStatistics } from "./session-entries.js";
+import { type FileEntry, type ModelUsageEntry, type NewSessionOptions, type SessionCursorRecord, type SessionEntry, type SessionHeader, type SessionTitleSource, type SessionTreeNode, type UsageStatistics } from "./session-entries.js";
 import { type SessionInfo } from "./session-listing.js";
 import { type SessionStorage } from "./session-storage.js";
 /** Copy a session's artifact directory to another session, matching interactive `/fork`. */
 export declare function copySessionArtifacts(sourceSessionFile: string, destinationSessionFile: string): Promise<void>;
-export type ReadonlySessionManager = Pick<SessionManager, "getCwd" | "getRecordedCwd" | "getSessionDir" | "getSessionId" | "getSessionFile" | "getSessionName" | "getArtifactsDir" | "getArtifactManager" | "allocateArtifactPath" | "saveArtifact" | "getArtifactPath" | "getLeafId" | "getLeafEntry" | "getEntry" | "getLabel" | "getBranch" | "getHeader" | "getEntries" | "getTree" | "getUsageStatistics" | "putBlob" | "putBlobSync">;
+export type ReadonlySessionManager = Pick<SessionManager, "getCwd" | "getRecordedCwd" | "getSessionDir" | "getSessionId" | "getSessionFile" | "getSessionName" | "getArtifactsDir" | "getArtifactManager" | "allocateArtifactPath" | "saveArtifact" | "getArtifactPath" | "getLeafId" | "getLeafEntry" | "getHistoryRevision" | "getHistoryVersion" | "getEntry" | "getLabel" | "getBranch" | "getHeader" | "getEntries" | "getTree" | "getUsageStatistics" | "putBlob" | "putBlobSync">;
 interface SessionManagerStateSnapshot {
     cwd: string;
     sessionDir: string;
@@ -27,6 +27,7 @@ interface SessionManagerStateSnapshot {
     fallbackRuntimeOnly: boolean;
     header: SessionHeader;
     entries: SessionEntry[];
+    cursor: SessionCursorRecord;
 }
 /**
  * The storage may have published a write that rejected, and an authoritative
@@ -249,7 +250,7 @@ export declare class SessionManager {
         id?: string;
         path?: string;
     }>;
-    saveArtifact(content: string, toolType: string): Promise<string | undefined>;
+    saveArtifact(content: string, toolType: string, signal?: AbortSignal): Promise<string | undefined>;
     getArtifactPath(id: string): Promise<string | null>;
     saveDraft(text: string): Promise<void>;
     consumeDraft(): Promise<string | null>;
@@ -422,6 +423,9 @@ export declare class SessionManager {
     getHeader(): SessionHeader | null;
     /** All session entries (excludes header). Returns a shallow copy. */
     getEntries(): SessionEntry[];
+    getHistoryRevision(): string;
+    /** Opaque journal boundary. Settle persistence before publishing it to consumers. */
+    getHistoryVersion(): string;
     /**
      * The session as a tree. A well-formed session has exactly one root; orphaned
      * entries (broken parent chain) are returned as roots too.
@@ -437,9 +441,7 @@ export declare class SessionManager {
     /**
      * Durably move the active branch past a discarded entry.
      *
-     * The loader reconstructs the active branch from the last physical journal
-     * entry, so changing the in-memory leaf alone is lost on reload. Known
-     * metadata children are chained onto the discarded entry's parent before the
+     * Known metadata children are chained onto the discarded entry's parent before the
      * entry is removed. If any child may carry content, the subtree is preserved
      * off-branch instead. Both paths append a metadata-only branch marker and
      * rewrite the journal, making the selected path durable.
diff --git a/dist/types/session/session-migrations.d.ts b/dist/types/session/session-migrations.d.ts
index 80aa77c3f5a8c4c373ed7aa1797885eb6dc17d24..b33493636c5db3069a870c526c6c1ead0334035b 100644
--- a/dist/types/session/session-migrations.d.ts
+++ b/dist/types/session/session-migrations.d.ts
@@ -5,7 +5,7 @@ export declare function generateId(byId: {
 }): string;
 /**
  * Run all necessary migrations to bring entries to current version.
- * Mutates entries in place. Returns true if any migration was applied.
+ * Mutates entries in place. Returns true when a version migration requires a journal rewrite.
  */
 export declare function migrateToCurrentVersion(entries: FileEntry[]): boolean;
 /** Exported for testing */
diff --git a/dist/types/session/turn-persistence.d.ts b/dist/types/session/turn-persistence.d.ts
index f072e666eba8f2af3a425b6e52929c28a7ad4a42..2228c0c066d2b8ca599c8b482738bf0bcc0a40fe 100644
--- a/dist/types/session/turn-persistence.d.ts
+++ b/dist/types/session/turn-persistence.d.ts
@@ -1,33 +1,6 @@
-/**
- * Helpers that share one cheap, structural identity for messages — both during
- * incremental persistence and for the mid-run-compaction ordering check.
- *
- * Previously `AgentSession` carried two near-duplicate routines
- * (`#sessionMessagesReferToSameTurn` + `#messageValueSignature`) that
- * reconstructed the branch path on every check (O(n²) `unshift`) and
- * `JSON.stringify`-compared the full message content on every pairwise hit.
- * Long-running sessions with many subagents fired this thousands of times per
- * minute and froze the TUI loop (see issue #3629). The persistence key already
- * encodes a stable logical identity — timestamp + role-specific discriminators
- * — so the structural compare is now the rare collision tiebreaker (e.g. two
- * provider responses at the same millisecond with `undefined` responseId),
- * not the load-bearing check.
- *
- * The helpers here keep that identity in one place and expose the planner so
- * the persistence-ordering logic is unit-testable without standing up an
- * `AgentSession`.
- */
 import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
 /**
- * Stable identity for messages that pass through {@link AgentSession}'s
- * incremental persistence path.
- *
- * The discriminators chosen per role are precisely the fields that uniquely
- * identify a single logical message instance:
- *
- * - `assistant` — timestamp + provider + model + responseId + stopReason
- *   (responseId is the canonical provider-side id when available; the rest
- *   disambiguate when it is not, e.g. local/dev models).
+ * Assistants use their SDK lifecycle ID. Legacy records retain their existing persistence key.
  * - `toolResult` — timestamp + toolCallId + toolName (toolCallId is unique
  *   per execution; toolName guards against synthetic reuse).
  * - `user` / `developer` — timestamp + attribution (attribution distinguishes
diff --git a/dist/types/task/executor.d.ts b/dist/types/task/executor.d.ts
index efb19716faa9a2717f3ab3093d7ca5f9248a34bf..67185c3e464021f930cca4fc02d353e0f91220e6 100644
--- a/dist/types/task/executor.d.ts
+++ b/dist/types/task/executor.d.ts
@@ -76,6 +76,7 @@ export declare function collectIrcPeerRoster(registry: AgentRegistry, selfId: st
 /** Options for subagent execution */
 export interface ExecutorOptions {
     cwd: string;
+    agentDir?: string;
     /** Additional workspace directories to seed on the subagent session (multi-root). */
     additionalDirectories?: string[];
     /** Exact provider credential resolver inherited from the parent session. */
diff --git a/dist/types/tools/bash-skill-urls.d.ts b/dist/types/tools/bash-skill-urls.d.ts
index c29cb5c0f12562a70dbdf9e818233d49360e0562..0f2e26f25b24bf9771305f5f6aa2a6ed3db305e1 100644
--- a/dist/types/tools/bash-skill-urls.d.ts
+++ b/dist/types/tools/bash-skill-urls.d.ts
@@ -24,6 +24,7 @@ export interface InternalUrlExpansionOptions {
     internalRouter?: InternalUrlResolver;
     localOptions?: LocalProtocolOptions;
     cwd?: string;
+    agentDir?: string;
     sessionFile?: string;
     sessionId?: string;
     agentRegistry?: ResolveContext["agentRegistry"];
diff --git a/dist/types/tools/index.d.ts b/dist/types/tools/index.d.ts
index 6313b96febc07fcdd8984697ba51869b2c4229fb..0a97b6d8cbf8b2a133166063dfb7d8c66c88770a 100644
--- a/dist/types/tools/index.d.ts
+++ b/dist/types/tools/index.d.ts
@@ -119,6 +119,8 @@ export interface DeferredDiagnosticsEntry {
 export interface ToolSession {
     /** Current working directory */
     cwd: string;
+    /** Agent directory supplied by the SDK; standalone tools use their settings root. */
+    agentDir?: string;
     /** Additional workspace directories beyond cwd (multi-root), forwarded to subagents. */
     additionalDirectories?: string[];
     /** Whether UI is available */
diff --git a/dist/types/tools/path-utils.d.ts b/dist/types/tools/path-utils.d.ts
index 6e56c1c2af70e74460cc97818787f0d43f1f57c8..2bf4649bd6d99bfee02eb950b686e8e98d0461ba 100644
--- a/dist/types/tools/path-utils.d.ts
+++ b/dist/types/tools/path-utils.d.ts
@@ -348,6 +348,7 @@ export interface ResolvedExternalSearchUrl {
 export interface ToolScopeOptions {
     rawPaths: string[];
     cwd: string;
+    agentDir?: string;
     /** Verb used in the "Cannot {action} internal URL without a backing file: …" message. */
     internalUrlAction: string;
     /** Collect absolute paths flagged immutable by their internal-URL handler. */
diff --git a/src/eval/js/context-manager.ts b/src/eval/js/context-manager.ts
index 12c74702a8cc2f04ea6eb09fef20efcf269fe4ef..340d547137f22cb804b024e67dbfb9c4e2ade740 100644
--- a/src/eval/js/context-manager.ts
+++ b/src/eval/js/context-manager.ts
@@ -81,13 +81,6 @@ interface PendingRun {
 	deferDrained?: PromiseWithResolvers<void>;
 	/** Set once the turn was cancelled; blocks new bridge calls during the drain. */
 	aborted: boolean;
-	/**
-	 * A worker `result` withheld because the cell still has bridge calls in
-	 * flight. `#runOne` reports a finished run without awaiting its pending
-	 * tools, so a floated or caught `agent()` would otherwise settle the run —
-	 * tearing down the abort listener — while the subagent kept going with
-	 * nothing left able to cancel it. Delivered once the last call drains.
-	 */
 	heldResult?: Extract<WorkerOutbound, { type: "result" }>;
 	settled: boolean;
 }
@@ -823,11 +816,6 @@ function settlePending(session: JsSession, msg: Extract<WorkerOutbound, { type:
 	// Once the turn is cancelled the scheduled kill is the sole settler, so a
 	// late worker result can't cut the abort drain short.
 	if (pending.aborted) return;
-	// A cell owns every bridge call it starts. The worker finishes a run without
-	// awaiting its outstanding tool calls, so `agent(...)` that is floated or
-	// caught would settle the run here — `runOnce` then drops the abort listener
-	// and the pending entry, leaving the subagent running with nothing able to
-	// cancel it. Hold the result until the last call drains.
 	if (pending.toolCalls.size > 0) {
 		pending.heldResult = msg;
 		return;
diff --git a/src/eval/js/worker-core.ts b/src/eval/js/worker-core.ts
index 3ff78b62a3aa4b81ad98e3b329bbedfa9ddc73ba..36a1556b082d32ab59a14e6b21157672629604a9 100644
--- a/src/eval/js/worker-core.ts
+++ b/src/eval/js/worker-core.ts
@@ -19,6 +19,7 @@ interface ActiveRun {
 	runId: string;
 	filename: string;
 	pendingTools: Map<string, PendingTool>;
+	pendingToolsDrained: PromiseWithResolvers<void> | undefined;
 	/** Rejections floated by this run's cell code, captured before its result was sent. */
 	floatingRejections: unknown[];
 }
@@ -120,6 +121,7 @@ export class WorkerCore {
 	#unsubscribe: () => void;
 	#uninstallRejectionGuard: () => void;
 	#options: WorkerCoreOptions;
+	#closedError: ToolError | undefined;
 
 	constructor(transport: Transport, options: WorkerCoreOptions) {
 		this.#transport = transport;
@@ -163,6 +165,7 @@ export class WorkerCore {
 	 * cell activity and must keep the default fatal path.
 	 */
 	#consumeRejection(reason: unknown): boolean {
+		if (this.#closedError !== undefined && reason === this.#closedError) return true;
 		const stack = reason instanceof Error && typeof reason.stack === "string" ? reason.stack : undefined;
 		if (stack) {
 			// The stack can name several cells (helper defined by an earlier cell,
@@ -221,6 +224,7 @@ export class WorkerCore {
 	}
 
 	#handle(msg: WorkerInbound): void {
+		if (this.#closedError) return;
 		switch (msg.type) {
 			case "init":
 				try {
@@ -350,7 +354,13 @@ export class WorkerCore {
 	}
 
 	async #runOne(runId: string, code: string, filename: string, snapshot: SessionSnapshot): Promise<void> {
-		const active: ActiveRun = { runId, filename, pendingTools: new Map(), floatingRejections: [] };
+		const active: ActiveRun = {
+			runId,
+			filename,
+			pendingTools: new Map(),
+			pendingToolsDrained: undefined,
+			floatingRejections: [],
+		};
 		this.#runs.set(runId, active);
 		const hooks: RuntimeHooks = {
 			onText: chunk => this.#transport.send({ type: "text", runId, chunk }),
@@ -368,17 +378,7 @@ export class WorkerCore {
 		} catch (error) {
 			result = { type: "result", runId, ok: false, error: errorPayload(error) };
 		}
-		try {
-			// One event-loop turn so rejections the cell already floated surface
-			// while this run can still own them (rejection callbacks run before
-			// timers fire).
-			await Bun.sleep(0);
-			result = foldFloatingRejections(active, result, hooks);
-		} finally {
-			this.#runs.delete(runId);
-			this.#rememberCellFile(filename);
-			this.#transport.send(result);
-		}
+		await this.#finishRun(active, result, hooks);
 	}
 
 	async #invokeTool(msg: Extract<WorkerInbound, { type: "tool" }>): Promise<void> {
@@ -386,6 +386,7 @@ export class WorkerCore {
 			runId: msg.runId,
 			filename: `tool-${msg.runId}`,
 			pendingTools: new Map(),
+			pendingToolsDrained: undefined,
 			floatingRejections: [],
 		};
 		this.#runs.set(msg.runId, active);
@@ -395,6 +396,8 @@ export class WorkerCore {
 			callTool: (name, args) => this.#callTool(active, name, args),
 		};
 
+		let result: RunResult;
+		let envelope: Record<string, unknown> | undefined;
 		try {
 			const runtime = this.#runtime;
 			if (!runtime) throw new ToolError("JavaScript kernel is not running");
@@ -406,7 +409,6 @@ export class WorkerCore {
 				}
 			}
 
-			let envelope: Record<string, unknown>;
 			if (msg.op === "describe") {
 				const names = msg.names.length > 0 ? msg.names : [...tools.keys()];
 				envelope = {
@@ -429,13 +431,42 @@ export class WorkerCore {
 				}
 				envelope = { ok: true, value: cloneable };
 			}
-			this.#transport.send({ type: "display", runId: msg.runId, output: { type: "json", data: envelope } });
-			this.#transport.send({ type: "result", runId: msg.runId, ok: true });
+			result = { type: "result", runId: msg.runId, ok: true };
 		} catch (error) {
-			this.#transport.send({ type: "result", runId: msg.runId, ok: false, error: errorPayload(error) });
+			result = { type: "result", runId: msg.runId, ok: false, error: errorPayload(error) };
+		}
+		await this.#finishRun(active, result, hooks, envelope);
+	}
+
+	async #finishRun(
+		active: ActiveRun,
+		result: RunResult,
+		hooks: RuntimeHooks,
+		envelope?: Record<string, unknown>,
+	): Promise<void> {
+		while (true) {
+			if (this.#closedError || this.#runs.get(active.runId) !== active) return;
+			if (active.pendingTools.size > 0) {
+				active.pendingToolsDrained ??= Promise.withResolvers<void>();
+				await active.pendingToolsDrained.promise;
+			}
+			await new Promise<void>(resolve => setTimeout(resolve, 0));
+			if (this.#closedError || this.#runs.get(active.runId) !== active) return;
+			if (active.pendingTools.size === 0) break;
+		}
+		try {
+			result = foldFloatingRejections(active, result, hooks);
 		} finally {
-			this.#runs.delete(msg.runId);
+			this.#runs.delete(active.runId);
 			this.#rememberCellFile(active.filename);
+			if (result.ok && envelope) {
+				this.#transport.send({
+					type: "display",
+					runId: active.runId,
+					output: { type: "json", data: envelope },
+				});
+			}
+			this.#transport.send(result);
 		}
 	}
 
@@ -449,6 +480,8 @@ export class WorkerCore {
 	}
 
 	async #callTool(active: ActiveRun, name: string, args: unknown, identity?: RuntimeCallIdentity): Promise<unknown> {
+		if (this.#closedError) throw this.#closedError;
+		if (this.#runs.get(active.runId) !== active) throw new ToolError("JS run no longer active");
 		const id = `tc-${active.runId}-${crypto.randomUUID()}`;
 		const { promise, resolve, reject } = Promise.withResolvers<unknown>();
 		active.pendingTools.set(id, { runId: active.runId, resolve, reject });
@@ -458,55 +491,60 @@ export class WorkerCore {
 			// Non-serializable args (DataCloneError from postMessage / IPC send).
 			// No reply will ever arrive; fail this call instead of stranding a
 			// pending entry until close.
-			active.pendingTools.delete(id);
 			reject(error);
 		}
-		return await promise;
+		try {
+			return await promise;
+		} finally {
+			active.pendingTools.delete(id);
+			if (active.pendingTools.size === 0) {
+				active.pendingToolsDrained?.resolve();
+				active.pendingToolsDrained = undefined;
+			}
+		}
 	}
 
 	#deliverToolReply(id: string, reply: ToolReply): void {
 		for (const active of this.#runs.values()) {
 			const pending = active.pendingTools.get(id);
 			if (!pending) continue;
-			active.pendingTools.delete(id);
 			if (reply.ok) pending.resolve(reply.value);
 			else pending.reject(errorFromPayload(reply.error));
 			return;
 		}
 	}
 
-	#close(): void {
+	#close(notify = true): void {
+		if (this.#closedError) return;
+		const error = new ToolError("JS worker closed");
+		this.#closedError = error;
+		this.#unsubscribe();
 		for (const active of this.#runs.values()) {
 			for (const pending of active.pendingTools.values()) {
-				pending.reject(new ToolError("JS worker closed"));
+				pending.reject(error);
 			}
 			active.pendingTools.clear();
+			active.pendingToolsDrained?.resolve();
+			active.pendingToolsDrained = undefined;
 		}
-		this.#runs.clear();
 		this.#runtime?.dispose?.();
 		this.#runtime = null;
-		this.#transport.send({ type: "closed" });
-		this.#uninstallRejectionGuard();
-		this.#unsubscribe();
-		this.#transport.close();
+		// Keep rejection ownership until floated bridge shutdown errors surface.
+		setTimeout(() => {
+			this.#runs.clear();
+			this.#uninstallRejectionGuard();
+			try {
+				if (notify) this.#transport.send({ type: "closed" });
+			} finally {
+				try {
+					this.#transport.close();
+				} catch {
+				}
+			}
+		}, 0);
 	}
 
 	dispose(): void {
-		for (const active of this.#runs.values()) {
-			for (const pending of active.pendingTools.values()) {
-				pending.reject(new ToolError("JS worker closed"));
-			}
-			active.pendingTools.clear();
-		}
-		this.#runs.clear();
-		this.#runtime?.dispose?.();
-		this.#runtime = null;
-		this.#uninstallRejectionGuard();
-		this.#unsubscribe();
-		try {
-			this.#transport.close();
-		} catch {
-			// Ignore
-		}
+		this.#close(false);
 	}
 }
diff --git a/src/internal-urls/memory-protocol.ts b/src/internal-urls/memory-protocol.ts
index 2b228efb41b55434f7780b960bf1de2b1c1604d2..034ca3494141dc2ae9977478d7a112ef87bf0b1d 100644
--- a/src/internal-urls/memory-protocol.ts
+++ b/src/internal-urls/memory-protocol.ts
@@ -1,6 +1,6 @@
 import * as fs from "node:fs/promises";
 import * as path from "node:path";
-import { getAgentDir, isEnoent } from "@oh-my-pi/pi-utils";
+import { isEnoent } from "@oh-my-pi/pi-utils";
 import { getMemoryRoot } from "../memories";
 import { getMnemopiSessionState, type MnemopiScopedMemoryHit, type MnemopiSessionState } from "../mnemopi/state";
 import { AgentRegistry } from "../registry/agent-registry";
@@ -25,32 +25,31 @@ const HINDSIGHT_UNADDRESSABLE =
 	"Hindsight memories are not addressable via memory://. Recall results are final — use `recall` to search or `reflect` to synthesize. `read memory://<id>` is only available with memory.backend=mnemopi.";
 
 /**
- * Snapshot of memory roots for every registered session, deduped.
- * Each session has its own cwd (possibly a worktree), so subagents and main
- * may see different roots.
+ * Snapshot of registered sessions' memory roots, deduped.
+ * Each session owns its agent directory and cwd. An explicit agent directory
+ * limits the sweep to sessions from that instance.
  */
-export function memoryRootsFromRegistry(): string[] {
-	const agentDir = getAgentDir();
+export function memoryRootsFromRegistry(context?: Pick<ResolveContext, "agentDir" | "agentRegistry">): string[] {
 	const roots: string[] = [];
-	for (const ref of AgentRegistry.global().list()) {
-		const sm = ref.session?.sessionManager;
-		if (!sm) continue;
-		const root = getMemoryRoot(agentDir, sm.getCwd());
+	for (const ref of (context?.agentRegistry ?? AgentRegistry.global()).list()) {
+		const session = ref.session;
+		if (!session || (context?.agentDir !== undefined && session.agentDir !== context.agentDir)) continue;
+		const root = getMemoryRoot(session.agentDir, session.sessionManager.getCwd());
 		if (root && !roots.includes(root)) roots.push(root);
 	}
 	return roots;
 }
 
 /**
- * File-backed memory roots visible to one caller. A context that names a cwd
- * pins the root to it; otherwise the bound caller's own cwd is used, so a
- * session-id-only caller never reads a peer project's summary. Contextless
- * legacy callers keep the registry-wide sweep.
+ * File-backed memory roots visible to one caller. The caller's agent directory
+ * and cwd select its root. Contextless callers keep the registry-wide sweep;
+ * an unbound cwd without an agent directory cannot identify a memory root.
  */
 function memoryRootsForContext(context: ResolveContext | undefined, caller: AgentSession | undefined): string[] {
 	const cwd = context?.cwd ?? caller?.sessionManager.getCwd();
-	if (cwd) return [getMemoryRoot(getAgentDir(), cwd)];
-	return memoryRootsFromRegistry();
+	const agentDir = context?.agentDir ?? caller?.agentDir;
+	if (cwd) return agentDir === undefined ? [] : [getMemoryRoot(agentDir, cwd)];
+	return memoryRootsFromRegistry(context);
 }
 
 function ensureWithinRoot(targetPath: string, rootPath: string): void {
@@ -274,7 +273,10 @@ interface MemoryCallerBinding {
  * sits in it, so two sessions in one worktree never impersonate each other.
  */
 function findCallerSession(context: ResolveContext): AgentSession | undefined {
-	const refs = (context.agentRegistry ?? AgentRegistry.global()).list();
+	let refs = (context.agentRegistry ?? AgentRegistry.global()).list();
+	if (context.agentDir !== undefined) {
+		refs = refs.filter(ref => ref.session?.agentDir === context.agentDir);
+	}
 	if (context.sessionFile !== undefined) {
 		const byFile = refs.find(ref => ref.session?.sessionFile === context.sessionFile)?.session;
 		if (byFile) return byFile;
@@ -303,7 +305,11 @@ function resolveMemoryCaller(context?: ResolveContext): MemoryCallerBinding {
 	}
 	// A cwd that names no single live session still scopes the file-backed root,
 	// but it identifies no bank: peer memory ids stay unreachable.
-	return { session: undefined, backend: memoryBackendFromContext(context), legacy: context.cwd === undefined };
+	return {
+		session: undefined,
+		backend: memoryBackendFromContext(context),
+		legacy: context.cwd === undefined && context.agentDir === undefined,
+	};
 }
 
 /**
diff --git a/src/internal-urls/types.ts b/src/internal-urls/types.ts
index 5d7725067619e5e292ade57ede75a80af898e54f..b8eaf90864ec4362e64c92f536421476ab0da595 100644
--- a/src/internal-urls/types.ts
+++ b/src/internal-urls/types.ts
@@ -92,6 +92,8 @@ export interface InternalUrl extends URL {
 export interface ResolveContext {
 	/** Working directory of the calling session. */
 	cwd?: string;
+	/** Agent directory supplied by the calling SDK session. */
+	agentDir?: string;
 	/**
 	 * Calling session's session file. Handlers that resolve agent ids which may
 	 * be parked (`history://<id>`, `agent://<id>`) refresh the caller's
diff --git a/src/mcp/loader.ts b/src/mcp/loader.ts
index 135a0c5dda889be04742636c4fbb6460780caa7e..28083486859fedc687a1fc0d65eca00826a84f3f 100644
--- a/src/mcp/loader.ts
+++ b/src/mcp/loader.ts
@@ -7,7 +7,8 @@ import { logger } from "@oh-my-pi/pi-utils";
 import type { LoadedCustomTool } from "../extensibility/custom-tools/types";
 import { AgentStorage } from "../session/agent-storage";
 import type { AuthStorage } from "../session/auth-storage";
-import { type MCPLoadResult, MCPManager } from "./manager";
+import { type MCPConfigLoader, type MCPLoadResult, MCPManager } from "./manager";
+import type { LoadMCPConfigsOptions } from "./config";
 import type { McpConnectionStatusEvent } from "./startup-events";
 import { MCPToolCache } from "./tool-cache";
 
@@ -39,6 +40,8 @@ export interface MCPToolsLoadOptions {
 	cacheStorage?: AgentStorage | null;
 	/** Auth storage used to resolve OAuth credentials before initial MCP connect */
 	authStorage?: AuthStorage;
+	configLoader?: MCPConfigLoader;
+	extensionRoots?: LoadMCPConfigsOptions["extensionRoots"];
 }
 
 async function resolveToolCache(storage: AgentStorage | null | undefined): Promise<MCPToolCache | null> {
@@ -61,7 +64,7 @@ async function resolveToolCache(storage: AgentStorage | null | undefined): Promi
  */
 export async function discoverAndLoadMCPTools(cwd: string, options?: MCPToolsLoadOptions): Promise<MCPToolsLoadResult> {
 	const toolCache = await resolveToolCache(options?.cacheStorage);
-	const manager = new MCPManager(cwd, toolCache);
+	const manager = new MCPManager(cwd, toolCache, options?.configLoader);
 	if (options?.authStorage) {
 		manager.setAuthStorage(options.authStorage);
 	}
@@ -73,6 +76,7 @@ export async function discoverAndLoadMCPTools(cwd: string, options?: MCPToolsLoa
 			enableProjectConfig: options?.enableProjectConfig,
 			filterExa: options?.filterExa,
 			filterBrowser: options?.filterBrowser,
+			extensionRoots: options?.extensionRoots,
 		});
 	} catch (error) {
 		// If discovery fails entirely, return empty result
diff --git a/src/modes/controllers/command-controller.ts b/src/modes/controllers/command-controller.ts
index 0c72d5194db9458f4cd9e528c661fcc827d84699..e5ce7e98d297025203dfebe1ec45bfece73bcdf8 100644
--- a/src/modes/controllers/command-controller.ts
+++ b/src/modes/controllers/command-controller.ts
@@ -691,7 +691,7 @@ export class CommandController {
 	async handleMemoryCommand(text: string): Promise<void> {
 		const argumentText = text.slice(7).trim();
 		const action = argumentText.split(/\s+/, 1)[0]?.toLowerCase() || "view";
-		const agentDir = this.ctx.settings.getAgentDir();
+		const agentDir = this.ctx.session.agentDir;
 		const backend = await resolveMemoryBackend(this.ctx.settings);
 
 		if (action === "view") {
diff --git a/src/modes/controllers/input-controller.ts b/src/modes/controllers/input-controller.ts
index c47691bf0b0efc0310a65b51b442160daa83725b..f5f0729d134c5302ccba5585a2a3fd05c161b3ef 100644
--- a/src/modes/controllers/input-controller.ts
+++ b/src/modes/controllers/input-controller.ts
@@ -2242,6 +2242,7 @@ export class InputController {
 				const manager = this.ctx.viewSession.sessionManager;
 				return {
 					cwd: manager.getCwd(),
+					agentDir: this.ctx.viewSession.agentDir,
 					sessionId: manager.getSessionId(),
 					sessionFile: manager.getSessionFile(),
 				};
diff --git a/src/modes/internal-url-autocomplete.ts b/src/modes/internal-url-autocomplete.ts
index b16fe83bd3c9571bbfad41cd14a2f6400963877f..b3dc73f96f8565da3a61a27fcc69e7f9fcc18563 100644
--- a/src/modes/internal-url-autocomplete.ts
+++ b/src/modes/internal-url-autocomplete.ts
@@ -88,7 +88,7 @@ export function extractInternalUrlContext(textBeforeCursor: string): InternalUrl
 }
 
 /** Caller binding shared by tool execution and prompt completion. */
-export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "sessionFile" | "sessionId" | "agentRegistry">;
+export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "agentDir" | "sessionFile" | "sessionId" | "agentRegistry">;
 
 /**
  * Suggestions for the internal-url token ending at the cursor, or `null` when
diff --git a/src/modes/rpc/rpc-mode.ts b/src/modes/rpc/rpc-mode.ts
index 2dd4420ab44d5557e6f96e23a845a3f16ce09a19..0ba76ac1ef5cfb1e3c7aa05e9c34795ee51fb23f 100644
--- a/src/modes/rpc/rpc-mode.ts
+++ b/src/modes/rpc/rpc-mode.ts
@@ -32,6 +32,7 @@ import {
 import { loadSlashCommands } from "../../extensibility/slash-commands";
 import { type Theme, theme } from "../../modes/theme/theme";
 import type { AgentSession } from "../../session/agent-session";
+import type { PromptDeliveryObserver } from "../../session/agent-session-types";
 import { SKILL_PROMPT_MESSAGE_TYPE, USER_INTERRUPT_LABEL } from "../../session/messages";
 import { executeAcpBuiltinSlashCommand } from "../../slash-commands/acp-builtins";
 import { buildAvailableSlashCommands } from "../../slash-commands/available-commands";
@@ -157,6 +158,7 @@ export async function runRpcSkillCommand(
 	invocation: RpcSkillInvocation,
 	streamingBehavior: "steer" | "followUp" = "steer",
 	prebuilt?: BuiltSkillPromptMessage,
+	deliveryObserver?: PromptDeliveryObserver,
 ): Promise<boolean> {
 	const built = prebuilt ?? (await buildSkillPromptMessage(invocation.skill, invocation, "user"));
 	return session.promptCustomMessage(
@@ -167,7 +169,7 @@ export async function runRpcSkillCommand(
 			details: built.details,
 			attribution: "user",
 		},
-		{ streamingBehavior },
+		deliveryObserver ? { streamingBehavior, deliveryObserver } : { streamingBehavior },
 	);
 }
 
@@ -210,10 +212,11 @@ export async function tryRunRpcSkillCommand(
 	session: RpcSkillCommandSession,
 	text: string,
 	streamingBehavior: "steer" | "followUp" = "steer",
+	deliveryObserver?: PromptDeliveryObserver,
 ): Promise<RpcSkillCommandResult | false> {
 	const invocation = resolveRpcSkillInvocation(session, text);
 	if (!invocation) return false;
-	await runRpcSkillCommand(session, invocation, streamingBehavior);
+	await runRpcSkillCommand(session, invocation, streamingBehavior, undefined, deliveryObserver);
 	return { agentInvoked: true };
 }
 
diff --git a/src/modes/utils/interactive-context-helpers.ts b/src/modes/utils/interactive-context-helpers.ts
index c834e91ae9e890c3817c684575b017b8aeced5c0..2643d31083a770e861187da12861c9443828486a 100644
--- a/src/modes/utils/interactive-context-helpers.ts
+++ b/src/modes/utils/interactive-context-helpers.ts
@@ -46,6 +46,7 @@ export async function refreshAssistantMessageLinkTargets(
 	if (hrefs.size === 0) return previous;
 	const resolved = await resolveMarkdownLinkTargets(texts, {
 		cwd: session.sessionManager.getCwd(),
+		agentDir: session.agentDir,
 		sessionFile: session.sessionFile,
 		settings: session.settings,
 		localProtocolOptions: {
diff --git a/src/sdk.ts b/src/sdk.ts
index 4b8eaa1219fcb1dd84dbdb423a497be7497cc1a1..93e8af5111b3d55e9f30e54903f2c5c204c9ca32 100644
--- a/src/sdk.ts
+++ b/src/sdk.ts
@@ -142,6 +142,7 @@ import {
 	parseMCPToolName,
 	shouldFilterBrowserMCPForPrelude,
 } from "./mcp";
+import type { MCPConfigLoader } from "./mcp/manager";
 import { MCP_CONNECTION_STATUS_EVENT_CHANNEL, type McpConnectionStatusEvent } from "./mcp/startup-events";
 import { resolveMCPToolAlias } from "./mcp/tool-bridge";
 import { createSessionMemoryRuntimeContext, resolveMemoryBackend } from "./memory-backend";
@@ -564,6 +565,7 @@ export interface CreateAgentSessionOptions {
 	enableMCP?: boolean;
 	/** Existing MCP manager to reuse when MCP is enabled (skips discovery, propagates to toolSession). */
 	mcpManager?: MCPManager;
+	mcpConfigLoader?: MCPConfigLoader;
 
 	/** Enable LSP integration (tool, formatting, diagnostics, warmup). Default: true */
 	enableLsp?: boolean;
@@ -1826,6 +1828,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 			get cwd() {
 				return sessionManager.getCwd();
 			},
+			agentDir,
 			isToolActive: name => activeToolNames.has(name),
 			setActiveToolNames,
 			toolRegistry,
@@ -2060,7 +2063,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 		if (enableMCP && !mcpManager) {
 			if (deferMCPDiscoveryForUI) {
 				const cacheStorage = settings.getStorage();
-				mcpManager = new MCPManager(cwd, cacheStorage ? new MCPToolCache(cacheStorage) : null);
+				mcpManager = new MCPManager(cwd, cacheStorage ? new MCPToolCache(cacheStorage) : null, options.mcpConfigLoader);
 				mcpManager.setAuthStorage(authStorage);
 				toolSession.mcpManager = mcpManager;
 
@@ -2099,6 +2102,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 					...mcpDiscoverOptions,
 					cacheStorage: settings.getStorage(),
 					authStorage,
+					configLoader: options.mcpConfigLoader,
 				});
 				mcpManager = mcpResult.manager;
 				toolSession.mcpManager = mcpManager;
@@ -2858,7 +2862,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 			cwd,
 			sessionManager,
 			modelRegistry,
-			() => (hasSession ? createSessionMemoryRuntimeContext(session, agentDir, cwd) : undefined),
+			() => (hasSession ? createSessionMemoryRuntimeContext(session, agentDir, sessionManager.getCwd()) : undefined),
 			settings,
 			localProtocolOptions,
 			() => (hasSession ? session.getAsyncJobSnapshot() : null),
diff --git a/src/session/agent-session-types.ts b/src/session/agent-session-types.ts
index 9237d9a59a5a225853258c20c4972cbd1a51f37b..58b03f25027623db725a0461e549a9b2d979a6f8 100644
--- a/src/session/agent-session-types.ts
+++ b/src/session/agent-session-types.ts
@@ -334,8 +334,16 @@ export interface AgentSessionConfig {
 	titleSystemPrompt?: string;
 }
 
+/** SDK hosts observe admission and final delivery of one submitted message. */
+export interface PromptDeliveryObserver {
+	onAccepted(delivery: "prompt" | "steer" | "followUp" | "aside"): void;
+	onConsumed(): void;
+	onDiscarded(): void;
+}
+
 /** Options for AgentSession.prompt(). */
 export interface PromptOptions {
+	deliveryObserver?: PromptDeliveryObserver;
 	/** Whether to expand file-based prompt templates (default: true). */
 	expandPromptTemplates?: boolean;
 	/** Image attachments. */
@@ -384,6 +392,7 @@ export interface SteerOptions {
 
 /** Options for AgentSession.sendUserMessage(). */
 export interface SendUserMessageOptions {
+	deliveryObserver?: PromptDeliveryObserver;
 	/** Queue behavior; omitted starts a turn when idle and steers while streaming. */
 	deliverAs?: "steer" | "followUp" | "aside";
 	/** Explicit billing/initiator attribution. */
diff --git a/src/session/agent-session.ts b/src/session/agent-session.ts
index 6c2b0bb9d23e9c6e6f3e81f38f7aecd46a80708b..882bb3465362f6662d8da7afea41921deb956a1a 100644
--- a/src/session/agent-session.ts
+++ b/src/session/agent-session.ts
@@ -33,9 +33,12 @@ import {
 	type AgentToolResult,
 	type AgentTurnEndContext,
 	AppendOnlyContextManager,
+	ASIDE_MESSAGE_COMMIT,
+	ASIDE_MESSAGE_DISCARD,
 	type AsideMessage,
 	type BeforeToolCallContext,
 	type BeforeToolCallResult,
+	type CommittableAsideMessage,
 	EventLoopKeepalive,
 	type QueuedMessagePreparation,
 	resolveTelemetry,
@@ -254,6 +257,7 @@ import type {
 	HandoffResult,
 	ModelCycleResult,
 	Prewalk,
+	PromptDeliveryObserver,
 	PromptOptions,
 	ResetSessionContextResult,
 	ResolvedRoleModel,
@@ -367,10 +371,7 @@ import {
 import type { BuildSessionContextOptions, SessionContext } from "./session-context";
 import {
 	getRestorableSessionModels,
-	isTranscriptEntry,
-	isUserRequestEntry,
-	transcriptEntryMessage,
-	userTurnDraft,
+	resolveTreeNavigationTarget,
 } from "./session-context";
 import { formatSessionDumpText } from "./session-dump-format";
 import type { BranchSummaryEntry, NewSessionOptions } from "./session-entries";
@@ -572,6 +573,7 @@ export class AgentSession {
 	readonly agent: Agent;
 	readonly sessionManager: SessionManager;
 	readonly settings: Settings;
+	readonly agentDir: string;
 	/** Session-start policy, independent of the selected project memory backend. */
 	readonly memoryEnabled: boolean;
 	/** Entries of tools mounted under `xd://`; empty when virtual devices are unmounted. */
@@ -699,6 +701,7 @@ export class AgentSession {
 	/** Host hook invoked when a typed user prompt is dropped before dispatch;
 	 *  see {@link setPromptDropped}. */
 	#promptDropped: ((prompt: DroppedPrompt) => void) | undefined;
+	#promptDeliveries: Map<CommittableAsideMessage, PromptDeliveryObserver> | undefined;
 	#titleGenerationAbortController = new AbortController();
 	#toolChoiceQueue = new ToolChoiceQueue();
 
@@ -759,6 +762,7 @@ export class AgentSession {
 	#messageEndPersistenceTail: Promise<void> = Promise.resolve();
 	#pendingMessageEndPersistence = new Map<string, Promise<void>>();
 	#persistedMessageKeys: { anchor: string; keys: Set<string> } | undefined;
+	#assistantIdentity: { messageId: string; phase: "streaming" | "settled" } | undefined;
 
 	// Custom commands (TypeScript slash commands)
 	#customCommands: LoadedCustomCommand[] = [];
@@ -1041,6 +1045,8 @@ export class AgentSession {
 		for (const record of records) {
 			this.agent.emitExternalEvent({ type: "message_start", message: record });
 			this.agent.emitExternalEvent({ type: "message_end", message: record });
+			const committable: CommittableAsideMessage = record;
+			committable[ASIDE_MESSAGE_COMMIT]?.();
 		}
 	}
 
@@ -1298,6 +1304,7 @@ export class AgentSession {
 		this.sessionManager = config.sessionManager;
 		this.settings = config.settings;
 		this.memoryEnabled = config.memoryEnabled ?? true;
+		this.agentDir = config.memoryAgentDir ?? config.settings.getAgentDir();
 		this.#modelRegistry = config.modelRegistry;
 		this.#extensionRoots =
 			config.extensionRoots ??
@@ -2661,6 +2668,40 @@ export class AgentSession {
 	 * event/persistence pipeline during teardown.
 	 */
 	#handleAgentEvent = (event: AgentEvent): Promise<void> => {
+		// Core snapshots every delta but serializes assistant lifecycles; user, tool and custom events can interleave.
+		if (
+			(event.type === "message_start" ||
+				event.type === "message_update" ||
+				event.type === "message_end" ||
+				event.type === "turn_end") &&
+			event.message.role === "assistant"
+		) {
+			const message = event.message;
+			const existing =
+				"messageId" in message && typeof message.messageId === "string" && message.messageId.length > 0
+					? message.messageId
+					: undefined;
+			const identity = this.#assistantIdentity;
+			const messageId =
+				existing ??
+				(event.type !== "message_start" &&
+				(identity?.phase === "streaming" || event.type === "turn_end")
+					? identity?.messageId
+					: undefined) ??
+				crypto.randomUUID();
+			Object.assign(message, { messageId });
+			if (event.type === "message_update") {
+				const update = event.assistantMessageEvent;
+				if ("partial" in update) Object.assign(update.partial, { messageId });
+				else if ("message" in update) Object.assign(update.message, { messageId });
+				else if ("error" in update) Object.assign(update.error, { messageId });
+			}
+			const phase = event.type === "message_end" || event.type === "turn_end" ? "settled" : "streaming";
+			if (identity?.messageId === messageId) identity.phase = phase;
+			else this.#assistantIdentity = { messageId, phase };
+		} else if (event.type === "agent_start" || event.type === "agent_end") {
+			this.#assistantIdentity = undefined;
+		}
 		const processing = this.#dispatchAgentEvent(event);
 		this.#inFlightEventHandlers.add(processing);
 		void processing.finally(() => this.#inFlightEventHandlers.delete(processing)).catch(() => {});
@@ -2817,27 +2858,17 @@ export class AgentSession {
 		return keys;
 	}
 
-	/**
-	 * True when {@link message} is structurally identical to a message already
-	 * appended to the current branch. Uses the current branch's memoized
-	 * persistence-key cache for the common missing-key case, and only walks the
-	 * branch to verify content when a key hit could be a rare collision.
-	 *
-	 * Error turns need one extra discriminator. An empty error turn carries no
-	 * content at all, so every failed attempt of one retry saga serializes to the
-	 * same `[]` — and when two attempts land in the same wall-clock millisecond
-	 * (mocked/zero retry delay, or a fast provider failure) they also share a
-	 * persistence key of `assistant:<ts>:<provider>:<model>::error`. Content
-	 * equality alone then reports the aggregated terminal turn ("Retry budget
-	 * exhausted after N retries: …") as a duplicate of the attempt error it
-	 * supersedes, and the session journal silently loses the record of why the
-	 * run stopped. The failure text is the only thing that distinguishes them.
-	 */
 	#sessionMessageAlreadyPersisted(message: AgentMessage): boolean {
 		const key = sessionMessagePersistenceKey(message);
 		if (key === undefined) return false;
 		const keys = this.#ensurePersistedMessageKeys();
 		if (!keys.has(key)) return false;
+		if (
+			message.role === "assistant" &&
+			"messageId" in message &&
+			typeof message.messageId === "string" &&
+			message.messageId.length > 0
+		) return true;
 		const branch = this.sessionManager.getBranch();
 		for (let index = branch.length - 1; index >= 0; index--) {
 			const entry = branch[index];
@@ -4675,7 +4706,7 @@ export class AgentSession {
 
 	/**
 	 * Synchronously mark the session as disposing so new work is rejected
-	 * immediately: eval starts throw, queued asides are dropped, and the
+	 * immediately: eval starts throw, queued asides are flushed, and the
 	 * aside provider is detached. Idempotent; `dispose()` runs it first.
 	 *
 	 * Wrappers that await other teardown before delegating to `dispose()` MUST
@@ -4683,6 +4714,7 @@ export class AgentSession {
 	 * gap slips past the disposal guards.
 	 */
 	beginDispose(): void {
+		if (this.#isDisposed) return;
 		this.#isDisposed = true;
 		this.#modelDiscoveryAbortController.abort();
 		this.#queuedMessageDrainBlocked = false;
@@ -4698,6 +4730,7 @@ export class AgentSession {
 		this.#titleGenerationAbortController.abort();
 		this.#abortAutolearnCapture();
 		this.#irc.flushPending();
+		this.#discardPromptDeliveries();
 		this.yieldQueue.clear();
 		this.agent.setAsideMessageProvider(undefined);
 		this.agent.hasIrcInterrupts = undefined;
@@ -4717,7 +4750,11 @@ export class AgentSession {
 	 */
 	#disposeCall?: Promise<void>;
 	dispose(options: AgentSessionDisposeOptions = {}): Promise<void> {
-		if (!this.#disposeCall) this.#disposeCall = this.#doDispose(options);
+		if (!this.#disposeCall) {
+			const { promise, resolve, reject } = Promise.withResolvers<void>();
+			this.#disposeCall = promise;
+			void this.#doDispose(options).then(resolve, reject);
+		}
 		return this.#disposeCall;
 	}
 
@@ -5108,6 +5145,8 @@ export class AgentSession {
 		// Drop the conversation: messages, queued steers/follow-ups, pending tool
 		// calls, and error state. agent.reset() keeps the model and system prompt.
 		this.#releaseQueuedTtsrReservations();
+		this.#discardPromptDeliveries(this.agent.peekSteeringQueue());
+		this.#discardPromptDeliveries(this.agent.peekFollowUpQueue());
 		this.agent.reset();
 		this.#pendingNextTurnMessages = [];
 		this.#experimentalContextNotesReminder = undefined;
@@ -5834,6 +5873,10 @@ export class AgentSession {
 		const filteredFollowUp = followUp.filter(message => !isVibeContext(message));
 		if (filteredSteering.length !== steering.length || filteredFollowUp.length !== followUp.length) {
 			this.agent.replaceQueues(filteredSteering, filteredFollowUp);
+			if (this.#promptDeliveries) {
+				this.#discardPromptDeliveries(steering.filter(isVibeContext));
+				this.#discardPromptDeliveries(followUp.filter(isVibeContext));
+			}
 			this.#reconcileQueuedMessageDrain();
 		}
 		this.#pendingNextTurnMessages = this.#pendingNextTurnMessages.filter(message => !isVibeContext(message));
@@ -6414,6 +6457,7 @@ export class AgentSession {
 			await this.#queueUserMessage(expandedText, options?.images, streamingBehavior, {
 				timestamp: submittedAt,
 				attribution: promptAttribution,
+				deliveryObserver: options?.deliveryObserver,
 			});
 			outcome.sessionClaimed = true;
 			return true;
@@ -6471,6 +6515,7 @@ export class AgentSession {
 					images: normalizedImages,
 					descriptionNotice: imageDescriptionNotice,
 				},
+				deliveryObserver: options?.deliveryObserver,
 			});
 			outcome.sessionClaimed = true;
 			return true;
@@ -6555,7 +6600,7 @@ export class AgentSession {
 	 */
 	async promptCustomMessage<T = unknown>(
 		message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">,
-		options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice"> & {
+		options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice" | "deliveryObserver"> & {
 			queueChipText?: string;
 			queueOnly?: boolean;
 		},
@@ -6565,7 +6610,7 @@ export class AgentSession {
 
 	async #promptCustomMessage<T = unknown>(
 		message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">,
-		options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice"> & {
+		options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice" | "deliveryObserver"> & {
 			queueChipText?: string;
 			queueOnly?: boolean;
 		},
@@ -6587,7 +6632,7 @@ export class AgentSession {
 	async #dispatchCustomPrompt<T = unknown>(
 		message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">,
 		options:
-			| (Pick<PromptOptions, "streamingBehavior" | "toolChoice"> & {
+			| (Pick<PromptOptions, "streamingBehavior" | "toolChoice" | "deliveryObserver"> & {
 					queueChipText?: string;
 					queueOnly?: boolean;
 			  })
@@ -6628,7 +6673,7 @@ export class AgentSession {
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
 			}
-			await this.#queueCustomMessage(message, streamingBehavior, options.queueChipText);
+			await this.#queueCustomMessage(message, streamingBehavior, options.queueChipText, options.deliveryObserver);
 			outcome.sessionClaimed = true;
 			return true;
 		}
@@ -6644,7 +6689,7 @@ export class AgentSession {
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
 			}
-			await this.#queueCustomMessage(message, streamingBehavior, options?.queueChipText);
+			await this.#queueCustomMessage(message, streamingBehavior, options?.queueChipText, options?.deliveryObserver);
 			outcome.sessionClaimed = true;
 			return true;
 		}
@@ -6790,7 +6835,7 @@ export class AgentSession {
 	async #promptWithMessage(
 		message: AgentMessage,
 		expandedText: string,
-		options?: Pick<PromptOptions, "toolChoice" | "images" | "skipCompactionCheck"> & {
+		options?: Pick<PromptOptions, "toolChoice" | "images" | "skipCompactionCheck" | "deliveryObserver"> & {
 			prependMessages?: AgentMessage[];
 			skipPostPromptRecoveryWait?: boolean;
 			acceptTerminalEmptyStop?: boolean;
@@ -6800,6 +6845,13 @@ export class AgentSession {
 		// every pre-dispatch bail (generation bump from abort, disposal, usage
 		// preflight denial) exits silently, and prompt() uses the outcome to hand
 		// the typed text back to the host instead of losing it.
+		if (options?.deliveryObserver) {
+			if (this.#isDisposed) {
+				this.#notifyPromptDelivery(options.deliveryObserver, "discarded");
+				return false;
+			}
+			this.#observePromptDelivery(message, options.deliveryObserver, "prompt");
+		}
 		this.#beginInFlight();
 		const generation = this.#promptGeneration;
 		this.#promptSequence++;
@@ -7001,6 +7053,9 @@ export class AgentSession {
 			}
 			return true;
 		} finally {
+			if (options?.deliveryObserver && this.#promptDeliveries?.has(message)) {
+				this.#discardPromptDeliveries([message]);
+			}
 			// The per-turn before_agent_start override lives only for this turn.
 			this.#tools.clearTurnSystemPromptOverride();
 			this.#usagePreflightReadyForNextModelCall = false;
@@ -7259,6 +7314,61 @@ export class AgentSession {
 		}
 	}
 
+	#notifyPromptDelivery(
+		observer: PromptDeliveryObserver,
+		delivery: "prompt" | "steer" | "followUp" | "aside" | "consumed" | "discarded",
+	): void {
+		try {
+			if (delivery === "consumed") observer.onConsumed();
+			else if (delivery === "discarded") observer.onDiscarded();
+			else observer.onAccepted(delivery);
+		} catch {
+			logger.warn("Prompt delivery observer failed", { delivery });
+		}
+	}
+
+	#observePromptDelivery(
+		message: CommittableAsideMessage,
+		observer: PromptDeliveryObserver,
+		delivery: "prompt" | "steer" | "followUp" | "aside",
+	): void {
+		this.#promptDeliveries ??= new Map();
+		this.#promptDeliveries.set(message, observer);
+		const commit = message[ASIDE_MESSAGE_COMMIT];
+		const discard = message[ASIDE_MESSAGE_DISCARD];
+		const finish = (consumed: boolean, error?: Error): void => {
+			const pending = this.#promptDeliveries?.get(message);
+			if (!pending) return;
+			this.#promptDeliveries?.delete(message);
+			if (this.#promptDeliveries?.size === 0) this.#promptDeliveries = undefined;
+			if (commit) message[ASIDE_MESSAGE_COMMIT] = commit;
+			else delete message[ASIDE_MESSAGE_COMMIT];
+			if (discard) message[ASIDE_MESSAGE_DISCARD] = discard;
+			else delete message[ASIDE_MESSAGE_DISCARD];
+			try {
+				if (consumed) commit?.();
+				else if (error) discard?.(error);
+			} finally {
+				if (consumed && delivery === "prompt") this.#notifyPromptDelivery(pending, "prompt");
+				this.#notifyPromptDelivery(pending, consumed ? "consumed" : "discarded");
+			}
+		};
+		message[ASIDE_MESSAGE_COMMIT] = () => finish(true);
+		message[ASIDE_MESSAGE_DISCARD] = error => finish(false, error);
+	}
+
+	#discardPromptDeliveries(messages?: Iterable<AgentMessage>): void {
+		const pending = this.#promptDeliveries;
+		if (!pending) return;
+		let error: Error | undefined;
+		for (const message of messages ?? pending.keys()) {
+			if (!pending.has(message)) continue;
+			error ??= new Error("Prompt removed before context insertion");
+			const committable: CommittableAsideMessage = message;
+			committable[ASIDE_MESSAGE_DISCARD]?.(error);
+		}
+	}
+
 	async #queueUserMessage(
 		text: string,
 		images: ImageContent[] | undefined,
@@ -7267,11 +7377,13 @@ export class AgentSession {
 			timestamp?: number;
 			attribution?: MessageAttribution;
 			preprocessed?: { images: ImageContent[] | undefined; descriptionNotice: CustomMessage | undefined };
+			deliveryObserver?: PromptDeliveryObserver;
 		},
 	): Promise<void> {
 		const attribution = options?.attribution ?? "user";
 		const timestamp = options?.timestamp;
 		const preprocessed = options?.preprocessed;
+		const deliveryObserver = options?.deliveryObserver;
 		// Captured before any await below so the aside branch can detect a
 		// newSession()/switchSession() that completed while normalization/vision
 		// description was in flight and drop a record that would otherwise land in a
@@ -7301,12 +7413,25 @@ export class AgentSession {
 			: normalizedImages?.length
 				? await this.#buildImageDescriptionNotice(normalizedImages)
 				: undefined;
+		if ((mode === "aside" || deliveryObserver) && await this.#sessionGenerationChanged(sessionGeneration)) {
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		if (deliveryObserver && this.#isDisposed) {
+			this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		const message: CommittableAsideMessage = {
+			role: "user", content, attribution, timestamp: timestamp ?? Date.now(),
+		};
+		if (mode === "steer") message.steering = true;
+		if (deliveryObserver) this.#observePromptDelivery(message, deliveryObserver, mode);
 		if (mode === "aside") {
-			if (await this.#sessionGenerationChanged(sessionGeneration)) return;
 			const records: AgentMessage[] = [];
 			if (imageDescriptionNotice) records.push(imageDescriptionNotice);
-			records.push({ role: "user", content, attribution, timestamp: timestamp ?? Date.now() });
+			records.push(message);
 			this.#irc.queueAside(records);
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "aside");
 			// The awaits above (image normalization / vision description) can span the run's
 			// settle, so the run may already be idle by the time the record lands in the aside
 			// queue with no loop left to drain it. Resuming here is a no-op while streaming and
@@ -7318,23 +7443,13 @@ export class AgentSession {
 		if (mode === "followUp") {
 			for (const notice of videoAttachmentNotices) this.agent.followUp(notice);
 			if (imageDescriptionNotice) this.agent.followUp(imageDescriptionNotice);
-			this.agent.followUp({
-				role: "user",
-				content,
-				attribution,
-				timestamp: timestamp ?? Date.now(),
-			});
+			this.agent.followUp(message);
 		} else {
 			for (const notice of videoAttachmentNotices) this.agent.steer(notice);
 			if (imageDescriptionNotice) this.agent.steer(imageDescriptionNotice);
-			this.agent.steer({
-				role: "user",
-				content,
-				steering: true,
-				attribution,
-				timestamp: timestamp ?? Date.now(),
-			});
+			this.agent.steer(message);
 		}
+		if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, mode);
 		this.#scheduleIdleQueueDrain();
 	}
 
@@ -7530,6 +7645,7 @@ export class AgentSession {
 		message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">,
 		deliverAs: "steer" | "followUp" | "aside",
 		queueChipText?: string,
+		deliveryObserver?: PromptDeliveryObserver,
 	): Promise<void> {
 		// Captured before the normalization await below — see #sessionGeneration's doc comment.
 		const sessionGeneration = this.#sessionGeneration;
@@ -7553,12 +7669,21 @@ export class AgentSession {
 			timestamp: Date.now(),
 		};
 		const normalizedAppMessage = await this.#normalizeAgentMessageImages(appMessage);
+		if ((deliverAs === "aside" || deliveryObserver) && await this.#sessionGenerationChanged(sessionGeneration)) {
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		if (deliveryObserver && this.#isDisposed) {
+			this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		if (deliveryObserver) this.#observePromptDelivery(normalizedAppMessage, deliveryObserver, deliverAs);
 		if (deliverAs === "aside") {
-			if (await this.#sessionGenerationChanged(sessionGeneration)) return;
 			// Non-interrupting: rides the same step-boundary aside poll as
 			// sendCustomMessage's streaming aside branch — not an agent-core queue
 			// entry, so no drain-retry latch and no idle-queue drain scheduling.
 			this.#irc.queueAside([normalizedAppMessage]);
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "aside");
 			// The image-normalization await above can span the run's settle, so the run may
 			// already be idle by the time the record lands in the aside queue with no loop
 			// left to drain it. Resuming here is a no-op while streaming and wakes/folds
@@ -7572,6 +7697,7 @@ export class AgentSession {
 		} else {
 			this.agent.steer(normalizedAppMessage);
 		}
+		if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, deliverAs);
 		this.#scheduleIdleQueueDrain();
 	}
 
@@ -7795,17 +7921,17 @@ export class AgentSession {
 		let deliveredAsAside = false;
 		if (options?.deliverAs === "aside") {
 			if (this.isStreaming) {
-				await this.#queueUserMessage(text, images, "aside", { attribution: options.attribution });
+				await this.#queueUserMessage(text, images, "aside", options);
 				return;
 			}
 			// Idle: fall through to the prompt flow below (starts a turn, like an omitted
 			// deliverAs) — there is no live run to inject an aside into.
 			deliveredAsAside = true;
 		} else if (options?.deliverAs === "followUp") {
-			await this.#queueUserMessage(text, images, "followUp", { attribution: options.attribution });
+			await this.#queueUserMessage(text, images, "followUp", options);
 			return;
 		} else if (options?.deliverAs === "steer") {
-			await this.#queueUserMessage(text, images, "steer", { attribution: options.attribution });
+			await this.#queueUserMessage(text, images, "steer", options);
 			return;
 		}
 
@@ -7821,6 +7947,7 @@ export class AgentSession {
 			expandPromptTemplates: false,
 			images,
 			streamingBehavior: deliveredAsAside ? "aside" : "steer",
+			deliveryObserver: options?.deliveryObserver,
 		});
 	}
 
@@ -7849,6 +7976,10 @@ export class AgentSession {
 			}
 		}
 		this.agent.replaceQueues(steeringAll.filter(keep), followUpAll.filter(keep));
+		if (this.#promptDeliveries) {
+			this.#discardPromptDeliveries(steeringAll.filter(message => !keep(message)));
+			this.#discardPromptDeliveries(followUpAll.filter(message => !keep(message)));
+		}
 		this.#reconcileQueuedMessageDrain();
 		return { steering, followUp };
 	}
@@ -7899,6 +8030,7 @@ export class AgentSession {
 		if (fromSteer >= 0) {
 			const removed = steering[fromSteer];
 			this.agent.replaceQueues(removeWithCompanions(steering, fromSteer), followUp.slice());
+			if (this.#promptDeliveries) this.#discardPromptDeliveries([removed]);
 			this.#reconcileQueuedMessageDrain();
 			return toRestoredQueuedMessage(removed);
 		}
@@ -7906,6 +8038,7 @@ export class AgentSession {
 		if (fromFollowUp >= 0) {
 			const removed = followUp[fromFollowUp];
 			this.agent.replaceQueues(steering.slice(), removeWithCompanions(followUp, fromFollowUp));
+			if (this.#promptDeliveries) this.#discardPromptDeliveries([removed]);
 			this.#reconcileQueuedMessageDrain();
 			return toRestoredQueuedMessage(removed);
 		}
@@ -8334,6 +8467,7 @@ export class AgentSession {
 			await this.#advisors.drainAndDetachRecorders();
 			try {
 				this.#releaseQueuedTtsrReservations();
+				this.#discardPromptDeliveries();
 				this.agent.reset();
 				if (options?.drop && previousSessionFile) {
 					try {
@@ -9376,6 +9510,9 @@ export class AgentSession {
 				break;
 			}
 			if (event.type === "error") {
+				if (event.reason === "aborted") {
+					throw new DOMException(event.error.errorMessage || "Ephemeral turn aborted", "AbortError");
+				}
 				throw new Error(event.error.errorMessage || "Ephemeral turn failed");
 			}
 		}
@@ -9751,6 +9888,11 @@ export class AgentSession {
 			}
 			generationSettled.resolve();
 			this.#sessionGenerationSettled = previousSessionGenerationSettled;
+			this.#discardPromptDeliveries(previousSteeringMessages);
+			this.#discardPromptDeliveries(previousFollowUpMessages);
+			this.#discardPromptDeliveries(previousIrcPending.interrupts);
+			this.#discardPromptDeliveries(previousIrcPending.asides);
+			this.#discardPromptDeliveries(previousIrcPending.deferredWakes);
 			return true;
 		} catch (error) {
 			this.sessionManager.restoreState(previousSessionState);
@@ -10014,6 +10156,8 @@ export class AgentSession {
 		this.#pendingNextTurnMessages = [];
 		this.#scheduledHiddenNextTurnGeneration = undefined;
 		this.#releaseQueuedTtsrReservations();
+		this.#discardPromptDeliveries(this.agent.peekSteeringQueue());
+		this.#discardPromptDeliveries(this.agent.peekFollowUpQueue());
 		this.agent.replaceQueues([], []);
 		this.#queuedMessageDrainBlocked = false;
 		this.#usagePreflightReadyForNextModelCall = false;
@@ -10103,6 +10247,9 @@ export class AgentSession {
 		options: {
 			summarize?: boolean;
 			customInstructions?: string;
+			requireIdle?: boolean;
+			/** Hosts settle and publish replacement before post-navigation hooks can start work. */
+			onHistoryReplaced?: () => void | Promise<void>;
 			/**
 			 * Opts into the two-phase `ask` toolResult re-answer protocol
 			 * (issue #5642): set only by the interactive `/tree` selector, which
@@ -10130,6 +10277,7 @@ export class AgentSession {
 		editorImages?: ImageContent[];
 		cancelled: boolean;
 		aborted?: boolean;
+		busy?: boolean;
 		summaryEntry?: BranchSummaryEntry;
 		/** Raw session context built during navigation — pass to renderInitialMessages to skip a second O(N) walk. */
 		sessionContext?: SessionContext;
@@ -10151,9 +10299,30 @@ export class AgentSession {
 		 */
 		askReanswerCommitted?: boolean;
 	}> {
+		const isBusy = () =>
+			this.isDisposed ||
+			this.hasAdmittedSubmission ||
+			this.isStreaming ||
+			this.isAborting ||
+			this.#abortInProgress ||
+			this.isRetrying ||
+			this.isCompacting ||
+			this.isGeneratingHandoff ||
+			this.isBashRunning ||
+			this.isEvalRunning ||
+			this.hasPendingBashMessages ||
+			this.hasPendingPythonMessages ||
+			this.hasPostPromptWork ||
+			this.agent.peekSteeringQueue().length > 0 ||
+			this.agent.peekFollowUpQueue().length > 0 ||
+			this.#pendingNextTurnMessages.length > 0 ||
+			(this.#asyncJobManager?.getRunningJobs(this.#agentId ? { ownerId: this.#agentId } : undefined).length ?? 0) > 0 ||
+			(this.#asyncJobManager?.hasPendingDeliveries(this.#agentId ? { ownerId: this.#agentId } : undefined) ?? false);
+		if (options.requireIdle && (this.isSessionTransitioning || isBusy())) return { cancelled: false, busy: true };
 		using _transition = this.#beginSessionTransition();
-		await this.#bash.flushPending();
+		if (!options.requireIdle) await this.#bash.flushPending();
 		const oldLeafId = this.sessionManager.getLeafId();
+		const historyVersion = options.requireIdle ? this.sessionManager.getHistoryVersion() : undefined;
 
 		const targetEntry = this.sessionManager.getEntry(targetId);
 		if (!targetEntry) {
@@ -10302,41 +10471,29 @@ export class AgentSession {
 			summaryDetails = hookSummary.details;
 		}
 
+		if (
+			options.requireIdle &&
+			(this.#sessionTransitionDepth > 1 || isBusy() || this.sessionManager.getHistoryVersion() !== historyVersion)
+		) {
+			this.#branchSummaryAbortController = undefined;
+			return { cancelled: false, busy: true };
+		}
+
 		// All cancellation/no-op exits are behind us. Invalidate prompt setup
 		// admitted on the abandoned branch before committing any tree changes.
 		this.#promptGeneration++;
 
 		// Determine the new leaf position based on target type
-		let newLeafId: string | null;
-		let editorText: string | undefined;
-		let editorImages: ImageContent[] | undefined;
+		const target = resolveTreeNavigationTarget(targetEntry, oldLeafId);
+		let newLeafId = target.leafId;
+		const editorText = target.draft?.text;
+		const editorImages = target.draft?.images.length ? target.draft.images : undefined;
 		// Set when the second-pass `ask` re-answer branch below actually commits a
 		// new sibling answer — the trigger for resuming the agent afterwards so the
 		// model consumes it, mirroring a live `ask` completion (issue #6483).
 		let isAskReanswerCompletion = false;
 
-		if (isTranscriptEntry(targetEntry) && isUserRequestEntry(targetEntry)) {
-			// User request (plain prompt, or a user-invoked skill/collab prompt): leaf = parent
-			// (null if root), the draft the user typed goes back to the editor with its images.
-			newLeafId = targetEntry.parentId;
-			editorText = userTurnDraft(targetEntry);
-			const request = transcriptEntryMessage(targetEntry);
-			const targetImages =
-				request && (request.role === "user" || request.role === "custom")
-					? this.#extractUserMessageImages(request.content)
-					: [];
-			if (targetImages.length > 0) editorImages = targetImages;
-		} else if (targetEntry.type === "custom_message" && targetEntry.customType !== SKILL_PROMPT_MESSAGE_TYPE) {
-			// Other custom message: leaf = parent (null if root), text goes to editor
-			newLeafId = targetEntry.parentId;
-			editorText =
-				typeof targetEntry.content === "string"
-					? targetEntry.content
-					: targetEntry.content
-							.filter((c): c is { type: "text"; text: string } => c.type === "text")
-							.map(c => c.text)
-							.join("");
-		} else if (
+		if (
 			targetEntry.type === "message" &&
 			targetEntry.message.role === "toolResult" &&
 			targetEntry.message.toolName === "ask" &&
@@ -10358,12 +10515,6 @@ export class AgentSession {
 			};
 			newLeafId = this.sessionManager.appendMessageToBranch(toolResultMessage, targetEntry.parentId);
 			isAskReanswerCompletion = true;
-		} else {
-			// Non-user message (or an agent/autoload skill-prompt injection): land the
-			// leaf on the selected node so it stays on the active branch. Injected skill
-			// prompts must not be re-editable — their content is a large expanded body,
-			// not a user turn (issue #5374).
-			newLeafId = targetId;
 		}
 
 		// Switch leaf (with or without summary)
@@ -10403,6 +10554,7 @@ export class AgentSession {
 		this.#closeCodexProviderSessionsForHistoryRewrite();
 
 		this.#branchSummaryAbortController = undefined;
+		if (options.onHistoryReplaced) await options.onHistoryReplaced();
 
 		// Report a committed `ask` re-answer so the interactive caller can resume
 		// the agent via `resumeAfterAskReanswer()` *after* rebuilding its
diff --git a/src/session/artifacts.ts b/src/session/artifacts.ts
index b617d0523f1f4163d3ee8182c82824eb6d5fe625..cfa66a94a29608a379a2fb58c222c04af940e180 100644
--- a/src/session/artifacts.ts
+++ b/src/session/artifacts.ts
@@ -4,7 +4,7 @@
  * Artifacts are stored in a directory alongside the session file,
  * accessible via artifact:// URLs.
  */
-import * as fs from "node:fs/promises";
+import fs from "node:fs/promises";
 import * as path from "node:path";
 import { replaceFileAtomically } from "../utils/atomic-file";
 
@@ -41,20 +41,20 @@ function sanitizeToolType(toolType: string): string {
  *
  * Returns the verified UTF-8 byte count.
  */
-export async function writeArtifact(path: string, content: string): Promise<number> {
+export async function writeArtifact(path: string, content: string, signal?: AbortSignal): Promise<number> {
+	signal?.throwIfAborted();
 	const expectedBytes = Buffer.byteLength(content);
 	const tempPath = `${path}.tmp-${crypto.randomUUID()}`;
 	try {
-		const writtenBytes = await Bun.write(tempPath, content);
-		if (writtenBytes !== expectedBytes) {
-			throw new Error(`Artifact write incomplete: wrote ${writtenBytes} of ${expectedBytes} bytes`);
-		}
+		await fs.writeFile(tempPath, content, { signal });
 		const file = Bun.file(tempPath);
 		if (file.size !== expectedBytes) {
 			throw new Error(`Artifact size mismatch: found ${file.size} of ${expectedBytes} bytes`);
 		}
 		await file.slice(0, Math.min(expectedBytes, 1)).arrayBuffer();
+		signal?.throwIfAborted();
 		await replaceFileAtomically(tempPath, path);
+		signal?.throwIfAborted();
 	} catch (error) {
 		await fs.rm(tempPath, { force: true });
 		throw error;
@@ -150,9 +150,10 @@ export class ArtifactManager {
 	 * @param toolType Tool name for file extension (e.g., "bash", "read")
 	 * @returns Artifact ID (numeric string)
 	 */
-	async save(content: string, toolType: string): Promise<string> {
+	async save(content: string, toolType: string, signal?: AbortSignal): Promise<string> {
+		signal?.throwIfAborted();
 		const { id, path } = await this.allocatePath(toolType);
-		await writeArtifact(path, content);
+		await writeArtifact(path, content, signal);
 		return id;
 	}
 
diff --git a/src/session/irc-bridge.ts b/src/session/irc-bridge.ts
index 35c0a35b1d19ec438d8cea17c9f957caa13efbb2..462fc3a06c4b8ca7d9d6d78fd7929629e9abb6bb 100644
--- a/src/session/irc-bridge.ts
+++ b/src/session/irc-bridge.ts
@@ -1,4 +1,4 @@
-import type { Agent, AgentMessage } from "@oh-my-pi/pi-agent-core";
+import { type Agent, type AgentMessage, ASIDE_MESSAGE_COMMIT, type CommittableAsideMessage } from "@oh-my-pi/pi-agent-core";
 import { logger, prompt } from "@oh-my-pi/pi-utils";
 import type { Settings } from "../config/settings";
 import { IrcBus, type IrcMessage } from "../irc/bus";
@@ -256,6 +256,8 @@ export class IrcBridge {
 		for (const record of this.drainPending()) {
 			this.#host.agent.emitExternalEvent({ type: "message_start", message: record });
 			this.#host.agent.emitExternalEvent({ type: "message_end", message: record });
+			const committable: CommittableAsideMessage = record;
+			committable[ASIDE_MESSAGE_COMMIT]?.();
 		}
 	}
 
diff --git a/src/session/session-context.ts b/src/session/session-context.ts
index 777d8c093350f38b70c21f6eef4db266e8f0eb84..b7fb3a9e6563badeb2439263163aa7b6e5b9ca34 100644
--- a/src/session/session-context.ts
+++ b/src/session/session-context.ts
@@ -2,11 +2,13 @@ import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
 import { getAnthropicCompactionPayload, isTurnStartEntry } from "@oh-my-pi/pi-agent-core/compaction";
 import {
 	coerceServiceTierByFamily,
+	type ImageContent,
 	type OpenAIResponsesHistoryPayload,
 	type ServiceTierByFamily,
 } from "@oh-my-pi/pi-ai";
 import * as snapcompact from "@oh-my-pi/snapcompact";
 import { isRecord } from "@oh-my-pi/pi-utils";
+import { getLatestTodoPhasesFromEntries, type TodoPhase } from "../tools/todo";
 import {
 	type CustomMessage,
 	createBranchSummaryMessage,
@@ -18,6 +20,7 @@ import {
 	isUserTurnInitiator,
 	normalizeCustomMessagePayload,
 	PREWALK_PLAN_MESSAGE_TYPE,
+	SKILL_PROMPT_MESSAGE_TYPE,
 	VIBE_MODE_CONTEXT_MESSAGE_TYPE,
 } from "./messages";
 import { CONTEXT_NOTES_ENTRY_TYPE, getContextNotes, renderContextNotes } from "./context-notes";
@@ -100,6 +103,7 @@ function snapcompactHistoryBlockOptions(
 
 export interface SessionContext {
 	messages: AgentMessage[];
+	todoPhases?: TodoPhase[] | null;
 	thinkingLevel?: string;
 	/** Configured thinking selector (`"auto"` or a concrete level) from the latest change. */
 	configuredThinkingLevel?: string;
@@ -158,6 +162,8 @@ export interface BuildSessionContextOptions {
 	 * latest compacted tail.
 	 */
 	transcript?: boolean;
+	/** Read-only snapshot loaders request canonical todos before history compaction. */
+	includeTodoPhases?: boolean;
 	/** In transcript mode, elide entries replaced by the latest compaction. */
 	collapseCompactedHistory?: boolean;
 	/**
@@ -282,6 +288,26 @@ function textContent(content: string | ReadonlyArray<{ type: string; text?: stri
 	return text;
 }
 
+/** Resolve plain tree navigation without changing the session or reopening tools. */
+export function resolveTreeNavigationTarget(
+	entry: SessionEntry,
+	currentLeafId: string | null,
+): { leafId: string | null; draft: { text: string; images: ImageContent[] } | null } {
+	if (entry.id === currentLeafId && !(entry.type === "message" && entry.message.role === "user")) {
+		return { leafId: currentLeafId, draft: null };
+	}
+	if (isTranscriptEntry(entry) && isUserRequestEntry(entry)) {
+		const request = transcriptEntryMessage(entry);
+		const content = request && (request.role === "user" || request.role === "custom") ? request.content : [];
+		const images = Array.isArray(content) ? content.filter((block): block is ImageContent => block.type === "image") : [];
+		return { leafId: entry.parentId, draft: { text: userTurnDraft(entry) ?? "", images } };
+	}
+	if (entry.type === "custom_message" && entry.customType !== SKILL_PROMPT_MESSAGE_TYPE) {
+		return { leafId: entry.parentId, draft: { text: textContent(entry.content), images: [] } };
+	}
+	return { leafId: entry.id, draft: null };
+}
+
 export function buildSessionContext(
 	entries: SessionEntry[],
 	leafId?: string | null,
@@ -302,6 +328,7 @@ export function buildSessionContext(
 		// Explicitly null - return no messages (navigated to before first entry)
 		return {
 			messages: [],
+			todoPhases: options?.includeTodoPhases ? [] : undefined,
 			thinkingLevel: "off",
 			serviceTier: undefined,
 			models: {},
@@ -320,6 +347,7 @@ export function buildSessionContext(
 	if (!leaf) {
 		return {
 			messages: [],
+			todoPhases: options?.includeTodoPhases ? [] : undefined,
 			thinkingLevel: "off",
 			serviceTier: undefined,
 			models: {},
@@ -339,6 +367,14 @@ export function buildSessionContext(
 		current = current.parentId ? byId.get(current.parentId) : undefined;
 	}
 	path.reverse();
+	let todoPhases: TodoPhase[] | null | undefined;
+	if (options?.includeTodoPhases) {
+		try {
+			todoPhases = getLatestTodoPhasesFromEntries(path);
+		} catch {
+			todoPhases = null;
+		}
+	}
 
 	// Extract settings and find compaction
 	let thinkingLevel: string | undefined = "off";
@@ -781,6 +817,7 @@ export function buildSessionContext(
 
 	return {
 		messages,
+		todoPhases,
 		cacheMissExplainedAt: options?.transcript ? cacheMissExplainedAt : undefined,
 		thinkingLevel,
 		configuredThinkingLevel,
diff --git a/src/session/session-entries.ts b/src/session/session-entries.ts
index d8be66daf349918222d1984b756e61c752f93d0a..03a2ef6dbc7863b4dd088db0f8b59e722045fb23 100644
--- a/src/session/session-entries.ts
+++ b/src/session/session-entries.ts
@@ -30,6 +30,13 @@ export interface SessionTitleSlotEntry {
 	pad: string;
 }
 
+/** Physical journal position, excluded from the conversation tree. */
+export interface SessionCursorRecord {
+	type: "session_cursor";
+	leafId: string | null;
+	revision: string;
+}
+
 export const EPHEMERAL_MODEL_CHANGE_ROLE = "fallback";
 
 export interface SessionHeader {
@@ -311,11 +318,11 @@ export type SessionEntry =
 	| CredentialPinEntry
 	| ResetBoundaryEntry;
 
-/** Raw logical file entry after loaders strip any fixed-width title slot. */
+/** Logical file entry after loaders fold physical title and cursor records. */
 export type FileEntry = SessionHeader | SessionEntry;
 
-/** Physical JSONL entry before slot-aware loaders fold the title slot. */
-export type RawFileEntry = SessionTitleSlotEntry | FileEntry;
+/** Physical JSONL record before loaders fold title and cursor state. */
+export type RawFileEntry = SessionTitleSlotEntry | SessionCursorRecord | FileEntry;
 
 /** Tree node for getTree() - defensive copy of session structure */
 export interface SessionTreeNode {
diff --git a/src/session/session-loader.ts b/src/session/session-loader.ts
index 732fc693457fc464ad5b4d4b6f620d96aa6177ce..b7c82d295038e1da3248c3a6080ae0f8aca2d6c2 100644
--- a/src/session/session-loader.ts
+++ b/src/session/session-loader.ts
@@ -1,10 +1,12 @@
+import { createHash } from "node:crypto";
 import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
 import { ConcatSink, getBlobsDir, isEnoent, isEnotdir, parseJsonlLenient } from "@oh-my-pi/pi-utils";
 import * as snapcompact from "@oh-my-pi/snapcompact";
 import { Semaphore } from "../task/parallel";
+import type { TodoPhase } from "../tools/todo";
 import { BlobStore, isBlobRef, lazyImageDataSync, resolveImageData, resolveImageDataUrl } from "./blob-store";
 import { buildSessionContext } from "./session-context";
-import type { FileEntry, RawFileEntry, SessionEntry, SessionHeader } from "./session-entries";
+import type { FileEntry, RawFileEntry, SessionCursorRecord, SessionEntry, SessionHeader } from "./session-entries";
 import { migrateToCurrentVersion } from "./session-migrations";
 import { isExternalizableImagePosition, isPersistenceTruncatedString } from "./session-persistence";
 import { FileSessionStorage, type SessionStorage } from "./session-storage";
@@ -37,6 +39,8 @@ export interface VisitEntriesFromFileStreamOptions {
 	onMalformedRecord?: () => void;
 	/** Rethrow missing source instead of visiting nothing (fork backstop). */
 	throwIfMissing?: boolean;
+	/** Physical cursor records are never passed to the entry visitor. */
+	onCursor?: (cursor: SessionCursorRecord) => void;
 	/**
 	 * Called with each stream chunk's byte length as it is consumed. Lets
 	 * callers derive the exact snapshot size without re-stating the file.
@@ -54,6 +58,7 @@ export interface LoadSessionOptions {
 export interface SessionLoadResult {
 	entries: FileEntry[];
 	titleSlot: SessionTitleUpdate | undefined;
+	cursor?: SessionCursorRecord;
 	malformedRecords: number;
 	/** Byte length of the snapshot actually parsed, or `null` when the path did not exist. */
 	sourceSize?: number | null;
@@ -86,19 +91,61 @@ function applyTitleSlot(entry: FileEntry | undefined, slot: SessionTitleUpdate |
 	}
 }
 
-/** Parse session JSONL while stripping and folding the optional fixed title slot. */
+function parseSessionCursor(record: SessionCursorRecord): SessionCursorRecord {
+	if (
+		(record.leafId !== null && (typeof record.leafId !== "string" || record.leafId.length === 0)) ||
+		typeof record.revision !== "string" ||
+		record.revision.length === 0
+	) {
+		throw new Error("Session cursor is malformed. The file was not modified.");
+	}
+	return { type: "session_cursor", leafId: record.leafId, revision: record.revision };
+}
+
+function validateSessionCursor(entries: readonly FileEntry[], cursor: SessionCursorRecord | undefined): void {
+	if (cursor && cursor.leafId !== null && !entries.some(entry => entry?.type !== "session" && entry?.id === cursor.leafId)) {
+		throw new Error(`Session cursor references missing entry ${cursor.leafId}. The file was not modified.`);
+	}
+}
+
+export function sessionHistoryVersion(
+	sessionId: string,
+	entries: readonly SessionEntry[],
+	cursor: Pick<SessionCursorRecord, "leafId" | "revision">,
+): string {
+	const hash = createHash("sha256");
+	hash.update(JSON.stringify([sessionId, cursor.leafId, cursor.revision]));
+	for (const entry of entries) hash.update(`\n${JSON.stringify(entry)}`);
+	return hash.digest("hex");
+}
+
+/** Parse session JSONL while folding physical title and cursor records. */
 export function parseSessionContent(content: string): SessionLoadResult {
 	const { body, slot } = splitTitleSlot(content);
 	let malformedRecords = 0;
-	const entries = parseJsonlLenient<RawFileEntry>(body, {
+	const records = parseJsonlLenient<RawFileEntry>(body, {
 		onMalformedRecord: () => {
 			malformedRecords++;
 		},
-	}) as FileEntry[];
+	});
+	let entryCount = 0;
+	let cursor: SessionCursorRecord | undefined;
+	for (const record of records) {
+		if (record?.type === "session_cursor") {
+			cursor = parseSessionCursor(record);
+		} else if (record?.type !== "title") {
+			records[entryCount++] = record;
+			if (cursor && record?.type !== "session" && typeof record?.id === "string") cursor.leafId = record.id;
+		}
+	}
+	records.length = entryCount;
+	const entries = records as FileEntry[];
+	validateSessionCursor(entries, cursor);
 	applyTitleSlot(entries[0], slot);
 	return {
 		entries,
 		titleSlot: slot,
+		cursor,
 		malformedRecords,
 		sourceSize: Buffer.byteLength(content, "utf8"),
 		invalidHeader: entries.length > 0 ? !isValidSessionHeader(entries[0]) : malformedRecords > 0,
@@ -171,17 +218,22 @@ export async function visitEntriesFromFileStream(
 					stopped = true;
 					break;
 				}
-				const entry = value as FileEntry;
-				if (!sawFirstEntry) {
-					sawFirstEntry = true;
-					applyTitleSlot(entry, titleSlot);
-				}
 				try {
-					if (visit(entry) === false) {
+					const record = value as RawFileEntry;
+					recordsSeen++;
+					if (record?.type === "session_cursor") {
+						options.onCursor?.(parseSessionCursor(record));
+						continue;
+					}
+					if (record?.type === "title") continue;
+					if (!sawFirstEntry) {
+						sawFirstEntry = true;
+						applyTitleSlot(record, titleSlot);
+					}
+					if (visit(record) === false) {
 						stopped = true;
 						break;
 					}
-					recordsSeen++;
 					entriesSinceYield++;
 					if (recordsSeen >= maxRecords) {
 						stopped = true;
@@ -299,12 +351,17 @@ export async function loadEntriesFromFileStream(
 	const entries: FileEntry[] = [];
 	let malformedRecords = 0;
 	let bytesConsumed = 0;
+	let cursor: SessionCursorRecord | undefined;
 	const titleSlot = await visitEntriesFromFileStream(
 		filePath,
 		entry => {
 			entries.push(entry);
+			if (cursor && entry?.type !== "session" && typeof entry?.id === "string") cursor.leafId = entry.id;
 		},
 		{
+			onCursor: record => {
+				cursor = record;
+			},
 			onMalformedRecord: () => {
 				malformedRecords++;
 			},
@@ -314,9 +371,11 @@ export async function loadEntriesFromFileStream(
 			},
 		},
 	);
+	validateSessionCursor(entries, cursor);
 	return {
 		entries,
 		titleSlot,
+		cursor,
 		malformedRecords,
 		sourceSize: bytesConsumed,
 		invalidHeader: entries.length > 0 ? !isValidSessionHeader(entries[0]) : malformedRecords > 0,
@@ -523,21 +582,37 @@ export async function resolveBlobRefsInEntries(entries: FileEntry[], blobStore:
 /**
  * Read-only transcript view of a session file: load entries, migrate to the
  * current version, resolve blob refs, and build the display transcript along
- * the persisted leaf path (last entry). Uses transcript mode (collapsed to the
+ * the persisted cursor path. Uses transcript mode (collapsed to the
  * latest compaction) so failed/aborted tail turns stay visible, unlike the
  * provider-context builder which drops them. Does NOT create a writer or take
  * the session lock — safe to call against a file another session is writing.
  */
 export async function loadSessionMessagesReadOnly(filePath: string): Promise<AgentMessage[]> {
-	const entries = await loadEntriesFromFile(filePath);
-	if (entries.length === 0) return [];
+	return (await loadSessionReadOnly(filePath, false)).messages;
+}
+
+/** Transcript consumers call this to read messages and canonical todos from one journal version. */
+export function loadSessionSnapshotReadOnly(
+	filePath: string,
+): Promise<{ messages: AgentMessage[]; todoPhases: TodoPhase[] | null; historyRevision: string }> {
+	return loadSessionReadOnly(filePath, true);
+}
+
+async function loadSessionReadOnly(
+	filePath: string,
+	includeTodoPhases: boolean,
+): Promise<{ messages: AgentMessage[]; todoPhases: TodoPhase[] | null; historyRevision: string }> {
+	const { entries, cursor } = await loadSessionFile(filePath);
+	if (entries.length === 0) return { messages: [], todoPhases: [], historyRevision: "empty" };
 	migrateToCurrentVersion(entries);
 	for (const entry of entries) repairTruncatedSnapcompactFrames(entry);
 	const blobs = new BlobStore(getBlobsDir());
 	const sessionEntries = entries.filter((e): e is SessionEntry => e.type !== "session");
-	const { messages } = buildSessionContext(sessionEntries, undefined, undefined, {
+	const { messages, todoPhases } = buildSessionContext(sessionEntries, cursor?.leafId, undefined, {
 		transcript: true,
+		includeTodoPhases,
 		collapseCompactedHistory: true,
+		keepDanglingToolCalls: true,
 		resolveFrameData: data => lazyImageDataSync(blobs, data),
 	});
 	// A collapsed summary carries the remote-compaction replacement history for
@@ -550,5 +625,33 @@ export async function loadSessionMessagesReadOnly(filePath: string): Promise<Age
 			: message,
 	);
 	await resolveBlobRefs(displayMessages, blobs);
-	return displayMessages;
+	return {
+		messages: displayMessages,
+		todoPhases: todoPhases ?? null,
+		historyRevision: cursor?.revision ?? entries[0]!.id,
+	};
+}
+
+export async function loadSessionHistoryReadOnly(filePath: string): Promise<{
+	sessionId: string;
+	entries: SessionEntry[];
+	activeLeafId: string | null;
+	revision: string;
+	version: string;
+}> {
+	const loaded = await loadSessionFile(filePath, undefined, { throwIfMissing: true });
+	const header = loaded.entries[0];
+	if (!header || header.type !== "session") throw new Error("Session header is missing or malformed.");
+	migrateToCurrentVersion(loaded.entries);
+	await resolveBlobRefsInEntries(loaded.entries, new BlobStore(getBlobsDir()));
+	const entries = loaded.entries.slice(1) as SessionEntry[];
+	const activeLeafId = loaded.cursor ? loaded.cursor.leafId : (entries.at(-1)?.id ?? null);
+	const revision = loaded.cursor?.revision ?? header.id;
+	return {
+		sessionId: header.id,
+		entries,
+		activeLeafId,
+		revision,
+		version: sessionHistoryVersion(header.id, entries, { leafId: activeLeafId, revision }),
+	};
 }
diff --git a/src/session/session-maintenance.ts b/src/session/session-maintenance.ts
index 4fdf8acf498e12b1f2589b8bdddd626e031b58a7..ae7db616b8e84533feade35799e146acdeef1173 100644
--- a/src/session/session-maintenance.ts
+++ b/src/session/session-maintenance.ts
@@ -753,6 +753,8 @@ export class SessionMaintenance {
 	 * Mutates the branch in place, persists via `rewriteEntries`, replays the
 	 * rebuilt context through the agent, and tears down provider sessions that
 	 * cache message identity — same rewrite contract as {@link dropImages}.
+	 * Cancellation stops before branch mutation; an admitted rewrite finishes
+	 * before cancellation is reported.
 	 *
 	 * No-op (zero counts) when nothing is eligible.
 	 */
@@ -766,8 +768,10 @@ export class SessionMaintenance {
 			toolResultsOnly?: boolean;
 		} = {},
 	): Promise<ShakeResult> {
+		opts.signal?.throwIfAborted();
 		if (mode === "images") {
 			const { removed } = await this.#host.dropImages();
+			opts.signal?.throwIfAborted();
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, imagesDropped: removed, tokensFreed: 0 };
 		}
 
@@ -795,11 +799,13 @@ export class SessionMaintenance {
 			this.#host.agent.replaceMessages(sessionContext.messages);
 			this.#host.resetAdvisorRuntimes("shake");
 			this.#host.closeCodexProviderSessionsForHistoryRewrite();
+			opts.signal?.throwIfAborted();
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, thinkingBlocksDropped: removed, tokensFreed: 0 };
 		}
 
 		const assertCurrent = () => {
-			if (opts.signal?.aborted || opts.isCurrent?.() === false) throw new CompactionCancelledError();
+			opts.signal?.throwIfAborted();
+			if (opts.isCurrent?.() === false) throw new CompactionCancelledError();
 		};
 		assertCurrent();
 		const branchEntries = this.#host.sessionManager.getBranch();
@@ -818,12 +824,7 @@ export class SessionMaintenance {
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, tokensFreed: 0 };
 		}
 
-		let reservedArtifact: { id?: string; path?: string } = {};
-		try {
-			reservedArtifact = await this.#host.sessionManager.allocateArtifactPath("shake");
-		} catch {
-			if (opts.requireArtifact) throw new Error("shake could not save a recovery artifact");
-		}
+		const reservedArtifact = await this.#host.sessionManager.allocateArtifactPath("shake");
 		assertCurrent();
 		let artifactId = reservedArtifact.id;
 		const calculateReplacementState = (id: string | undefined) => {
@@ -842,15 +843,9 @@ export class SessionMaintenance {
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, tokensFreed: 0 };
 		}
 		if (reservedArtifact.path && reservedArtifact.id) {
-			try {
-				await writeArtifact(reservedArtifact.path, this.#shakeArtifactText(regions));
-			} catch {
-				if (opts.requireArtifact) throw new Error("shake could not save a recovery artifact");
-				artifactId = undefined;
-				({ replacements, replacementTokenCounts, savings } = calculateReplacementState(artifactId));
-			}
+			await writeArtifact(reservedArtifact.path, this.#shakeArtifactText(regions), opts.signal);
 		} else {
-			artifactId = await this.#saveShakeArtifact(regions);
+			artifactId = await this.#saveShakeArtifact(regions, opts.signal);
 			assertCurrent();
 			if (opts.requireArtifact && !artifactId) {
 				throw new Error("shake could not save a recovery artifact");
@@ -917,6 +912,7 @@ export class SessionMaintenance {
 		this.#host.agent.replaceMessages(sessionContext.messages);
 		this.#host.resetAdvisorRuntimes("shake");
 		this.#host.closeCodexProviderSessionsForHistoryRewrite();
+		opts.signal?.throwIfAborted();
 
 		return {
 			mode,
@@ -966,12 +962,8 @@ export class SessionMaintenance {
 	}
 
 	/** Persist the shake artifact, using the session manager's in-memory fallback when needed. */
-	async #saveShakeArtifact(regions: ShakeRegion[]): Promise<string | undefined> {
-		try {
-			return await this.#host.sessionManager.saveArtifact(this.#shakeArtifactText(regions), "shake");
-		} catch {
-			return undefined;
-		}
+	async #saveShakeArtifact(regions: ShakeRegion[], signal?: AbortSignal): Promise<string | undefined> {
+		return this.#host.sessionManager.saveArtifact(this.#shakeArtifactText(regions), "shake", signal);
 	}
 
 	/**
diff --git a/src/session/session-manager.ts b/src/session/session-manager.ts
index 650628e4949b60d88f4861e0101eb9bc87347768..011421ac8b57350f784a313a288cd46621f08594 100644
--- a/src/session/session-manager.ts
+++ b/src/session/session-manager.ts
@@ -55,6 +55,7 @@ import {
 	type NewSessionOptions,
 	type ResetBoundaryEntry,
 	type ServiceTierChangeEntry,
+	type SessionCursorRecord,
 	type SessionEntry,
 	type SessionHeader,
 	type SessionInitEntry,
@@ -69,11 +70,11 @@ import {
 } from "./session-entries";
 import { findMostRecentSession, listAllSessions, listSessions, type SessionInfo } from "./session-listing";
 import {
-	loadEntriesFromFile,
 	loadSessionFile,
 	parseSessionContent,
 	resolveBlobRefsInEntries,
 	type SessionLoadResult,
+	sessionHistoryVersion,
 	visitEntriesFromFile,
 } from "./session-loader";
 import { generateId, migrateToCurrentVersion } from "./session-migrations";
@@ -547,6 +548,8 @@ export type ReadonlySessionManager = Pick<
 	| "getArtifactPath"
 	| "getLeafId"
 	| "getLeafEntry"
+	| "getHistoryRevision"
+	| "getHistoryVersion"
 	| "getEntry"
 	| "getLabel"
 	| "getBranch"
@@ -574,6 +577,7 @@ interface SessionManagerStateSnapshot {
 	fallbackRuntimeOnly: boolean;
 	header: SessionHeader;
 	entries: SessionEntry[];
+	cursor: SessionCursorRecord;
 }
 
 interface DiskQueueOptions {
@@ -587,6 +591,8 @@ interface AtomicEntryBatch {
 	entryIds: Set<string>;
 	deferredNotifications: SessionEntry[];
 	preBatchLeafId: string | null;
+	preBatchRevision: string;
+	externalRevision?: string;
 	externalLeafChanged: boolean;
 	externalLeafId: string | null;
 }
@@ -656,6 +662,7 @@ export class SessionManager {
 	readonly #blobs: BlobStore;
 
 	#sessionId = "";
+	#historyRevision = "";
 	#sessionName: string | undefined;
 	#titleSource: SessionTitleSource | undefined;
 	#titleRevision = 0;
@@ -1007,8 +1014,9 @@ export class SessionManager {
 		return this.#writer;
 	}
 
-	#lineFor(entry: FileEntry): string {
-		return `${stringifyJson(prepareEntryForPersistence(entry, this.#blobs)) ?? "null"}\n`;
+	#lineFor(entry: FileEntry | SessionCursorRecord): string {
+		const persisted = entry.type === "session_cursor" ? entry : prepareEntryForPersistence(entry, this.#blobs);
+		return `${stringifyJson(persisted) ?? "null"}\n`;
 	}
 	#recordDurableAppend(line: string): void {
 		this.#expectedDiskSize = (this.#expectedDiskSize ?? 0) + Buffer.byteLength(line, "utf8");
@@ -1064,10 +1072,15 @@ export class SessionManager {
 		});
 	}
 
+	#cursorRecord(): SessionCursorRecord {
+		return { type: "session_cursor", leafId: this.#index.leafId(), revision: this.#historyRevision };
+	}
+
 	#fileBody(): string {
 		let body = this.#titleSlotLine();
 		body += this.#lineFor(this.#header);
 		for (const entry of this.#entries) body += this.#lineFor(entry);
+		body += this.#lineFor(this.#cursorRecord());
 		return body;
 	}
 
@@ -1250,7 +1263,7 @@ export class SessionManager {
 		}
 	}
 
-	#appendToSessionFile(entry: SessionEntry): void {
+	#appendToSessionFile(entry: SessionEntry | SessionCursorRecord): void {
 		if (this.#released || !this.#persist || !this.#sessionFile) return;
 		if (this.#atomicEntryBatch) {
 			this.#fileIsCurrent = false;
@@ -1279,6 +1292,7 @@ export class SessionManager {
 		// durable entry exists, later appends cannot satisfy its delete predicate.
 		if (
 			this.#storage.withSessionFileLockSync &&
+			entry.type !== "session_cursor" &&
 			this.#draftOnlySessionCleanupArmed &&
 			!isDraftOnlyMetadataEntry(entry) &&
 			this.#entries.every(candidate => candidate === entry || isDraftOnlyMetadataEntry(candidate))
@@ -1295,7 +1309,7 @@ export class SessionManager {
 		this.#appendToCurrentSessionFile(entry);
 	}
 
-	#appendToCurrentSessionFile(entry: SessionEntry): void {
+	#appendToCurrentSessionFile(entry: SessionEntry | SessionCursorRecord): void {
 		// Atomic replacement / move window: do not open a fresh append writer that
 		// a Windows EPERM replace could detach from the current JSONL path.
 		// - moveTo: write a full body to the live relocation path (source pre-
@@ -1331,7 +1345,10 @@ export class SessionManager {
 		// opens a fresh append handle and the entry still lands.
 		try {
 			const writer = this.#appendWriter();
-			const line = this.#lineFor(entry);
+			let line = this.#lineFor(entry);
+			if (entry.type !== "session_cursor" && entry.id !== this.#index.leafId()) {
+				line += this.#lineFor(this.#cursorRecord());
+			}
 			if (writer.appendSync && !this.#storage.defersSyncPublish) {
 				writer.appendSync(line);
 				this.#recordDurableAppend(line);
@@ -1393,13 +1410,13 @@ export class SessionManager {
 		}
 
 		const epoch = this.#diskEpoch;
-		const line = this.#lineFor(entry);
 		await this.#scheduleDiskWork(
 			async () => {
 				if (this.#released) return;
 				const sessionFile = this.#sessionFile;
 				if (!sessionFile) return;
 				try {
+					const line = this.#lineFor(entry) + this.#lineFor(this.#cursorRecord());
 					await this.#appendWriter().append(line);
 					this.#recordDurableAppend(line);
 					await this.#storage.updateSessionTitle(sessionFile, update);
@@ -1433,6 +1450,7 @@ export class SessionManager {
 		this.#expectedDiskSize = null;
 		this.#reconcileSessionDirForFallback();
 		this.#sessionId = mintSessionId();
+		this.#historyRevision = this.#sessionId;
 		this.#sessionName = undefined;
 		this.#titleSource = undefined;
 		this.#titleUpdatedAt = "";
@@ -1486,7 +1504,7 @@ export class SessionManager {
 		return this.#sessionFile;
 	}
 
-	#applyEntries(header: SessionHeader, entries: SessionEntry[]): void {
+	#applyEntries(header: SessionHeader, entries: SessionEntry[], cursor?: SessionCursorRecord): void {
 		this.#header = header;
 		this.#entries = entries;
 		this.#sessionId = header.id;
@@ -1494,6 +1512,8 @@ export class SessionManager {
 		this.#titleSource = header.titleSource;
 		this.#titleUpdatedAt = header.timestamp;
 		this.#index.rebuild(entries);
+		this.#historyRevision = cursor?.revision ?? header.id;
+		if (cursor) this.#index.setLeaf(cursor.leafId);
 	}
 
 	#freshEntryFields(): { id: string; parentId: string | null; timestamp: string } {
@@ -1505,26 +1525,31 @@ export class SessionManager {
 	}
 
 	#setLeaf(id: string | null): void {
+		if (this.#released || id === this.#index.leafId()) return;
 		this.#index.setLeaf(id);
+		this.#historyRevision = Bun.randomUUIDv7();
 		const batch = this.#atomicEntryBatch;
 		if (batch && !batch.collecting) {
 			batch.externalLeafChanged = true;
 			batch.externalLeafId = id;
+			batch.externalRevision = this.#historyRevision;
 		}
+		this.#appendToSessionFile(this.#cursorRecord());
 	}
 
-	#recordEntry(entry: SessionEntry): void {
+	#recordEntry(entry: SessionEntry, retainedLeafId?: string | null): void {
 		if (this.#released) {
 			logger.warn("Dropped session entry appended after terminal release", { type: entry.type });
 			return;
 		}
 		this.#entries.push(entry);
 		this.#index.insert(entry);
+		if (retainedLeafId !== undefined) this.#index.setLeaf(retainedLeafId);
 		const batch = this.#atomicEntryBatch;
 		if (batch?.collecting) batch.entryIds.add(entry.id);
 		if (batch && !batch.collecting) {
 			batch.externalLeafChanged = true;
-			batch.externalLeafId = entry.id;
+			batch.externalLeafId = this.#index.leafId();
 		}
 		this.#appendToSessionFile(entry);
 		if (batch) batch.deferredNotifications.push(entry);
@@ -1546,6 +1571,7 @@ export class SessionManager {
 		this.#entries = retained;
 		this.#index.rebuild(retained);
 		this.#index.setLeaf(restoredLeaf && this.#index.has(restoredLeaf) ? restoredLeaf : null);
+		this.#historyRevision = batch.externalRevision ?? batch.preBatchRevision;
 	}
 
 	#draftPath(): string | null {
@@ -1644,6 +1670,7 @@ export class SessionManager {
 			// a rollback observe the move it is undoing.
 			header: structuredClone(this.#header),
 			entries: [...this.#entries],
+			cursor: this.#cursorRecord(),
 		};
 	}
 
@@ -1682,7 +1709,7 @@ export class SessionManager {
 		this.#forceFileCreation = snapshot.onDisk;
 		this.#draftOnlySessionCleanupArmed = snapshot.draftOnlySessionCleanupArmed;
 		this.#fallbackRuntimeOnly = snapshot.fallbackRuntimeOnly;
-		this.#applyEntries(snapshot.header, [...snapshot.entries]);
+		this.#applyEntries(snapshot.header, [...snapshot.entries], snapshot.cursor);
 		this.#additionalDirectories = snapshot.header.additionalDirectories ?? [];
 		this.#sessionName = snapshot.sessionName;
 
@@ -1809,7 +1836,7 @@ export class SessionManager {
 			this.#fallbackRuntimeOnly = false;
 		}
 
-		this.#applyEntries(header, fileEntries.slice(1) as SessionEntry[]);
+		this.#applyEntries(header, fileEntries.slice(1) as SessionEntry[], loaded.cursor);
 		this.#expectedDiskSize = sourceSize;
 		this.#additionalDirectories = header.additionalDirectories ?? [];
 		this.#titleUpdatedAt = titleSlot?.updatedAt ?? header.timestamp;
@@ -2068,6 +2095,8 @@ export class SessionManager {
 			manager.#additionalDirectories.length > 0 ? [...manager.#additionalDirectories] : undefined;
 		manager.#entries = structuredClone(this.#entries);
 		manager.#index.rebuild(manager.#entries);
+		manager.#index.setLeaf(this.#index.leafId());
+		manager.#historyRevision = this.#historyRevision;
 		manager.#forceFileCreation = true;
 		await manager.#rewriteAtomically();
 		return manager;
@@ -2103,6 +2132,7 @@ export class SessionManager {
 			entryIds: new Set(),
 			deferredNotifications: [],
 			preBatchLeafId: this.#index.leafId(),
+			preBatchRevision: this.#historyRevision,
 			externalLeafChanged: false,
 			externalLeafId: null,
 		};
@@ -2503,9 +2533,10 @@ export class SessionManager {
 		return (await this.#artifactManagerForSession()?.allocatePath(toolType)) ?? {};
 	}
 
-	async saveArtifact(content: string, toolType: string): Promise<string | undefined> {
+	async saveArtifact(content: string, toolType: string, signal?: AbortSignal): Promise<string | undefined> {
+		signal?.throwIfAborted();
 		const manager = this.#artifactManagerForSession();
-		if (manager) return manager.save(content, toolType);
+		if (manager) return manager.save(content, toolType, signal);
 
 		// Non-persistent session: keep an in-memory copy so spill truncation works.
 		this.#inMemoryArtifacts ??= new Map();
@@ -2726,8 +2757,7 @@ export class SessionManager {
 			timestamp: nowIso(),
 			message,
 		};
-		this.#recordEntry(entry);
-		this.#index.setLeaf(activeLeafId);
+		this.#recordEntry(entry, activeLeafId);
 		return entry.id;
 	}
 
@@ -2750,8 +2780,7 @@ export class SessionManager {
 			timestamp: nowIso(),
 			...usage,
 		};
-		this.#recordEntry(entry);
-		if (activeLeafId !== owner.parentId) this.#index.setLeaf(activeLeafId);
+		this.#recordEntry(entry, activeLeafId !== owner.parentId ? activeLeafId : undefined);
 		return entry.id;
 	}
 
@@ -3058,6 +3087,16 @@ export class SessionManager {
 		return [...this.#entries];
 	}
 
+	getHistoryRevision(): string {
+		return this.#historyRevision;
+	}
+
+	/** Opaque journal boundary. Settle persistence before publishing it to consumers. */
+	getHistoryVersion(): string {
+		if (this.#diskFailure) throw this.#diskFailure;
+		return sessionHistoryVersion(this.#sessionId, this.#entries, this.#cursorRecord());
+	}
+
 	/**
 	 * The session as a tree. A well-formed session has exactly one root; orphaned
 	 * entries (broken parent chain) are returned as roots too.
@@ -3083,9 +3122,7 @@ export class SessionManager {
 	/**
 	 * Durably move the active branch past a discarded entry.
 	 *
-	 * The loader reconstructs the active branch from the last physical journal
-	 * entry, so changing the in-memory leaf alone is lost on reload. Known
-	 * metadata children are chained onto the discarded entry's parent before the
+	 * Known metadata children are chained onto the discarded entry's parent before the
 	 * entry is removed. If any child may carry content, the subtree is preserved
 	 * off-branch instead. Both paths append a metadata-only branch marker and
 	 * rewrite the journal, making the selected path durable.
@@ -3181,6 +3218,7 @@ export class SessionManager {
 		this.#header = header;
 		this.#entries = [...entriesToKeep, ...labels];
 		this.#sessionId = newSessionId;
+		this.#historyRevision = newSessionId;
 		this.#sessionName = header.title;
 		this.#titleSource = header.titleSource;
 		this.#titleUpdatedAt = timestamp;
@@ -3243,7 +3281,11 @@ export class SessionManager {
 			cwd: path.resolve(cwd),
 		};
 		const file = path.join(sessionDir, `${fileSafeTimestamp(timestamp)}_${id}.jsonl`);
-		storage.writeTextSync(file, `${serializeTitleSlot({ updatedAt: timestamp })}${JSON.stringify(header)}\n`);
+		const cursor: SessionCursorRecord = { type: "session_cursor", leafId: null, revision: id };
+		storage.writeTextSync(
+			file,
+			`${serializeTitleSlot({ updatedAt: timestamp })}${JSON.stringify(header)}\n${JSON.stringify(cursor)}\n`,
+		);
 		return file;
 	}
 
@@ -3276,10 +3318,12 @@ export class SessionManager {
 		// A missing source must fail instead of forking an empty parentless session:
 		// the loader swallows ENOENT by default for fresh-session opens, so fork opts out.
 		let sourceEntries: FileEntry[];
+		let sourceCursor: SessionCursorRecord | undefined;
 		try {
-			sourceEntries = structuredClone(
-				await loadEntriesFromFile(sourcePath, storage, { throwIfMissing: true }),
-			) as FileEntry[];
+			const loaded = await loadSessionFile(sourcePath, storage, { throwIfMissing: true });
+			if (loaded.invalidHeader) throw new Error("Fork source has a malformed session header.");
+			sourceEntries = structuredClone(loaded.entries);
+			sourceCursor = loaded.cursor;
 		} catch (err) {
 			if (isEnoent(err) || isEnotdir(err)) throw new ForkSourceNotFoundError(sourcePath);
 			throw err;
@@ -3308,10 +3352,15 @@ export class SessionManager {
 		manager.#hasTitleSlot = true;
 		manager.#entries = history;
 		manager.#index.rebuild(history);
+		if (sourceCursor) manager.#index.setLeaf(sourceCursor.leafId);
+		manager.#historyRevision = sourceCursor?.revision ?? sourceHeader?.id ?? manager.#sessionId;
 		manager.sanitizeLoadedOpenAIResponsesReplayMetadata();
 		if (options?.repairInterruptedTail) {
+			const leafId = manager.#index.leafId();
+			const entryCount = history.length;
 			SessionManager.#repairForkedInterruptedTail(history, manager.#index.pathTo());
 			manager.#index.rebuild(history);
+			if (history.length === entryCount) manager.#index.setLeaf(leafId);
 		}
 		manager.#forceFileCreation = true;
 		await manager.#rewriteAtomically();
diff --git a/src/session/session-migrations.ts b/src/session/session-migrations.ts
index 9f1dbf3631bc1fb8b8e136442e702a3af2e60804..9fb6540a85d0b3082c3693fd249adb6aada1a3de 100644
--- a/src/session/session-migrations.ts
+++ b/src/session/session-migrations.ts
@@ -58,10 +58,19 @@ function migrateV2ToV3(entries: FileEntry[]): void {
 
 /**
  * Run all necessary migrations to bring entries to current version.
- * Mutates entries in place. Returns true if any migration was applied.
+ * Mutates entries in place. Returns true when a version migration requires a journal rewrite.
  */
 export function migrateToCurrentVersion(entries: FileEntry[]): boolean {
-	const header = entries.find(e => e.type === "session") as SessionHeader | undefined;
+	const header = entries.find((entry): entry is SessionHeader => entry.type === "session");
+	if (header) {
+		for (const [index, entry] of entries.entries()) {
+			if (entry.type !== "message" || entry.message.role !== "assistant") continue;
+			const message = entry.message;
+			if ("messageId" in message && typeof message.messageId === "string" && message.messageId.length > 0) continue;
+			const entryId = typeof entry.id === "string" ? entry.id : `entry:${index}`;
+			Object.assign(message, { messageId: `legacy:${header.id}:${entryId}` });
+		}
+	}
 	const version = header?.version ?? 1;
 
 	if (version >= CURRENT_SESSION_VERSION) return false;
diff --git a/src/session/turn-persistence.ts b/src/session/turn-persistence.ts
index 60992f7152e0abeaa8fe923889fb726ba9cb2e3e..758985bdf9a19aa1981b6a620719fbf1b9f09ccf 100644
--- a/src/session/turn-persistence.ts
+++ b/src/session/turn-persistence.ts
@@ -1,34 +1,7 @@
-/**
- * Helpers that share one cheap, structural identity for messages — both during
- * incremental persistence and for the mid-run-compaction ordering check.
- *
- * Previously `AgentSession` carried two near-duplicate routines
- * (`#sessionMessagesReferToSameTurn` + `#messageValueSignature`) that
- * reconstructed the branch path on every check (O(n²) `unshift`) and
- * `JSON.stringify`-compared the full message content on every pairwise hit.
- * Long-running sessions with many subagents fired this thousands of times per
- * minute and froze the TUI loop (see issue #3629). The persistence key already
- * encodes a stable logical identity — timestamp + role-specific discriminators
- * — so the structural compare is now the rare collision tiebreaker (e.g. two
- * provider responses at the same millisecond with `undefined` responseId),
- * not the load-bearing check.
- *
- * The helpers here keep that identity in one place and expose the planner so
- * the persistence-ordering logic is unit-testable without standing up an
- * `AgentSession`.
- */
 import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
 
 /**
- * Stable identity for messages that pass through {@link AgentSession}'s
- * incremental persistence path.
- *
- * The discriminators chosen per role are precisely the fields that uniquely
- * identify a single logical message instance:
- *
- * - `assistant` — timestamp + provider + model + responseId + stopReason
- *   (responseId is the canonical provider-side id when available; the rest
- *   disambiguate when it is not, e.g. local/dev models).
+ * Assistants use their SDK lifecycle ID. Legacy records retain their existing persistence key.
  * - `toolResult` — timestamp + toolCallId + toolName (toolCallId is unique
  *   per execution; toolName guards against synthetic reuse).
  * - `user` / `developer` — timestamp + attribution (attribution distinguishes
@@ -42,6 +15,9 @@ import type { AgentMessage } from "@oh-my-pi/pi-agent-core";
 export function sessionMessagePersistenceKey(message: AgentMessage): string | undefined {
 	switch (message.role) {
 		case "assistant":
+			if ("messageId" in message && typeof message.messageId === "string" && message.messageId.length > 0) {
+				return `assistant-id:${message.messageId}`;
+			}
 			return [
 				"assistant",
 				message.timestamp,
diff --git a/src/slash-commands/builtin-lifecycle.ts b/src/slash-commands/builtin-lifecycle.ts
index 8277fb228d3a49e49ae0ab9c8be53be8a57ad454..875d0faf906e6d608ff935290943d459ecaed920 100644
--- a/src/slash-commands/builtin-lifecycle.ts
+++ b/src/slash-commands/builtin-lifecycle.ts
@@ -593,7 +593,7 @@ export const BUILTIN_LIFECYCLE_SLASH_COMMANDS: ReadonlyArray<SlashCommandSpec> =
 			switch (verb) {
 				case "view": {
 					const payload = await backend.buildDeveloperInstructions(
-						runtime.settings.getAgentDir(),
+						runtime.session.agentDir,
 						runtime.settings,
 						runtime.session,
 					);
@@ -602,20 +602,20 @@ export const BUILTIN_LIFECYCLE_SLASH_COMMANDS: ReadonlyArray<SlashCommandSpec> =
 				}
 				case "clear":
 				case "reset": {
-					await backend.clear(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					await backend.clear(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.session.refreshBaseSystemPrompt();
 					await runtime.output("Memory cleared.");
 					return commandConsumed();
 				}
 				case "enqueue":
 				case "rebuild": {
-					await backend.enqueue(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					await backend.enqueue(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.output("Memory consolidation enqueued.");
 					return commandConsumed();
 				}
 				case "queue": {
 					const payload = await backend.queuePreview?.({
-						agentDir: runtime.settings.getAgentDir(),
+						agentDir: runtime.session.agentDir,
 						cwd: runtime.cwd,
 						session: runtime.session,
 					});
@@ -623,14 +623,14 @@ export const BUILTIN_LIFECYCLE_SLASH_COMMANDS: ReadonlyArray<SlashCommandSpec> =
 					return commandConsumed();
 				}
 				case "sync": {
-					await backend.enqueue(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					await backend.enqueue(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.output("Memory consolidation ran.");
 					return commandConsumed();
 				}
 				case "stats":
 				case "diagnose": {
 					const hook = verb === "stats" ? backend.stats : backend.diagnose;
-					const payload = await hook?.(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					const payload = await hook?.(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.output(payload ?? memoryStatsUnavailableMessage(backend.id, verb));
 					return commandConsumed();
 				}
diff --git a/src/task/executor.ts b/src/task/executor.ts
index e93287f2095b76beab5a62de9f0efd0de620463f..04ecb90c130a0dfc7a835dd187d2887fde51e92b 100644
--- a/src/task/executor.ts
+++ b/src/task/executor.ts
@@ -394,6 +394,7 @@ function isRecord(value: unknown): value is Record<string, unknown> {
 /** Options for subagent execution */
 export interface ExecutorOptions {
 	cwd: string;
+	agentDir?: string;
 	/** Additional workspace directories to seed on the subagent session (multi-root). */
 	additionalDirectories?: string[];
 	/** Exact provider credential resolver inherited from the parent session. */
@@ -3650,6 +3651,7 @@ export async function runSubprocess(options: ExecutorOptions): Promise<SingleRes
 				forRevive = false,
 			): CreateAgentSessionOptions => ({
 				cwd: worktree ?? cwd,
+				agentDir: options.agentDir ?? settings.getAgentDir(),
 				additionalDirectories: worktree !== undefined ? undefined : options.additionalDirectories,
 				authStorage,
 				modelRegistry,
diff --git a/src/task/persisted-revive.ts b/src/task/persisted-revive.ts
index 134f38175bfce9a904fd3edc85f8dd474bd79f06..06c74b9597c3e77d4c8b15ebf14a92fdbdad280f 100644
--- a/src/task/persisted-revive.ts
+++ b/src/task/persisted-revive.ts
@@ -147,6 +147,7 @@ export function createPersistedSubagentReviverFactory(
 			const mcpProxyTools = mcpManager ? createMCPProxyTools(mcpManager) : [];
 			const { session } = await createAgentSession({
 				cwd: ctx.session.sessionManager.getCwd(),
+				agentDir: ctx.session.agentDir,
 				authStorage: ctx.authStorage,
 				// Revived agents join the root session tree, so their observability
 				// frames ride the same bus the RPC/collab surfaces subscribed to.
diff --git a/src/task/structured-subagent.ts b/src/task/structured-subagent.ts
index 1ad7cf53d9f50e6dff3cf8af97dfb06f6e4632eb..46e7cd2aeede7f68fbfb053f876a21a44366f8b8 100644
--- a/src/task/structured-subagent.ts
+++ b/src/task/structured-subagent.ts
@@ -418,6 +418,7 @@ function buildExecutorOptions(
 	const enableMCP = !restrictToolNames && (session.enableMCP ?? true);
 	return {
 		cwd: session.cwd,
+		agentDir: session.agentDir ?? session.settings.getAgentDir(),
 		additionalDirectories: session.additionalDirectories,
 		getApiKey: session.getApiKey,
 		credentialSourceSessionId: session.getCredentialSourceSessionId?.(),
diff --git a/src/tools/ast-edit.ts b/src/tools/ast-edit.ts
index 620a18ea401954bf09e974233922ec553f99f6aa..674a70883f2b32622567744e353c937714d0bcce 100644
--- a/src/tools/ast-edit.ts
+++ b/src/tools/ast-edit.ts
@@ -287,6 +287,7 @@ export class AstEditTool implements AgentTool<typeof astEditSchema, AstEditToolD
 			const scope = await resolveToolSearchScope({
 				rawPaths: params.paths,
 				cwd: this.session.cwd,
+				agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 				internalUrlAction: "rewrite",
 				settings: this.session.settings,
 				signal,
diff --git a/src/tools/ast-grep.ts b/src/tools/ast-grep.ts
index d655948db560fb5b849fe9a0bc3c27f05ba73bce..969adad8525f14a6f210d8fd0888b0c0696e541b 100644
--- a/src/tools/ast-grep.ts
+++ b/src/tools/ast-grep.ts
@@ -222,6 +222,7 @@ export class AstGrepTool implements AgentTool<typeof astGrepSchema, AstGrepToolD
 			const scope = await resolveToolSearchScope({
 				rawPaths,
 				cwd: this.session.cwd,
+				agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 				internalUrlAction: "search",
 				settings: this.session.settings,
 				signal,
diff --git a/src/tools/bash-skill-urls.ts b/src/tools/bash-skill-urls.ts
index a0107da2f550a5b91e41ef9bbd4afc1533718ac2..4013bfae3d4ae9863907a46fbe85f13d43e3c25a 100644
--- a/src/tools/bash-skill-urls.ts
+++ b/src/tools/bash-skill-urls.ts
@@ -53,6 +53,7 @@ export interface InternalUrlExpansionOptions {
 	internalRouter?: InternalUrlResolver;
 	localOptions?: LocalProtocolOptions;
 	cwd?: string;
+	agentDir?: string;
 	sessionFile?: string;
 	sessionId?: string;
 	agentRegistry?: ResolveContext["agentRegistry"];
@@ -299,6 +300,7 @@ async function resolveInternalUrlToPath(
 	agentRegistry?: ResolveContext["agentRegistry"],
 	rules?: readonly Rule[],
 	skillUrlForDirectory?: boolean,
+	agentDir?: string,
 ): Promise<string> {
 	const url = normalizeLocalScheme(rawUrl);
 	const scheme = extractScheme(url);
@@ -342,6 +344,7 @@ async function resolveInternalUrlToPath(
 	try {
 		resource = await internalRouter.resolve(url, {
 			cwd,
+			agentDir,
 			pathOnly: true,
 			sessionFile,
 			sessionId,
@@ -397,6 +400,7 @@ export async function expandInternalUrls(command: string, options: InternalUrlEx
 				options.agentRegistry,
 				options.rules,
 				options.skillUrlForDirectory,
+				options.agentDir,
 			);
 		} catch (error) {
 			// Containment violations fail closed: never hand the raw token to the
diff --git a/src/tools/bash.ts b/src/tools/bash.ts
index 3ed8130c2652099f28e6510c63295ada7e93807d..9f763da5542c6f7fe0da4454f1c3d1bac6285707 100644
--- a/src/tools/bash.ts
+++ b/src/tools/bash.ts
@@ -1071,6 +1071,7 @@ export class BashTool implements AgentTool<typeof bashSchemaBase | typeof bashSc
 			attachments: this.session.getImageAttachments?.() ?? [],
 			internalRouter: InternalUrlRouter.instance(),
 			cwd: this.session.cwd,
+			agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 			sessionFile: this.session.getSessionFile() ?? undefined,
 			sessionId: this.session.sessionManager?.getSessionId?.() ?? this.session.getSessionId?.() ?? undefined,
 			agentRegistry: this.session.agentRegistry,
diff --git a/src/tools/glob.ts b/src/tools/glob.ts
index c8e62cb63c1029ff25d7c3f7b5032d50e9c7048b..d9e7d95de5fed9c843fee4ed9f790f5af7ae1ad1 100644
--- a/src/tools/glob.ts
+++ b/src/tools/glob.ts
@@ -224,6 +224,7 @@ export class GlobTool implements AgentTool<typeof findSchema, GlobToolDetails> {
 					const memoryGlob = splitMemoryGlobPattern(rawPattern);
 					const resource = await internalRouter.resolve(memoryGlob.baseUrl, {
 						cwd: this.session.cwd,
+						agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 						settings: this.session.settings,
 						signal,
 						sessionFile: this.session.getSessionFile() ?? undefined,
@@ -245,6 +246,7 @@ export class GlobTool implements AgentTool<typeof findSchema, GlobToolDetails> {
 				}
 				const resource = await internalRouter.resolve(rawPattern, {
 					cwd: this.session.cwd,
+					agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 					settings: this.session.settings,
 					signal,
 					sessionFile: this.session.getSessionFile() ?? undefined,
diff --git a/src/tools/grep.ts b/src/tools/grep.ts
index a872036ad4da7cafe332023553d3c60db8788719..2f46c6f16982444181e1bf602056e8cec6d73725 100644
--- a/src/tools/grep.ts
+++ b/src/tools/grep.ts
@@ -781,6 +781,7 @@ async function resolveInternalSearchInputs(opts: {
 	pathSpecs: readonly GrepPathSpec[];
 	resolvedPaths: string[];
 	cwd: string;
+	agentDir?: string;
 	settings: unknown;
 	signal?: AbortSignal;
 	archiveDisplayMap: ReadonlyMap<string, string>;
@@ -802,6 +803,7 @@ async function resolveInternalSearchInputs(opts: {
 	let virtualScopePath: string | undefined;
 	const context: ResolveContext = {
 		cwd: opts.cwd,
+		agentDir: opts.agentDir,
 		settings: opts.settings,
 		signal: opts.signal,
 		sessionFile: opts.sessionFile,
@@ -1017,6 +1019,7 @@ export class GrepTool implements AgentTool<typeof searchSchema, GrepToolDetails>
 					pathSpecs,
 					resolvedPaths,
 					cwd: this.session.cwd,
+					agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 					archiveDisplayMap,
 					settings: this.session.settings,
 					signal,
@@ -1066,6 +1069,7 @@ export class GrepTool implements AgentTool<typeof searchSchema, GrepToolDetails>
 					const scope = await resolveToolSearchScope({
 						rawPaths: searchablePaths,
 						cwd: this.session.cwd,
+						agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 						internalUrlAction: "search",
 						settings: this.session.settings,
 						localProtocolOptions: this.session.localProtocolOptions,
diff --git a/src/tools/index.ts b/src/tools/index.ts
index f02502d471e5ae454243f96008dea8bee41742c2..ba777ed3adc50e232d03263aecd04ee46a536cae 100644
--- a/src/tools/index.ts
+++ b/src/tools/index.ts
@@ -156,6 +156,8 @@ export interface DeferredDiagnosticsEntry {
 export interface ToolSession {
 	/** Current working directory */
 	cwd: string;
+	/** Agent directory supplied by the SDK; standalone tools use their settings root. */
+	agentDir?: string;
 	/** Additional workspace directories beyond cwd (multi-root), forwarded to subagents. */
 	additionalDirectories?: string[];
 	/** Whether UI is available */
diff --git a/src/tools/learn.ts b/src/tools/learn.ts
index 461c6abf4f7e375a3b9134d80f2803c31028f1ad..79435fbd22e97e6ebfc902fb6b90ea0bc5e03fef 100644
--- a/src/tools/learn.ts
+++ b/src/tools/learn.ts
@@ -82,7 +82,7 @@ export class LearnTool implements AgentTool<typeof learnSchema> {
 			}
 		} else if (backend === "local") {
 			const result = await localBackend.save?.(
-				{ agentDir: this.session.settings.getAgentDir(), cwd: this.session.settings.getCwd() },
+				{ agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(), cwd: this.session.cwd },
 				{ content: params.memory, context: params.context, source: "coding-agent-learn", importance: 0.8 },
 			);
 			if (!result || result.stored === 0) {
diff --git a/src/tools/path-utils.ts b/src/tools/path-utils.ts
index 2a589d63f76f74a5d946e5249db4d4294c2af396..de107f9f04dae8a652b8067ccc0d21e918ec79f2 100644
--- a/src/tools/path-utils.ts
+++ b/src/tools/path-utils.ts
@@ -1508,6 +1508,7 @@ export interface ResolvedExternalSearchUrl {
 export interface ToolScopeOptions {
 	rawPaths: string[];
 	cwd: string;
+	agentDir?: string;
 	/** Verb used in the "Cannot {action} internal URL without a backing file: …" message. */
 	internalUrlAction: string;
 	/** Collect absolute paths flagged immutable by their internal-URL handler. */
@@ -1620,6 +1621,7 @@ export async function resolveToolSearchScope(opts: ToolScopeOptions): Promise<To
 		}
 		const resource = await internalRouter.resolve(rawPath, {
 			cwd,
+			agentDir: opts.agentDir,
 			settings: opts.settings,
 			signal: opts.signal,
 			sessionFile: opts.sessionFile,
diff --git a/src/tools/read.ts b/src/tools/read.ts
index 3308f974f132d3577f045f635efaf19c0eb53f48..45efd4e43ad38e6b607cb41b994806144f7f095d 100644
--- a/src/tools/read.ts
+++ b/src/tools/read.ts
@@ -2803,6 +2803,7 @@ export class ReadTool implements AgentTool<typeof readSchema, ReadToolDetails> {
 		// Resolve the internal URL
 		const resource = await internalRouter.resolve(url, {
 			cwd: this.session.cwd,
+			agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 			settings: this.session.settings,
 			signal,
 			sessionFile: this.session.getSessionFile() ?? undefined,
