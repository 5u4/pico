diff --git a/dist/types/internal-urls/memory-protocol.d.ts b/dist/types/internal-urls/memory-protocol.d.ts
index b5a4db84f23f88ccfad24c0bb96d07de7efe77b2..178a90dcb2933075053242cdba00bde6b8e4064c 100644
--- a/dist/types/internal-urls/memory-protocol.d.ts
+++ b/dist/types/internal-urls/memory-protocol.d.ts
@@ -1,10 +1,10 @@
 import type { InternalResource, InternalUrl, ProtocolHandler, ResolveContext, UrlCompletion } from "./types.js";
 /**
- * Snapshot of memory roots for every registered session, deduped.
- * Each session has its own cwd (possibly a worktree), so subagents and main
- * may see different roots.
+ * Snapshot of registered sessions' memory roots, deduped.
+ * Each session owns its agent directory and cwd. An explicit agent directory
+ * limits the sweep to sessions from that instance.
  */
-export declare function memoryRootsFromRegistry(): string[];
+export declare function memoryRootsFromRegistry(context?: Pick<ResolveContext, "agentDir" | "agentRegistry">): string[];
 export interface MemoryGlobPattern {
     baseUrl: string;
     globPattern: string;
diff --git a/dist/types/internal-urls/types.d.ts b/dist/types/internal-urls/types.d.ts
index deb47109f71ac8637bf947676986fbcea0cf0bc6..c8c1b0f9b1f1e82f4dcd533488e880d2213ed883 100644
--- a/dist/types/internal-urls/types.d.ts
+++ b/dist/types/internal-urls/types.d.ts
@@ -87,6 +87,8 @@ export interface InternalUrl extends URL {
 export interface ResolveContext {
     /** Working directory of the calling session. */
     cwd?: string;
+    /** Agent directory supplied by the calling SDK session. */
+    agentDir?: string;
     /**
      * Calling session's session file. Handlers that resolve agent ids which may
      * be parked (`history://<id>`, `agent://<id>`) refresh the caller's
diff --git a/dist/types/mcp/loader.d.ts b/dist/types/mcp/loader.d.ts
index 88cb17b3f860d90cffe81d31d47bac132174498f..055bf8ec78ba0c17f37541a8d75deb05b4873421 100644
--- a/dist/types/mcp/loader.d.ts
+++ b/dist/types/mcp/loader.d.ts
@@ -1,7 +1,8 @@
 import type { LoadedCustomTool } from "../extensibility/custom-tools/types.js";
 import { AgentStorage } from "../session/agent-storage.js";
 import type { AuthStorage } from "../session/auth-storage.js";
-import { MCPManager } from "./manager.js";
+import { type MCPConfigLoader, MCPManager } from "./manager.js";
+import type { LoadMCPConfigsOptions } from "./config.js";
 import type { McpConnectionStatusEvent } from "./startup-events.js";
 /** Result from loading MCP tools */
 export interface MCPToolsLoadResult {
@@ -33,6 +34,8 @@ export interface MCPToolsLoadOptions {
     cacheStorage?: AgentStorage | null;
     /** Auth storage used to resolve OAuth credentials before initial MCP connect */
     authStorage?: AuthStorage;
+    configLoader?: MCPConfigLoader;
+    extensionRoots?: LoadMCPConfigsOptions["extensionRoots"];
 }
 /**
  * Discover and load MCP tools from .mcp.json files.
diff --git a/dist/types/modes/internal-url-autocomplete.d.ts b/dist/types/modes/internal-url-autocomplete.d.ts
index 4ae91925cbcd33bb348cc1793f4ca1a0e37280e1..a1fedd8109c84f132bd1f1d8386cf98858782d46 100644
--- a/dist/types/modes/internal-url-autocomplete.d.ts
+++ b/dist/types/modes/internal-url-autocomplete.d.ts
@@ -23,7 +23,7 @@ export interface InternalUrlContext {
  */
 export declare function extractInternalUrlContext(textBeforeCursor: string): InternalUrlContext | null;
 /** Caller binding shared by tool execution and prompt completion. */
-export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "sessionFile" | "sessionId" | "agentRegistry">;
+export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "agentDir" | "sessionFile" | "sessionId" | "agentRegistry">;
 /**
  * Suggestions for the internal-url token ending at the cursor, or `null` when
  * the text is not such a token or no candidate matches the typed query.
diff --git a/dist/types/modes/rpc/rpc-mode.d.ts b/dist/types/modes/rpc/rpc-mode.d.ts
index d2c6e672d3c088aa917c8c0f8deb6fc38c5f8792..70f7b393faa2f3d5faa642f5b48a2678ed031db6 100644
--- a/dist/types/modes/rpc/rpc-mode.d.ts
+++ b/dist/types/modes/rpc/rpc-mode.d.ts
@@ -1,6 +1,7 @@
 import { type ExtensionUIContext, type ExtensionUIDialogOptions, type ExtensionUISelectItem } from "../../extensibility/extensions/index.js";
 import { type BuiltSkillPromptMessage, type Skill } from "../../extensibility/skills.js";
 import type { AgentSession } from "../../session/agent-session.js";
+import type { PromptDeliveryObserver } from "../../session/agent-session-types.js";
 import type { EventBus } from "../../utils/event-bus.js";
 import { RpcSubagentRegistry } from "./rpc-subagents.js";
 import type { RpcCommand, RpcExtensionUIRequest, RpcExtensionUIResponse, RpcHostToolCallRequest, RpcHostToolCancelRequest, RpcHostToolResult, RpcHostToolUpdate, RpcHostUriCancelRequest, RpcHostUriRequest, RpcHostUriResult, RpcResponse } from "./rpc-types.js";
@@ -63,7 +64,7 @@ export declare function resolveRpcSkillInvocation(session: RpcSkillCommandSessio
  * Must not run on the RPC serial queue's response path — register it with
  * watchAndReportLocalOnlyPromptResult and answer the command first.
  */
-export declare function runRpcSkillCommand(session: RpcSkillCommandSession, invocation: RpcSkillInvocation, streamingBehavior?: "steer" | "followUp", prebuilt?: BuiltSkillPromptMessage): Promise<boolean>;
+export declare function runRpcSkillCommand(session: RpcSkillCommandSession, invocation: RpcSkillInvocation, streamingBehavior?: "steer" | "followUp", prebuilt?: BuiltSkillPromptMessage, deliveryObserver?: PromptDeliveryObserver): Promise<boolean>;
 /**
  * Skill branch of the `prompt` command: resolves the invocation cheaply, then
  * registers the slow dispatch with watchAndReportLocalOnlyPromptResult and
@@ -81,7 +82,7 @@ export declare function dispatchRpcSkillPrompt(input: {
     onError: (error: Error) => void;
     extensionUserMessageTracker: RpcExtensionUserMessageTracker;
 }): Promise<RpcSkillCommandResult | null>;
-export declare function tryRunRpcSkillCommand(session: RpcSkillCommandSession, text: string, streamingBehavior?: "steer" | "followUp"): Promise<RpcSkillCommandResult | false>;
+export declare function tryRunRpcSkillCommand(session: RpcSkillCommandSession, text: string, streamingBehavior?: "steer" | "followUp", deliveryObserver?: PromptDeliveryObserver): Promise<RpcSkillCommandResult | false>;
 export declare function reportLocalOnlyPromptResult(input: {
     id: string | undefined;
     prompt: Promise<boolean>;
diff --git a/dist/types/sdk.d.ts b/dist/types/sdk.d.ts
index e3b75c288b6c8ded7d709832d65a2cf993461374..9f7cedc1f7eaf2262386264ad13accbda065c8d3 100644
--- a/dist/types/sdk.d.ts
+++ b/dist/types/sdk.d.ts
@@ -16,6 +16,7 @@ import { type FileSlashCommand } from "./extensibility/slash-commands.js";
 import type { HindsightSessionState } from "./hindsight/state.js";
 import { type LocalProtocolOptions } from "./internal-urls/index.js";
 import { MCPManager, type MCPToolsLoadResult } from "./mcp/index.js";
+import type { MCPConfigLoader } from "./mcp/manager.js";
 import type { MnemopiSessionState } from "./mnemopi/state.js";
 import { type AgentRef, AgentRegistry } from "./registry/agent-registry.js";
 import { AgentSession, type PlanYolo, type Prewalk } from "./session/agent-session.js";
@@ -193,6 +194,7 @@ export interface CreateAgentSessionOptions {
     enableMCP?: boolean;
     /** Existing MCP manager to reuse when MCP is enabled (skips discovery, propagates to toolSession). */
     mcpManager?: MCPManager;
+    mcpConfigLoader?: MCPConfigLoader;
     /** Enable LSP integration (tool, formatting, diagnostics, warmup). Default: true */
     enableLsp?: boolean;
     /** Restrict LSP to navigation and diagnostics even when enabled. Defaults to true for restricted sessions. */
diff --git a/dist/types/session/agent-session-types.d.ts b/dist/types/session/agent-session-types.d.ts
index abf74e5efad48f840d09492eb6a453a0f10eb656..18e1f0a262d090909f3abfebe706bafbc818d530 100644
--- a/dist/types/session/agent-session-types.d.ts
+++ b/dist/types/session/agent-session-types.d.ts
@@ -308,8 +308,15 @@ export interface AgentSessionConfig {
     /** System prompt used by automatic session-title generation. */
     titleSystemPrompt?: string;
 }
+/** SDK hosts observe admission and final delivery of one submitted message. */
+export interface PromptDeliveryObserver {
+    onAccepted(delivery: "prompt" | "steer" | "followUp" | "aside"): void;
+    onConsumed(): void;
+    onDiscarded(): void;
+}
 /** Options for AgentSession.prompt(). */
 export interface PromptOptions {
+    deliveryObserver?: PromptDeliveryObserver;
     /** Whether to expand file-based prompt templates (default: true). */
     expandPromptTemplates?: boolean;
     /** Image attachments. */
diff --git a/dist/types/session/agent-session.d.ts b/dist/types/session/agent-session.d.ts
index e9c8ade46a1ad9521d3ad3bf47ca82315212c54c..1dc8db1f94cc2f932a03aa89b62cae67c294e260 100644
--- a/dist/types/session/agent-session.d.ts
+++ b/dist/types/session/agent-session.d.ts
@@ -53,7 +53,7 @@ import type { TodoPhase } from "../tools/todo.js";
 import type { WorkPoolYieldItem } from "../task/workpool-yield.js";
 import type { VibeModeState } from "../vibe/state.js";
 import type { AgentSessionEvent, AgentSessionEventListener } from "./agent-session-events.js";
-import type { AgentSessionConfig, AgentSessionDisposeOptions, AsyncJobSnapshot, CommandMetadataChangedListener, ContextUsageBreakdown, DroppedPrompt, FollowUpOptions, FreshSessionResult, HandoffResult, ModelCycleResult, Prewalk, PromptOptions, ResetSessionContextResult, ResolvedRoleModel, RestoredQueuedMessage, RoleModelCycle, RoleModelCycleResult, SessionHandoffOptions, SessionOAuthAccountList, SessionStats, UsageFallbackConfirmer } from "./agent-session-types.js";
+import type { AgentSessionConfig, AgentSessionDisposeOptions, AsyncJobSnapshot, CommandMetadataChangedListener, ContextUsageBreakdown, DroppedPrompt, FollowUpOptions, FreshSessionResult, HandoffResult, ModelCycleResult, Prewalk, PromptDeliveryObserver, PromptOptions, ResetSessionContextResult, ResolvedRoleModel, RestoredQueuedMessage, RoleModelCycle, RoleModelCycleResult, SessionHandoffOptions, SessionOAuthAccountList, SessionStats, UsageFallbackConfirmer } from "./agent-session-types.js";
 import type { ClientBridge } from "./client-bridge.js";
 import { type CustomMessage, type CustomMessagePayload } from "./messages.js";
 import type { ServingModel } from "./retry-fallback-chains.js";
@@ -78,6 +78,7 @@ export declare class AgentSession {
     readonly agent: Agent;
     readonly sessionManager: SessionManager;
     readonly settings: Settings;
+    readonly agentDir: string;
     /** Entries of tools mounted under `xd://`; empty when virtual devices are unmounted. */
     getXdevToolEntries: () => Array<{
         name: string;
@@ -235,7 +236,7 @@ export declare class AgentSession {
     markMovedFromEmptySessionFile(sessionFile: string): void;
     /**
      * Synchronously mark the session as disposing so new work is rejected
-     * immediately: eval starts throw, queued asides are dropped, and the
+     * immediately: eval starts throw, queued asides are flushed, and the
      * aside provider is detached. Idempotent; `dispose()` runs it first.
      *
      * Wrappers that await other teardown before delegating to `dispose()` MUST
@@ -554,7 +555,7 @@ export declare class AgentSession {
      * won the generation race), so hosts waiting on a terminal `agent_end` can
      * stop instead of hanging.
      */
-    promptCustomMessage<T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">, options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice"> & {
+    promptCustomMessage<T = unknown>(message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">, options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice" | "deliveryObserver"> & {
         queueChipText?: string;
         queueOnly?: boolean;
     }): Promise<boolean>;
@@ -616,6 +617,7 @@ export declare class AgentSession {
      */
     sendUserMessage(content: string | (TextContent | ImageContent)[], options?: {
         deliverAs?: "steer" | "followUp" | "aside";
+        deliveryObserver?: PromptDeliveryObserver;
     }): Promise<void>;
     /** Clear queued messages and return the user-restorable ones (text plus any attached images).
      *  Only user-authored messages (plain user turns, `attribution:"user"` custom like `/skill`) are
diff --git a/dist/types/task/executor.d.ts b/dist/types/task/executor.d.ts
index aec2ce132254e1ba7eb03f59e8fefe494bf40a52..602f7932c6bdf6c91fe449ad5cae87b3f2427aef 100644
--- a/dist/types/task/executor.d.ts
+++ b/dist/types/task/executor.d.ts
@@ -75,6 +75,7 @@ export declare function collectIrcPeerRoster(registry: AgentRegistry, selfId: st
 /** Options for subagent execution */
 export interface ExecutorOptions {
     cwd: string;
+    agentDir?: string;
     /** Additional workspace directories to seed on the subagent session (multi-root). */
     additionalDirectories?: string[];
     /** Exact provider credential resolver inherited from the parent session. */
diff --git a/dist/types/tools/bash-skill-urls.d.ts b/dist/types/tools/bash-skill-urls.d.ts
index 3b2082b47aedce441d203e34e8b947bb8f80f383..9817b085c51df99663511c80286a05293e8ce10a 100644
--- a/dist/types/tools/bash-skill-urls.d.ts
+++ b/dist/types/tools/bash-skill-urls.d.ts
@@ -14,6 +14,7 @@ export interface InternalUrlExpansionOptions {
     internalRouter?: InternalUrlResolver;
     localOptions?: LocalProtocolOptions;
     cwd?: string;
+    agentDir?: string;
     sessionFile?: string;
     sessionId?: string;
     agentRegistry?: ResolveContext["agentRegistry"];
diff --git a/dist/types/tools/index.d.ts b/dist/types/tools/index.d.ts
index 494649a89af723df86edc202ebc8ec1f23c7abf0..cadcaf6ddbb4612f096fd67d7e84489206137bf8 100644
--- a/dist/types/tools/index.d.ts
+++ b/dist/types/tools/index.d.ts
@@ -119,6 +119,8 @@ export interface DeferredDiagnosticsEntry {
 export interface ToolSession {
     /** Current working directory */
     cwd: string;
+    /** Agent directory supplied by the SDK; standalone tools use their settings root. */
+    agentDir?: string;
     /** Additional workspace directories beyond cwd (multi-root), forwarded to subagents. */
     additionalDirectories?: string[];
     /** Whether UI is available */
diff --git a/dist/types/tools/path-utils.d.ts b/dist/types/tools/path-utils.d.ts
index 59bf87045e41157def1b78a751aa899cec558d3c..cea71dea7d7dcf69e31356bc0cf376a2ce779bc0 100644
--- a/dist/types/tools/path-utils.d.ts
+++ b/dist/types/tools/path-utils.d.ts
@@ -352,6 +352,7 @@ export interface ResolvedExternalSearchUrl {
 export interface ToolScopeOptions {
     rawPaths: string[];
     cwd: string;
+    agentDir?: string;
     /** Verb used in the "Cannot {action} internal URL without a backing file: …" message. */
     internalUrlAction: string;
     /** Collect absolute paths flagged immutable by their internal-URL handler. */
diff --git a/src/eval/js/context-manager.ts b/src/eval/js/context-manager.ts
index 233b6c514af8be2f5399d04a829be8894eff66d6..c4d99347ca76f77099bc34e51b5a06fb735af0b5 100644
--- a/src/eval/js/context-manager.ts
+++ b/src/eval/js/context-manager.ts
@@ -70,13 +70,6 @@ interface PendingRun {
 	deferDrained?: PromiseWithResolvers<void>;
 	/** Set once the turn was cancelled; blocks new bridge calls during the drain. */
 	aborted: boolean;
-	/**
-	 * A worker `result` withheld because the cell still has bridge calls in
-	 * flight. `#runOne` reports a finished run without awaiting its pending
-	 * tools, so a floated or caught `agent()` would otherwise settle the run —
-	 * tearing down the abort listener — while the subagent kept going with
-	 * nothing left able to cancel it. Delivered once the last call drains.
-	 */
 	heldResult?: Extract<WorkerOutbound, { type: "result" }>;
 	settled: boolean;
 }
@@ -683,11 +676,6 @@ function settlePending(session: JsSession, msg: Extract<WorkerOutbound, { type:
 	// Once the turn is cancelled the scheduled kill is the sole settler, so a
 	// late worker result can't cut the abort drain short.
 	if (pending.aborted) return;
-	// A cell owns every bridge call it starts. The worker finishes a run without
-	// awaiting its outstanding tool calls, so `agent(...)` that is floated or
-	// caught would settle the run here — `runOnce` then drops the abort listener
-	// and the pending entry, leaving the subagent running with nothing able to
-	// cancel it. Hold the result until the last call drains.
 	if (pending.toolCalls.size > 0) {
 		pending.heldResult = msg;
 		return;
diff --git a/src/eval/js/worker-core.ts b/src/eval/js/worker-core.ts
index 30528423386976590fdf3bf7c3115fbabf0ce4f9..a3022fe6ad8cc40bb1e27570d2eb16404119b06c 100644
--- a/src/eval/js/worker-core.ts
+++ b/src/eval/js/worker-core.ts
@@ -19,6 +19,7 @@ interface ActiveRun {
 	runId: string;
 	filename: string;
 	pendingTools: Map<string, PendingTool>;
+	pendingToolsDrained: PromiseWithResolvers<void> | undefined;
 	/** Rejections floated by this run's cell code, captured before its result was sent. */
 	floatingRejections: unknown[];
 }
@@ -120,6 +121,7 @@ export class WorkerCore {
 	#unsubscribe: () => void;
 	#uninstallRejectionGuard: () => void;
 	#options: WorkerCoreOptions;
+	#closedError: ToolError | undefined;
 
 	constructor(transport: Transport, options: WorkerCoreOptions) {
 		this.#transport = transport;
@@ -163,6 +165,7 @@ export class WorkerCore {
 	 * cell activity and must keep the default fatal path.
 	 */
 	#consumeRejection(reason: unknown): boolean {
+		if (this.#closedError !== undefined && reason === this.#closedError) return true;
 		const stack = reason instanceof Error && typeof reason.stack === "string" ? reason.stack : undefined;
 		if (stack) {
 			// The stack can name several cells (helper defined by an earlier cell,
@@ -221,6 +224,7 @@ export class WorkerCore {
 	}
 
 	#handle(msg: WorkerInbound): void {
+		if (this.#closedError) return;
 		switch (msg.type) {
 			case "init":
 				try {
@@ -298,7 +302,13 @@ export class WorkerCore {
 	}
 
 	async #runOne(runId: string, code: string, filename: string, snapshot: SessionSnapshot): Promise<void> {
-		const active: ActiveRun = { runId, filename, pendingTools: new Map(), floatingRejections: [] };
+		const active: ActiveRun = {
+			runId,
+			filename,
+			pendingTools: new Map(),
+			pendingToolsDrained: undefined,
+			floatingRejections: [],
+		};
 		this.#runs.set(runId, active);
 		const hooks: RuntimeHooks = {
 			onText: chunk => this.#transport.send({ type: "text", runId, chunk }),
@@ -316,17 +326,7 @@ export class WorkerCore {
 		} catch (error) {
 			result = { type: "result", runId, ok: false, error: errorPayload(error) };
 		}
-		try {
-			// One event-loop turn so rejections the cell already floated surface
-			// while this run can still own them (rejection callbacks run before
-			// timers fire).
-			await Bun.sleep(0);
-			result = foldFloatingRejections(active, result, hooks);
-		} finally {
-			this.#runs.delete(runId);
-			this.#rememberCellFile(filename);
-			this.#transport.send(result);
-		}
+		await this.#finishRun(active, result, hooks);
 	}
 
 	async #invokeTool(msg: Extract<WorkerInbound, { type: "tool" }>): Promise<void> {
@@ -334,6 +334,7 @@ export class WorkerCore {
 			runId: msg.runId,
 			filename: `tool-${msg.runId}`,
 			pendingTools: new Map(),
+			pendingToolsDrained: undefined,
 			floatingRejections: [],
 		};
 		this.#runs.set(msg.runId, active);
@@ -343,6 +344,8 @@ export class WorkerCore {
 			callTool: (name, args) => this.#callTool(active, name, args),
 		};
 
+		let result: RunResult;
+		let envelope: Record<string, unknown> | undefined;
 		try {
 			const runtime = this.#runtime;
 			if (!runtime) throw new ToolError("JavaScript kernel is not running");
@@ -354,7 +357,6 @@ export class WorkerCore {
 				}
 			}
 
-			let envelope: Record<string, unknown>;
 			if (msg.op === "describe") {
 				const names = msg.names.length > 0 ? msg.names : [...tools.keys()];
 				envelope = {
@@ -377,13 +379,42 @@ export class WorkerCore {
 				}
 				envelope = { ok: true, value: cloneable };
 			}
-			this.#transport.send({ type: "display", runId: msg.runId, output: { type: "json", data: envelope } });
-			this.#transport.send({ type: "result", runId: msg.runId, ok: true });
+			result = { type: "result", runId: msg.runId, ok: true };
 		} catch (error) {
-			this.#transport.send({ type: "result", runId: msg.runId, ok: false, error: errorPayload(error) });
+			result = { type: "result", runId: msg.runId, ok: false, error: errorPayload(error) };
+		}
+		await this.#finishRun(active, result, hooks, envelope);
+	}
+
+	async #finishRun(
+		active: ActiveRun,
+		result: RunResult,
+		hooks: RuntimeHooks,
+		envelope?: Record<string, unknown>,
+	): Promise<void> {
+		while (true) {
+			if (this.#closedError || this.#runs.get(active.runId) !== active) return;
+			if (active.pendingTools.size > 0) {
+				active.pendingToolsDrained ??= Promise.withResolvers<void>();
+				await active.pendingToolsDrained.promise;
+			}
+			await new Promise<void>(resolve => setTimeout(resolve, 0));
+			if (this.#closedError || this.#runs.get(active.runId) !== active) return;
+			if (active.pendingTools.size === 0) break;
+		}
+		try {
+			result = foldFloatingRejections(active, result, hooks);
 		} finally {
-			this.#runs.delete(msg.runId);
+			this.#runs.delete(active.runId);
 			this.#rememberCellFile(active.filename);
+			if (result.ok && envelope) {
+				this.#transport.send({
+					type: "display",
+					runId: active.runId,
+					output: { type: "json", data: envelope },
+				});
+			}
+			this.#transport.send(result);
 		}
 	}
 
@@ -397,6 +428,8 @@ export class WorkerCore {
 	}
 
 	async #callTool(active: ActiveRun, name: string, args: unknown): Promise<unknown> {
+		if (this.#closedError) throw this.#closedError;
+		if (this.#runs.get(active.runId) !== active) throw new ToolError("JS run no longer active");
 		const id = `tc-${active.runId}-${crypto.randomUUID()}`;
 		const { promise, resolve, reject } = Promise.withResolvers<unknown>();
 		active.pendingTools.set(id, { runId: active.runId, resolve, reject });
@@ -406,55 +439,60 @@ export class WorkerCore {
 			// Non-serializable args (DataCloneError from postMessage / IPC send).
 			// No reply will ever arrive; fail this call instead of stranding a
 			// pending entry until close.
-			active.pendingTools.delete(id);
 			reject(error);
 		}
-		return await promise;
+		try {
+			return await promise;
+		} finally {
+			active.pendingTools.delete(id);
+			if (active.pendingTools.size === 0) {
+				active.pendingToolsDrained?.resolve();
+				active.pendingToolsDrained = undefined;
+			}
+		}
 	}
 
 	#deliverToolReply(id: string, reply: ToolReply): void {
 		for (const active of this.#runs.values()) {
 			const pending = active.pendingTools.get(id);
 			if (!pending) continue;
-			active.pendingTools.delete(id);
 			if (reply.ok) pending.resolve(reply.value);
 			else pending.reject(errorFromPayload(reply.error));
 			return;
 		}
 	}
 
-	#close(): void {
+	#close(notify = true): void {
+		if (this.#closedError) return;
+		const error = new ToolError("JS worker closed");
+		this.#closedError = error;
+		this.#unsubscribe();
 		for (const active of this.#runs.values()) {
 			for (const pending of active.pendingTools.values()) {
-				pending.reject(new ToolError("JS worker closed"));
+				pending.reject(error);
 			}
 			active.pendingTools.clear();
+			active.pendingToolsDrained?.resolve();
+			active.pendingToolsDrained = undefined;
 		}
-		this.#runs.clear();
 		this.#runtime?.dispose?.();
 		this.#runtime = null;
-		this.#transport.send({ type: "closed" });
-		this.#uninstallRejectionGuard();
-		this.#unsubscribe();
-		this.#transport.close();
+		// Keep rejection ownership until floated bridge shutdown errors surface.
+		setTimeout(() => {
+			this.#runs.clear();
+			this.#uninstallRejectionGuard();
+			try {
+				if (notify) this.#transport.send({ type: "closed" });
+			} finally {
+				try {
+					this.#transport.close();
+				} catch {
+				}
+			}
+		}, 0);
 	}
 
 	dispose(): void {
-		for (const active of this.#runs.values()) {
-			for (const pending of active.pendingTools.values()) {
-				pending.reject(new ToolError("JS worker closed"));
-			}
-			active.pendingTools.clear();
-		}
-		this.#runs.clear();
-		this.#runtime?.dispose?.();
-		this.#runtime = null;
-		this.#uninstallRejectionGuard();
-		this.#unsubscribe();
-		try {
-			this.#transport.close();
-		} catch {
-			// Ignore
-		}
+		this.#close(false);
 	}
 }
diff --git a/src/internal-urls/memory-protocol.ts b/src/internal-urls/memory-protocol.ts
index 6fff4150972c3a93d485e69a16f2b4b25104b416..16f8351587b08762a3ad66f40c0cb27fe8f947ee 100644
--- a/src/internal-urls/memory-protocol.ts
+++ b/src/internal-urls/memory-protocol.ts
@@ -1,6 +1,6 @@
 import * as fs from "node:fs/promises";
 import * as path from "node:path";
-import { getAgentDir, isEnoent } from "@oh-my-pi/pi-utils";
+import { isEnoent } from "@oh-my-pi/pi-utils";
 import { getMemoryRoot } from "../memories";
 import { getMnemopiSessionState, type MnemopiScopedMemoryHit, type MnemopiSessionState } from "../mnemopi/state";
 import { AgentRegistry } from "../registry/agent-registry";
@@ -25,32 +25,31 @@ const HINDSIGHT_UNADDRESSABLE =
 	"Hindsight memories are not addressable via memory://. Recall results are final — use `recall` to search or `reflect` to synthesize. `read memory://<id>` is only available with memory.backend=mnemopi.";
 
 /**
- * Snapshot of memory roots for every registered session, deduped.
- * Each session has its own cwd (possibly a worktree), so subagents and main
- * may see different roots.
+ * Snapshot of registered sessions' memory roots, deduped.
+ * Each session owns its agent directory and cwd. An explicit agent directory
+ * limits the sweep to sessions from that instance.
  */
-export function memoryRootsFromRegistry(): string[] {
-	const agentDir = getAgentDir();
+export function memoryRootsFromRegistry(context?: Pick<ResolveContext, "agentDir" | "agentRegistry">): string[] {
 	const roots: string[] = [];
-	for (const ref of AgentRegistry.global().list()) {
-		const sm = ref.session?.sessionManager;
-		if (!sm) continue;
-		const root = getMemoryRoot(agentDir, sm.getCwd());
+	for (const ref of (context?.agentRegistry ?? AgentRegistry.global()).list()) {
+		const session = ref.session;
+		if (!session || (context?.agentDir !== undefined && session.agentDir !== context.agentDir)) continue;
+		const root = getMemoryRoot(session.agentDir, session.sessionManager.getCwd());
 		if (root && !roots.includes(root)) roots.push(root);
 	}
 	return roots;
 }
 
 /**
- * File-backed memory roots visible to one caller. A context that names a cwd
- * pins the root to it; otherwise the bound caller's own cwd is used, so a
- * session-id-only caller never reads a peer project's summary. Contextless
- * legacy callers keep the registry-wide sweep.
+ * File-backed memory roots visible to one caller. The caller's agent directory
+ * and cwd select its root. Contextless callers keep the registry-wide sweep;
+ * an unbound cwd without an agent directory cannot identify a memory root.
  */
 function memoryRootsForContext(context: ResolveContext | undefined, caller: AgentSession | undefined): string[] {
 	const cwd = context?.cwd ?? caller?.sessionManager.getCwd();
-	if (cwd) return [getMemoryRoot(getAgentDir(), cwd)];
-	return memoryRootsFromRegistry();
+	const agentDir = context?.agentDir ?? caller?.agentDir;
+	if (cwd) return agentDir === undefined ? [] : [getMemoryRoot(agentDir, cwd)];
+	return memoryRootsFromRegistry(context);
 }
 
 function ensureWithinRoot(targetPath: string, rootPath: string): void {
@@ -272,7 +271,10 @@ interface MemoryCallerBinding {
  * sits in it, so two sessions in one worktree never impersonate each other.
  */
 function findCallerSession(context: ResolveContext): AgentSession | undefined {
-	const refs = (context.agentRegistry ?? AgentRegistry.global()).list();
+	let refs = (context.agentRegistry ?? AgentRegistry.global()).list();
+	if (context.agentDir !== undefined) {
+		refs = refs.filter(ref => ref.session?.agentDir === context.agentDir);
+	}
 	if (context.sessionFile !== undefined) {
 		const byFile = refs.find(ref => ref.session?.sessionFile === context.sessionFile)?.session;
 		if (byFile) return byFile;
@@ -301,7 +303,11 @@ function resolveMemoryCaller(context?: ResolveContext): MemoryCallerBinding {
 	}
 	// A cwd that names no single live session still scopes the file-backed root,
 	// but it identifies no bank: peer memory ids stay unreachable.
-	return { session: undefined, backend: memoryBackendFromContext(context), legacy: context.cwd === undefined };
+	return {
+		session: undefined,
+		backend: memoryBackendFromContext(context),
+		legacy: context.cwd === undefined && context.agentDir === undefined,
+	};
 }
 
 /**
diff --git a/src/internal-urls/types.ts b/src/internal-urls/types.ts
index 5d7725067619e5e292ade57ede75a80af898e54f..b8eaf90864ec4362e64c92f536421476ab0da595 100644
--- a/src/internal-urls/types.ts
+++ b/src/internal-urls/types.ts
@@ -92,6 +92,8 @@ export interface InternalUrl extends URL {
 export interface ResolveContext {
 	/** Working directory of the calling session. */
 	cwd?: string;
+	/** Agent directory supplied by the calling SDK session. */
+	agentDir?: string;
 	/**
 	 * Calling session's session file. Handlers that resolve agent ids which may
 	 * be parked (`history://<id>`, `agent://<id>`) refresh the caller's
diff --git a/src/mcp/loader.ts b/src/mcp/loader.ts
index 135a0c5dda889be04742636c4fbb6460780caa7e..28083486859fedc687a1fc0d65eca00826a84f3f 100644
--- a/src/mcp/loader.ts
+++ b/src/mcp/loader.ts
@@ -7,7 +7,8 @@ import { logger } from "@oh-my-pi/pi-utils";
 import type { LoadedCustomTool } from "../extensibility/custom-tools/types";
 import { AgentStorage } from "../session/agent-storage";
 import type { AuthStorage } from "../session/auth-storage";
-import { type MCPLoadResult, MCPManager } from "./manager";
+import { type MCPConfigLoader, type MCPLoadResult, MCPManager } from "./manager";
+import type { LoadMCPConfigsOptions } from "./config";
 import type { McpConnectionStatusEvent } from "./startup-events";
 import { MCPToolCache } from "./tool-cache";
 
@@ -39,6 +40,8 @@ export interface MCPToolsLoadOptions {
 	cacheStorage?: AgentStorage | null;
 	/** Auth storage used to resolve OAuth credentials before initial MCP connect */
 	authStorage?: AuthStorage;
+	configLoader?: MCPConfigLoader;
+	extensionRoots?: LoadMCPConfigsOptions["extensionRoots"];
 }
 
 async function resolveToolCache(storage: AgentStorage | null | undefined): Promise<MCPToolCache | null> {
@@ -61,7 +64,7 @@ async function resolveToolCache(storage: AgentStorage | null | undefined): Promi
  */
 export async function discoverAndLoadMCPTools(cwd: string, options?: MCPToolsLoadOptions): Promise<MCPToolsLoadResult> {
 	const toolCache = await resolveToolCache(options?.cacheStorage);
-	const manager = new MCPManager(cwd, toolCache);
+	const manager = new MCPManager(cwd, toolCache, options?.configLoader);
 	if (options?.authStorage) {
 		manager.setAuthStorage(options.authStorage);
 	}
@@ -73,6 +76,7 @@ export async function discoverAndLoadMCPTools(cwd: string, options?: MCPToolsLoa
 			enableProjectConfig: options?.enableProjectConfig,
 			filterExa: options?.filterExa,
 			filterBrowser: options?.filterBrowser,
+			extensionRoots: options?.extensionRoots,
 		});
 	} catch (error) {
 		// If discovery fails entirely, return empty result
diff --git a/src/modes/controllers/command-controller.ts b/src/modes/controllers/command-controller.ts
index c70f2a471fa69518c823a11457098acb49a253c5..b8769a0778471c03a88dbbd4f50ba7be3876cd2e 100644
--- a/src/modes/controllers/command-controller.ts
+++ b/src/modes/controllers/command-controller.ts
@@ -689,7 +689,7 @@ export class CommandController {
 	async handleMemoryCommand(text: string): Promise<void> {
 		const argumentText = text.slice(7).trim();
 		const action = argumentText.split(/\s+/, 1)[0]?.toLowerCase() || "view";
-		const agentDir = this.ctx.settings.getAgentDir();
+		const agentDir = this.ctx.session.agentDir;
 		const backend = await resolveMemoryBackend(this.ctx.settings);
 
 		if (action === "view") {
diff --git a/src/modes/controllers/input-controller.ts b/src/modes/controllers/input-controller.ts
index addee4e7677f5cf8fc73160c66b478d3963271c6..a3eedc9efa6f5ae60655b9223378aabdac5b4b8e 100644
--- a/src/modes/controllers/input-controller.ts
+++ b/src/modes/controllers/input-controller.ts
@@ -2057,6 +2057,7 @@ export class InputController {
 				const manager = this.ctx.viewSession.sessionManager;
 				return {
 					cwd: manager.getCwd(),
+					agentDir: this.ctx.viewSession.agentDir,
 					sessionId: manager.getSessionId(),
 					sessionFile: manager.getSessionFile(),
 				};
diff --git a/src/modes/internal-url-autocomplete.ts b/src/modes/internal-url-autocomplete.ts
index b16fe83bd3c9571bbfad41cd14a2f6400963877f..b3dc73f96f8565da3a61a27fcc69e7f9fcc18563 100644
--- a/src/modes/internal-url-autocomplete.ts
+++ b/src/modes/internal-url-autocomplete.ts
@@ -88,7 +88,7 @@ export function extractInternalUrlContext(textBeforeCursor: string): InternalUrl
 }
 
 /** Caller binding shared by tool execution and prompt completion. */
-export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "sessionFile" | "sessionId" | "agentRegistry">;
+export type InternalUrlCallerContext = Pick<ResolveContext, "cwd" | "agentDir" | "sessionFile" | "sessionId" | "agentRegistry">;
 
 /**
  * Suggestions for the internal-url token ending at the cursor, or `null` when
diff --git a/src/modes/rpc/rpc-mode.ts b/src/modes/rpc/rpc-mode.ts
index 45d59e034199245a591df37abe13eef9e43804f3..507eab85c7f691da0c961c4d2b7531085a881c77 100644
--- a/src/modes/rpc/rpc-mode.ts
+++ b/src/modes/rpc/rpc-mode.ts
@@ -32,6 +32,7 @@ import {
 import { loadSlashCommands } from "../../extensibility/slash-commands";
 import { type Theme, theme } from "../../modes/theme/theme";
 import type { AgentSession } from "../../session/agent-session";
+import type { PromptDeliveryObserver } from "../../session/agent-session-types";
 import { SKILL_PROMPT_MESSAGE_TYPE, USER_INTERRUPT_LABEL } from "../../session/messages";
 import { executeAcpBuiltinSlashCommand } from "../../slash-commands/acp-builtins";
 import { buildAvailableSlashCommands } from "../../slash-commands/available-commands";
@@ -151,6 +152,7 @@ export async function runRpcSkillCommand(
 	invocation: RpcSkillInvocation,
 	streamingBehavior: "steer" | "followUp" = "steer",
 	prebuilt?: BuiltSkillPromptMessage,
+	deliveryObserver?: PromptDeliveryObserver,
 ): Promise<boolean> {
 	const built = prebuilt ?? (await buildSkillPromptMessage(invocation.skill, invocation.args, "user"));
 	return session.promptCustomMessage(
@@ -161,7 +163,7 @@ export async function runRpcSkillCommand(
 			details: built.details,
 			attribution: "user",
 		},
-		{ streamingBehavior },
+		deliveryObserver ? { streamingBehavior, deliveryObserver } : { streamingBehavior },
 	);
 }
 
@@ -204,10 +206,11 @@ export async function tryRunRpcSkillCommand(
 	session: RpcSkillCommandSession,
 	text: string,
 	streamingBehavior: "steer" | "followUp" = "steer",
+	deliveryObserver?: PromptDeliveryObserver,
 ): Promise<RpcSkillCommandResult | false> {
 	const invocation = resolveRpcSkillInvocation(session, text);
 	if (!invocation) return false;
-	await runRpcSkillCommand(session, invocation, streamingBehavior);
+	await runRpcSkillCommand(session, invocation, streamingBehavior, undefined, deliveryObserver);
 	return { agentInvoked: true };
 }
 
diff --git a/src/modes/utils/interactive-context-helpers.ts b/src/modes/utils/interactive-context-helpers.ts
index 13dfe99d03580734d07af6240d0d218a4c6be9b8..3f828ef59b70ab0d88db57bad08cd0a9b16b99ec 100644
--- a/src/modes/utils/interactive-context-helpers.ts
+++ b/src/modes/utils/interactive-context-helpers.ts
@@ -46,6 +46,7 @@ export async function refreshAssistantMessageLinkTargets(
 	if (hrefs.size === 0) return previous;
 	const resolved = await resolveMarkdownLinkTargets(texts, {
 		cwd: session.sessionManager.getCwd(),
+		agentDir: session.agentDir,
 		sessionFile: session.sessionFile,
 		settings: session.settings,
 		localProtocolOptions: {
diff --git a/src/sdk.ts b/src/sdk.ts
index 4353d352dff8ffa52ce14dc3fafe350d2611d963..243ae7471f48954d7f57a2f29c06ad999750955f 100644
--- a/src/sdk.ts
+++ b/src/sdk.ts
@@ -140,6 +140,7 @@ import {
 	parseMCPToolName,
 	shouldFilterBrowserMCPForPrelude,
 } from "./mcp";
+import type { MCPConfigLoader } from "./mcp/manager";
 import { MCP_CONNECTION_STATUS_EVENT_CHANNEL, type McpConnectionStatusEvent } from "./mcp/startup-events";
 import { createSessionMemoryRuntimeContext, resolveMemoryBackend } from "./memory-backend";
 import { MEMORY_BACKEND_TOOL_NAMES } from "./memory-backend/tool-names";
@@ -542,6 +543,7 @@ export interface CreateAgentSessionOptions {
 	enableMCP?: boolean;
 	/** Existing MCP manager to reuse when MCP is enabled (skips discovery, propagates to toolSession). */
 	mcpManager?: MCPManager;
+	mcpConfigLoader?: MCPConfigLoader;
 
 	/** Enable LSP integration (tool, formatting, diagnostics, warmup). Default: true */
 	enableLsp?: boolean;
@@ -1788,6 +1790,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 			get cwd() {
 				return sessionManager.getCwd();
 			},
+			agentDir,
 			isToolActive: name => activeToolNames.has(name),
 			setActiveToolNames,
 			toolRegistry,
@@ -2016,7 +2019,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 		if (enableMCP && !mcpManager) {
 			if (deferMCPDiscoveryForUI) {
 				const cacheStorage = settings.getStorage();
-				mcpManager = new MCPManager(cwd, cacheStorage ? new MCPToolCache(cacheStorage) : null);
+				mcpManager = new MCPManager(cwd, cacheStorage ? new MCPToolCache(cacheStorage) : null, options.mcpConfigLoader);
 				mcpManager.setAuthStorage(authStorage);
 				toolSession.mcpManager = mcpManager;
 
@@ -2055,6 +2058,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 					...mcpDiscoverOptions,
 					cacheStorage: settings.getStorage(),
 					authStorage,
+					configLoader: options.mcpConfigLoader,
 				});
 				mcpManager = mcpResult.manager;
 				toolSession.mcpManager = mcpManager;
@@ -2805,7 +2809,7 @@ async function createAgentSessionScoped(options: CreateAgentSessionOptions): Pro
 			cwd,
 			sessionManager,
 			modelRegistry,
-			() => (hasSession ? createSessionMemoryRuntimeContext(session, agentDir, cwd) : undefined),
+			() => (hasSession ? createSessionMemoryRuntimeContext(session, agentDir, sessionManager.getCwd()) : undefined),
 			settings,
 			localProtocolOptions,
 			() => (hasSession ? session.getAsyncJobSnapshot() : null),
diff --git a/src/session/agent-session-types.ts b/src/session/agent-session-types.ts
index dc7f27445225bb9ecc263cd7310ef384f84c0049..4481a4ed7a316820a1fd54655de3a5048bd71f1d 100644
--- a/src/session/agent-session-types.ts
+++ b/src/session/agent-session-types.ts
@@ -327,8 +327,16 @@ export interface AgentSessionConfig {
 	titleSystemPrompt?: string;
 }
 
+/** SDK hosts observe admission and final delivery of one submitted message. */
+export interface PromptDeliveryObserver {
+	onAccepted(delivery: "prompt" | "steer" | "followUp" | "aside"): void;
+	onConsumed(): void;
+	onDiscarded(): void;
+}
+
 /** Options for AgentSession.prompt(). */
 export interface PromptOptions {
+	deliveryObserver?: PromptDeliveryObserver;
 	/** Whether to expand file-based prompt templates (default: true). */
 	expandPromptTemplates?: boolean;
 	/** Image attachments. */
diff --git a/src/session/agent-session.ts b/src/session/agent-session.ts
index e8d114fd9f65a64e64f9642f44b9abd85db87798..15c92b62e31b98bd7d30cb4acc980b3c256d5925 100644
--- a/src/session/agent-session.ts
+++ b/src/session/agent-session.ts
@@ -33,9 +33,12 @@ import {
 	type AgentToolResult,
 	type AgentTurnEndContext,
 	AppendOnlyContextManager,
+	ASIDE_MESSAGE_COMMIT,
+	ASIDE_MESSAGE_DISCARD,
 	type AsideMessage,
 	type BeforeToolCallContext,
 	type BeforeToolCallResult,
+	type CommittableAsideMessage,
 	EventLoopKeepalive,
 	resolveTelemetry,
 	type StreamFn,
@@ -247,6 +250,7 @@ import type {
 	HandoffResult,
 	ModelCycleResult,
 	Prewalk,
+	PromptDeliveryObserver,
 	PromptOptions,
 	ResetSessionContextResult,
 	ResolvedRoleModel,
@@ -532,6 +536,7 @@ export class AgentSession {
 	readonly agent: Agent;
 	readonly sessionManager: SessionManager;
 	readonly settings: Settings;
+	readonly agentDir: string;
 	/** Entries of tools mounted under `xd://`; empty when virtual devices are unmounted. */
 	getXdevToolEntries: () => Array<{ name: string; summary: string }>;
 	readonly yieldQueue: YieldQueue;
@@ -656,6 +661,7 @@ export class AgentSession {
 	/** Host hook invoked when a typed user prompt is dropped before dispatch;
 	 *  see {@link setPromptDropped}. */
 	#promptDropped: ((prompt: DroppedPrompt) => void) | undefined;
+	#promptDeliveries: Map<CommittableAsideMessage, PromptDeliveryObserver> | undefined;
 	#titleGenerationAbortController = new AbortController();
 	#toolChoiceQueue = new ToolChoiceQueue();
 
@@ -979,6 +985,8 @@ export class AgentSession {
 		for (const record of records) {
 			this.agent.emitExternalEvent({ type: "message_start", message: record });
 			this.agent.emitExternalEvent({ type: "message_end", message: record });
+			const committable: CommittableAsideMessage = record;
+			committable[ASIDE_MESSAGE_COMMIT]?.();
 		}
 	}
 
@@ -1225,6 +1233,7 @@ export class AgentSession {
 		this.#codeModeState = config.codeModeState ?? {};
 		this.sessionManager = config.sessionManager;
 		this.settings = config.settings;
+		this.agentDir = config.memoryAgentDir ?? config.settings.getAgentDir();
 		this.#modelRegistry = config.modelRegistry;
 		this.#extensionRoots =
 			config.extensionRoots ??
@@ -4497,7 +4506,7 @@ export class AgentSession {
 
 	/**
 	 * Synchronously mark the session as disposing so new work is rejected
-	 * immediately: eval starts throw, queued asides are dropped, and the
+	 * immediately: eval starts throw, queued asides are flushed, and the
 	 * aside provider is detached. Idempotent; `dispose()` runs it first.
 	 *
 	 * Wrappers that await other teardown before delegating to `dispose()` MUST
@@ -4505,6 +4514,7 @@ export class AgentSession {
 	 * gap slips past the disposal guards.
 	 */
 	beginDispose(): void {
+		if (this.#isDisposed) return;
 		this.#isDisposed = true;
 		this.#modelDiscoveryAbortController.abort();
 		this.#queuedMessageDrainBlocked = false;
@@ -4517,6 +4527,7 @@ export class AgentSession {
 		this.#titleGenerationAbortController.abort();
 		this.#abortAutolearnCapture();
 		this.#irc.flushPending();
+		this.#discardPromptDeliveries();
 		this.yieldQueue.clear();
 		this.agent.setAsideMessageProvider(undefined);
 		this.agent.hasIrcInterrupts = undefined;
@@ -4536,7 +4547,11 @@ export class AgentSession {
 	 */
 	#disposeCall?: Promise<void>;
 	dispose(options: AgentSessionDisposeOptions = {}): Promise<void> {
-		if (!this.#disposeCall) this.#disposeCall = this.#doDispose(options);
+		if (!this.#disposeCall) {
+			const { promise, resolve, reject } = Promise.withResolvers<void>();
+			this.#disposeCall = promise;
+			void this.#doDispose(options).then(resolve, reject);
+		}
 		return this.#disposeCall;
 	}
 
@@ -4911,6 +4926,8 @@ export class AgentSession {
 
 		// Drop the conversation: messages, queued steers/follow-ups, pending tool
 		// calls, and error state. agent.reset() keeps the model and system prompt.
+		this.#discardPromptDeliveries(this.agent.peekSteeringQueue());
+		this.#discardPromptDeliveries(this.agent.peekFollowUpQueue());
 		this.agent.reset();
 		this.#pendingNextTurnMessages = [];
 		this.#experimentalContextNotesReminder = undefined;
@@ -5631,6 +5648,10 @@ export class AgentSession {
 		const filteredFollowUp = followUp.filter(message => !isVibeContext(message));
 		if (filteredSteering.length !== steering.length || filteredFollowUp.length !== followUp.length) {
 			this.agent.replaceQueues(filteredSteering, filteredFollowUp);
+			if (this.#promptDeliveries) {
+				this.#discardPromptDeliveries(steering.filter(isVibeContext));
+				this.#discardPromptDeliveries(followUp.filter(isVibeContext));
+			}
 			this.#reconcileQueuedMessageDrain();
 		}
 		this.#pendingNextTurnMessages = this.#pendingNextTurnMessages.filter(message => !isVibeContext(message));
@@ -6176,7 +6197,9 @@ export class AgentSession {
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
 			}
-			await this.#queueUserMessage(expandedText, options?.images, streamingBehavior, submittedAt);
+			await this.#queueUserMessage(
+				expandedText, options?.images, streamingBehavior, submittedAt, undefined, options?.deliveryObserver,
+			);
 			return true;
 		}
 
@@ -6222,10 +6245,11 @@ export class AgentSession {
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
 			}
-			await this.#queueUserMessage(expandedText, options?.images, streamingBehavior, submittedAt, {
-				images: normalizedImages,
-				descriptionNotice: imageDescriptionNotice,
-			});
+			await this.#queueUserMessage(
+				expandedText, options?.images, streamingBehavior, submittedAt,
+				{ images: normalizedImages, descriptionNotice: imageDescriptionNotice },
+				options?.deliveryObserver,
+			);
 			return true;
 		}
 
@@ -6303,7 +6327,7 @@ export class AgentSession {
 	 */
 	async promptCustomMessage<T = unknown>(
 		message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">,
-		options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice"> & {
+		options?: Pick<PromptOptions, "streamingBehavior" | "toolChoice" | "deliveryObserver"> & {
 			queueChipText?: string;
 			queueOnly?: boolean;
 		},
@@ -6342,7 +6366,7 @@ export class AgentSession {
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
 			}
-			await this.#queueCustomMessage(message, streamingBehavior, options.queueChipText);
+			await this.#queueCustomMessage(message, streamingBehavior, options.queueChipText, options.deliveryObserver);
 			return true;
 		}
 		if (this.isStreaming) {
@@ -6352,7 +6376,7 @@ export class AgentSession {
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
 			}
-			await this.#queueCustomMessage(message, streamingBehavior, options?.queueChipText);
+			await this.#queueCustomMessage(message, streamingBehavior, options?.queueChipText, options?.deliveryObserver);
 			return true;
 		}
 
@@ -6375,7 +6399,7 @@ export class AgentSession {
 	async #promptWithMessage(
 		message: AgentMessage,
 		expandedText: string,
-		options?: Pick<PromptOptions, "toolChoice" | "images" | "skipCompactionCheck"> & {
+		options?: Pick<PromptOptions, "toolChoice" | "images" | "skipCompactionCheck" | "deliveryObserver"> & {
 			prependMessages?: AgentMessage[];
 			skipPostPromptRecoveryWait?: boolean;
 			acceptTerminalEmptyStop?: boolean;
@@ -6385,6 +6409,13 @@ export class AgentSession {
 		// every pre-dispatch bail (generation bump from abort, disposal, usage
 		// preflight denial) exits silently, and prompt() uses the outcome to hand
 		// the typed text back to the host instead of losing it.
+		if (options?.deliveryObserver) {
+			if (this.#isDisposed) {
+				this.#notifyPromptDelivery(options.deliveryObserver, "discarded");
+				return false;
+			}
+			this.#observePromptDelivery(message, options.deliveryObserver, "prompt");
+		}
 		this.#beginInFlight();
 		const generation = this.#promptGeneration;
 		this.#promptSequence++;
@@ -6614,6 +6645,9 @@ export class AgentSession {
 			}
 			return true;
 		} finally {
+			if (options?.deliveryObserver && this.#promptDeliveries?.has(message)) {
+				this.#discardPromptDeliveries([message]);
+			}
 			// The per-turn before_agent_start override lives only for this turn.
 			this.#tools.clearTurnSystemPromptOverride();
 			this.#usagePreflightReadyForNextModelCall = false;
@@ -6866,12 +6900,68 @@ export class AgentSession {
 		}
 	}
 
+	#notifyPromptDelivery(
+		observer: PromptDeliveryObserver,
+		delivery: "prompt" | "steer" | "followUp" | "aside" | "consumed" | "discarded",
+	): void {
+		try {
+			if (delivery === "consumed") observer.onConsumed();
+			else if (delivery === "discarded") observer.onDiscarded();
+			else observer.onAccepted(delivery);
+		} catch {
+			logger.warn("Prompt delivery observer failed", { delivery });
+		}
+	}
+
+	#observePromptDelivery(
+		message: CommittableAsideMessage,
+		observer: PromptDeliveryObserver,
+		delivery: "prompt" | "steer" | "followUp" | "aside",
+	): void {
+		this.#promptDeliveries ??= new Map();
+		this.#promptDeliveries.set(message, observer);
+		const commit = message[ASIDE_MESSAGE_COMMIT];
+		const discard = message[ASIDE_MESSAGE_DISCARD];
+		const finish = (consumed: boolean, error?: Error): void => {
+			const pending = this.#promptDeliveries?.get(message);
+			if (!pending) return;
+			this.#promptDeliveries?.delete(message);
+			if (this.#promptDeliveries?.size === 0) this.#promptDeliveries = undefined;
+			if (commit) message[ASIDE_MESSAGE_COMMIT] = commit;
+			else delete message[ASIDE_MESSAGE_COMMIT];
+			if (discard) message[ASIDE_MESSAGE_DISCARD] = discard;
+			else delete message[ASIDE_MESSAGE_DISCARD];
+			try {
+				if (consumed) commit?.();
+				else if (error) discard?.(error);
+			} finally {
+				if (consumed && delivery === "prompt") this.#notifyPromptDelivery(pending, "prompt");
+				this.#notifyPromptDelivery(pending, consumed ? "consumed" : "discarded");
+			}
+		};
+		message[ASIDE_MESSAGE_COMMIT] = () => finish(true);
+		message[ASIDE_MESSAGE_DISCARD] = error => finish(false, error);
+	}
+
+	#discardPromptDeliveries(messages?: Iterable<AgentMessage>): void {
+		const pending = this.#promptDeliveries;
+		if (!pending) return;
+		let error: Error | undefined;
+		for (const message of messages ?? pending.keys()) {
+			if (!pending.has(message)) continue;
+			error ??= new Error("Prompt removed before context insertion");
+			const committable: CommittableAsideMessage = message;
+			committable[ASIDE_MESSAGE_DISCARD]?.(error);
+		}
+	}
+
 	async #queueUserMessage(
 		text: string,
 		images: ImageContent[] | undefined,
 		mode: "steer" | "followUp" | "aside",
 		timestamp?: number,
 		preprocessed?: { images: ImageContent[] | undefined; descriptionNotice: CustomMessage | undefined },
+		deliveryObserver?: PromptDeliveryObserver,
 	): Promise<void> {
 		// Captured before any await below so the aside branch can detect a
 		// newSession()/switchSession() that completed while normalization/vision
@@ -6902,12 +6992,25 @@ export class AgentSession {
 			: normalizedImages?.length
 				? await this.#buildImageDescriptionNotice(normalizedImages)
 				: undefined;
+		if ((mode === "aside" || deliveryObserver) && await this.#sessionGenerationChanged(sessionGeneration)) {
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		if (deliveryObserver && this.#isDisposed) {
+			this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		const message: CommittableAsideMessage = {
+			role: "user", content, attribution: "user", timestamp: timestamp ?? Date.now(),
+		};
+		if (mode === "steer") message.steering = true;
+		if (deliveryObserver) this.#observePromptDelivery(message, deliveryObserver, mode);
 		if (mode === "aside") {
-			if (await this.#sessionGenerationChanged(sessionGeneration)) return;
 			const records: AgentMessage[] = [];
 			if (imageDescriptionNotice) records.push(imageDescriptionNotice);
-			records.push({ role: "user", content, attribution: "user", timestamp: timestamp ?? Date.now() });
+			records.push(message);
 			this.#irc.queueAside(records);
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "aside");
 			// The awaits above (image normalization / vision description) can span the run's
 			// settle, so the run may already be idle by the time the record lands in the aside
 			// queue with no loop left to drain it. Resuming here is a no-op while streaming and
@@ -6919,23 +7022,13 @@ export class AgentSession {
 		if (mode === "followUp") {
 			for (const notice of videoAttachmentNotices) this.agent.followUp(notice);
 			if (imageDescriptionNotice) this.agent.followUp(imageDescriptionNotice);
-			this.agent.followUp({
-				role: "user",
-				content,
-				attribution: "user",
-				timestamp: timestamp ?? Date.now(),
-			});
+			this.agent.followUp(message);
 		} else {
 			for (const notice of videoAttachmentNotices) this.agent.steer(notice);
 			if (imageDescriptionNotice) this.agent.steer(imageDescriptionNotice);
-			this.agent.steer({
-				role: "user",
-				content,
-				steering: true,
-				attribution: "user",
-				timestamp: timestamp ?? Date.now(),
-			});
+			this.agent.steer(message);
 		}
+		if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, mode);
 		this.#scheduleIdleQueueDrain();
 	}
 
@@ -7131,6 +7224,7 @@ export class AgentSession {
 		message: Pick<CustomMessage<T>, "customType" | "content" | "display" | "details" | "attribution">,
 		deliverAs: "steer" | "followUp" | "aside",
 		queueChipText?: string,
+		deliveryObserver?: PromptDeliveryObserver,
 	): Promise<void> {
 		// Captured before the normalization await below — see #sessionGeneration's doc comment.
 		const sessionGeneration = this.#sessionGeneration;
@@ -7154,12 +7248,21 @@ export class AgentSession {
 			timestamp: Date.now(),
 		};
 		const normalizedAppMessage = await this.#normalizeAgentMessageImages(appMessage);
+		if ((deliverAs === "aside" || deliveryObserver) && await this.#sessionGenerationChanged(sessionGeneration)) {
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		if (deliveryObserver && this.#isDisposed) {
+			this.#notifyPromptDelivery(deliveryObserver, "discarded");
+			return;
+		}
+		if (deliveryObserver) this.#observePromptDelivery(normalizedAppMessage, deliveryObserver, deliverAs);
 		if (deliverAs === "aside") {
-			if (await this.#sessionGenerationChanged(sessionGeneration)) return;
 			// Non-interrupting: rides the same step-boundary aside poll as
 			// sendCustomMessage's streaming aside branch — not an agent-core queue
 			// entry, so no drain-retry latch and no idle-queue drain scheduling.
 			this.#irc.queueAside([normalizedAppMessage]);
+			if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, "aside");
 			// The image-normalization await above can span the run's settle, so the run may
 			// already be idle by the time the record lands in the aside queue with no loop
 			// left to drain it. Resuming here is a no-op while streaming and wakes/folds
@@ -7173,6 +7276,7 @@ export class AgentSession {
 		} else {
 			this.agent.steer(normalizedAppMessage);
 		}
+		if (deliveryObserver) this.#notifyPromptDelivery(deliveryObserver, deliverAs);
 		this.#scheduleIdleQueueDrain();
 	}
 
@@ -7325,7 +7429,7 @@ export class AgentSession {
 	 */
 	async sendUserMessage(
 		content: string | (TextContent | ImageContent)[],
-		options?: { deliverAs?: "steer" | "followUp" | "aside" },
+		options?: { deliverAs?: "steer" | "followUp" | "aside"; deliveryObserver?: PromptDeliveryObserver },
 	): Promise<void> {
 		// Normalize content to text string + optional images
 		let text: string;
@@ -7350,17 +7454,17 @@ export class AgentSession {
 		let deliveredAsAside = false;
 		if (options?.deliverAs === "aside") {
 			if (this.isStreaming) {
-				await this.#queueUserMessage(text, images, "aside");
+				await this.#queueUserMessage(text, images, "aside", undefined, undefined, options.deliveryObserver);
 				return;
 			}
 			// Idle: fall through to the prompt flow below (starts a turn, like an omitted
 			// deliverAs) — there is no live run to inject an aside into.
 			deliveredAsAside = true;
 		} else if (options?.deliverAs === "followUp") {
-			await this.#queueUserMessage(text, images, "followUp");
+			await this.#queueUserMessage(text, images, "followUp", undefined, undefined, options.deliveryObserver);
 			return;
 		} else if (options?.deliverAs === "steer") {
-			await this.#queueUserMessage(text, images, "steer");
+			await this.#queueUserMessage(text, images, "steer", undefined, undefined, options.deliveryObserver);
 			return;
 		}
 
@@ -7375,6 +7479,7 @@ export class AgentSession {
 			expandPromptTemplates: false,
 			images,
 			streamingBehavior: deliveredAsAside ? "aside" : "steer",
+			deliveryObserver: options?.deliveryObserver,
 		});
 	}
 
@@ -7398,6 +7503,10 @@ export class AgentSession {
 			? isAdvisorCard
 			: m => !isUserQueuedMessage(m) && !isHiddenUserCompanion(m);
 		this.agent.replaceQueues(steeringAll.filter(keep), followUpAll.filter(keep));
+		if (this.#promptDeliveries) {
+			this.#discardPromptDeliveries(steeringAll.filter(message => !keep(message)));
+			this.#discardPromptDeliveries(followUpAll.filter(message => !keep(message)));
+		}
 		this.#reconcileQueuedMessageDrain();
 		return { steering, followUp };
 	}
@@ -7448,6 +7557,7 @@ export class AgentSession {
 		if (fromSteer >= 0) {
 			const removed = steering[fromSteer];
 			this.agent.replaceQueues(removeWithCompanions(steering, fromSteer), followUp.slice());
+			if (this.#promptDeliveries) this.#discardPromptDeliveries([removed]);
 			this.#reconcileQueuedMessageDrain();
 			return toRestoredQueuedMessage(removed);
 		}
@@ -7455,6 +7565,7 @@ export class AgentSession {
 		if (fromFollowUp >= 0) {
 			const removed = followUp[fromFollowUp];
 			this.agent.replaceQueues(steering.slice(), removeWithCompanions(followUp, fromFollowUp));
+			if (this.#promptDeliveries) this.#discardPromptDeliveries([removed]);
 			this.#reconcileQueuedMessageDrain();
 			return toRestoredQueuedMessage(removed);
 		}
@@ -7866,6 +7977,7 @@ export class AgentSession {
 			advisorRecordersDetached = true;
 			await this.#advisors.drainAndDetachRecorders();
 			try {
+				this.#discardPromptDeliveries();
 				this.agent.reset();
 				if (options?.drop && previousSessionFile) {
 					try {
@@ -9249,6 +9361,11 @@ export class AgentSession {
 			}
 			transitionSettled.resolve();
 			this.#sessionTransitionSettled = previousSessionTransitionSettled;
+			this.#discardPromptDeliveries(previousSteeringMessages);
+			this.#discardPromptDeliveries(previousFollowUpMessages);
+			this.#discardPromptDeliveries(previousIrcPending.interrupts);
+			this.#discardPromptDeliveries(previousIrcPending.asides);
+			this.#discardPromptDeliveries(previousIrcPending.deferredWakes);
 			return true;
 		} catch (error) {
 			this.sessionManager.restoreState(previousSessionState);
@@ -9505,6 +9622,8 @@ export class AgentSession {
 
 		this.#pendingNextTurnMessages = [];
 		this.#scheduledHiddenNextTurnGeneration = undefined;
+		this.#discardPromptDeliveries(this.agent.peekSteeringQueue());
+		this.#discardPromptDeliveries(this.agent.peekFollowUpQueue());
 		this.agent.replaceQueues([], []);
 		this.#queuedMessageDrainBlocked = false;
 		this.#usagePreflightReadyForNextModelCall = false;
diff --git a/src/session/irc-bridge.ts b/src/session/irc-bridge.ts
index 35c0a35b1d19ec438d8cea17c9f957caa13efbb2..462fc3a06c4b8ca7d9d6d78fd7929629e9abb6bb 100644
--- a/src/session/irc-bridge.ts
+++ b/src/session/irc-bridge.ts
@@ -1,4 +1,4 @@
-import type { Agent, AgentMessage } from "@oh-my-pi/pi-agent-core";
+import { type Agent, type AgentMessage, ASIDE_MESSAGE_COMMIT, type CommittableAsideMessage } from "@oh-my-pi/pi-agent-core";
 import { logger, prompt } from "@oh-my-pi/pi-utils";
 import type { Settings } from "../config/settings";
 import { IrcBus, type IrcMessage } from "../irc/bus";
@@ -256,6 +256,8 @@ export class IrcBridge {
 		for (const record of this.drainPending()) {
 			this.#host.agent.emitExternalEvent({ type: "message_start", message: record });
 			this.#host.agent.emitExternalEvent({ type: "message_end", message: record });
+			const committable: CommittableAsideMessage = record;
+			committable[ASIDE_MESSAGE_COMMIT]?.();
 		}
 	}
 
diff --git a/src/slash-commands/builtin-lifecycle.ts b/src/slash-commands/builtin-lifecycle.ts
index 34d3c8dc97b0e7dfe0b9f9f1f800aff72c992e58..6fdd3c8fd35c4da60192605a503419d387624975 100644
--- a/src/slash-commands/builtin-lifecycle.ts
+++ b/src/slash-commands/builtin-lifecycle.ts
@@ -592,7 +592,7 @@ export const BUILTIN_LIFECYCLE_SLASH_COMMANDS: ReadonlyArray<SlashCommandSpec> =
 			switch (verb) {
 				case "view": {
 					const payload = await backend.buildDeveloperInstructions(
-						runtime.settings.getAgentDir(),
+						runtime.session.agentDir,
 						runtime.settings,
 						runtime.session,
 					);
@@ -601,20 +601,20 @@ export const BUILTIN_LIFECYCLE_SLASH_COMMANDS: ReadonlyArray<SlashCommandSpec> =
 				}
 				case "clear":
 				case "reset": {
-					await backend.clear(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					await backend.clear(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.session.refreshBaseSystemPrompt();
 					await runtime.output("Memory cleared.");
 					return commandConsumed();
 				}
 				case "enqueue":
 				case "rebuild": {
-					await backend.enqueue(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					await backend.enqueue(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.output("Memory consolidation enqueued.");
 					return commandConsumed();
 				}
 				case "queue": {
 					const payload = await backend.queuePreview?.({
-						agentDir: runtime.settings.getAgentDir(),
+						agentDir: runtime.session.agentDir,
 						cwd: runtime.cwd,
 						session: runtime.session,
 					});
@@ -622,14 +622,14 @@ export const BUILTIN_LIFECYCLE_SLASH_COMMANDS: ReadonlyArray<SlashCommandSpec> =
 					return commandConsumed();
 				}
 				case "sync": {
-					await backend.enqueue(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					await backend.enqueue(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.output("Memory consolidation ran.");
 					return commandConsumed();
 				}
 				case "stats":
 				case "diagnose": {
 					const hook = verb === "stats" ? backend.stats : backend.diagnose;
-					const payload = await hook?.(runtime.settings.getAgentDir(), runtime.cwd, runtime.session);
+					const payload = await hook?.(runtime.session.agentDir, runtime.cwd, runtime.session);
 					await runtime.output(payload ?? memoryStatsUnavailableMessage(backend.id, verb));
 					return commandConsumed();
 				}
diff --git a/src/task/executor.ts b/src/task/executor.ts
index cac1a75d60cbfff5ab4f0331667eafd38082e82b..c50be1a55a19b5a8e7403d96086d1d3c534f3b8f 100644
--- a/src/task/executor.ts
+++ b/src/task/executor.ts
@@ -388,6 +388,7 @@ function isRecord(value: unknown): value is Record<string, unknown> {
 /** Options for subagent execution */
 export interface ExecutorOptions {
 	cwd: string;
+	agentDir?: string;
 	/** Additional workspace directories to seed on the subagent session (multi-root). */
 	additionalDirectories?: string[];
 	/** Exact provider credential resolver inherited from the parent session. */
@@ -3434,6 +3435,7 @@ export async function runSubprocess(options: ExecutorOptions): Promise<SingleRes
 				forRevive = false,
 			): CreateAgentSessionOptions => ({
 				cwd: worktree ?? cwd,
+				agentDir: options.agentDir ?? settings.getAgentDir(),
 				additionalDirectories: worktree !== undefined ? undefined : options.additionalDirectories,
 				authStorage,
 				modelRegistry,
diff --git a/src/task/persisted-revive.ts b/src/task/persisted-revive.ts
index 5a56eb7024472518d641f0a6ebc1493161db0f89..1beb46885cd6ceee831bd0f4974c60e1fa943767 100644
--- a/src/task/persisted-revive.ts
+++ b/src/task/persisted-revive.ts
@@ -125,6 +125,7 @@ export function createPersistedSubagentReviverFactory(
 			const mcpProxyTools = mcpManager ? createMCPProxyTools(mcpManager) : [];
 			const { session } = await createAgentSession({
 				cwd: ctx.session.sessionManager.getCwd(),
+				agentDir: ctx.session.agentDir,
 				authStorage: ctx.authStorage,
 				// Revived agents join the root session tree, so their observability
 				// frames ride the same bus the RPC/collab surfaces subscribed to.
diff --git a/src/task/structured-subagent.ts b/src/task/structured-subagent.ts
index f5ae96904baff7d8a15b8604e327d67953f32471..2169f3a992ccda2ecdd547d4c02c641b8f20b411 100644
--- a/src/task/structured-subagent.ts
+++ b/src/task/structured-subagent.ts
@@ -404,6 +404,7 @@ function buildExecutorOptions(
 	const enableMCP = !restrictToolNames && (session.enableMCP ?? true);
 	return {
 		cwd: session.cwd,
+		agentDir: session.agentDir ?? session.settings.getAgentDir(),
 		additionalDirectories: session.additionalDirectories,
 		getApiKey: session.getApiKey,
 		agent: policy.effectiveAgent,
diff --git a/src/tools/ast-edit.ts b/src/tools/ast-edit.ts
index 620a18ea401954bf09e974233922ec553f99f6aa..674a70883f2b32622567744e353c937714d0bcce 100644
--- a/src/tools/ast-edit.ts
+++ b/src/tools/ast-edit.ts
@@ -287,6 +287,7 @@ export class AstEditTool implements AgentTool<typeof astEditSchema, AstEditToolD
 			const scope = await resolveToolSearchScope({
 				rawPaths: params.paths,
 				cwd: this.session.cwd,
+				agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 				internalUrlAction: "rewrite",
 				settings: this.session.settings,
 				signal,
diff --git a/src/tools/ast-grep.ts b/src/tools/ast-grep.ts
index d655948db560fb5b849fe9a0bc3c27f05ba73bce..969adad8525f14a6f210d8fd0888b0c0696e541b 100644
--- a/src/tools/ast-grep.ts
+++ b/src/tools/ast-grep.ts
@@ -222,6 +222,7 @@ export class AstGrepTool implements AgentTool<typeof astGrepSchema, AstGrepToolD
 			const scope = await resolveToolSearchScope({
 				rawPaths,
 				cwd: this.session.cwd,
+				agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 				internalUrlAction: "search",
 				settings: this.session.settings,
 				signal,
diff --git a/src/tools/bash-skill-urls.ts b/src/tools/bash-skill-urls.ts
index d3303a5bd173e12e736c968a5aae074c7cba23bc..9b18cb35f5de4fc172211b496c17d05a667ea96d 100644
--- a/src/tools/bash-skill-urls.ts
+++ b/src/tools/bash-skill-urls.ts
@@ -43,6 +43,7 @@ export interface InternalUrlExpansionOptions {
 	internalRouter?: InternalUrlResolver;
 	localOptions?: LocalProtocolOptions;
 	cwd?: string;
+	agentDir?: string;
 	sessionFile?: string;
 	sessionId?: string;
 	agentRegistry?: ResolveContext["agentRegistry"];
@@ -268,6 +269,7 @@ async function resolveInternalUrlToPath(
 	sessionId?: string,
 	agentRegistry?: ResolveContext["agentRegistry"],
 	rules?: readonly Rule[],
+	agentDir?: string,
 ): Promise<string> {
 	const url = normalizeLocalScheme(rawUrl);
 	const scheme = extractScheme(url);
@@ -311,6 +313,7 @@ async function resolveInternalUrlToPath(
 	try {
 		resource = await internalRouter.resolve(url, {
 			cwd,
+			agentDir,
 			pathOnly: true,
 			sessionFile,
 			sessionId,
@@ -382,6 +385,7 @@ export async function expandInternalUrls(command: string, options: InternalUrlEx
 				options.sessionId,
 				options.agentRegistry,
 				options.rules,
+				options.agentDir,
 			);
 		} catch {
 			continue;
diff --git a/src/tools/bash.ts b/src/tools/bash.ts
index 164862c96d532bb80831b484af5a074ea7145ab2..454832e5736b721e4c8484bc94dc839e970e6aea 100644
--- a/src/tools/bash.ts
+++ b/src/tools/bash.ts
@@ -1058,6 +1058,7 @@ export class BashTool implements AgentTool<typeof bashSchemaBase | typeof bashSc
 			attachments: this.session.getImageAttachments?.() ?? [],
 			internalRouter: InternalUrlRouter.instance(),
 			cwd: this.session.cwd,
+			agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 			sessionFile: this.session.getSessionFile() ?? undefined,
 			sessionId: this.session.sessionManager?.getSessionId?.() ?? this.session.getSessionId?.() ?? undefined,
 			agentRegistry: this.session.agentRegistry,
diff --git a/src/tools/glob.ts b/src/tools/glob.ts
index c8e62cb63c1029ff25d7c3f7b5032d50e9c7048b..d9e7d95de5fed9c843fee4ed9f790f5af7ae1ad1 100644
--- a/src/tools/glob.ts
+++ b/src/tools/glob.ts
@@ -224,6 +224,7 @@ export class GlobTool implements AgentTool<typeof findSchema, GlobToolDetails> {
 					const memoryGlob = splitMemoryGlobPattern(rawPattern);
 					const resource = await internalRouter.resolve(memoryGlob.baseUrl, {
 						cwd: this.session.cwd,
+						agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 						settings: this.session.settings,
 						signal,
 						sessionFile: this.session.getSessionFile() ?? undefined,
@@ -245,6 +246,7 @@ export class GlobTool implements AgentTool<typeof findSchema, GlobToolDetails> {
 				}
 				const resource = await internalRouter.resolve(rawPattern, {
 					cwd: this.session.cwd,
+					agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 					settings: this.session.settings,
 					signal,
 					sessionFile: this.session.getSessionFile() ?? undefined,
diff --git a/src/tools/grep.ts b/src/tools/grep.ts
index e45558f80f7b09fb3ddde53779b127a7976d369b..edb47daf2ef367b96b4f9fe8d1cf5707fef30e35 100644
--- a/src/tools/grep.ts
+++ b/src/tools/grep.ts
@@ -775,6 +775,7 @@ async function resolveInternalSearchInputs(opts: {
 	pathSpecs: readonly GrepPathSpec[];
 	resolvedPaths: string[];
 	cwd: string;
+	agentDir?: string;
 	settings: unknown;
 	signal?: AbortSignal;
 	archiveDisplayMap: ReadonlyMap<string, string>;
@@ -796,6 +797,7 @@ async function resolveInternalSearchInputs(opts: {
 	let virtualScopePath: string | undefined;
 	const context: ResolveContext = {
 		cwd: opts.cwd,
+		agentDir: opts.agentDir,
 		settings: opts.settings,
 		signal: opts.signal,
 		sessionFile: opts.sessionFile,
@@ -1011,6 +1013,7 @@ export class GrepTool implements AgentTool<typeof searchSchema, GrepToolDetails>
 					pathSpecs,
 					resolvedPaths,
 					cwd: this.session.cwd,
+					agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 					archiveDisplayMap,
 					settings: this.session.settings,
 					signal,
@@ -1060,6 +1063,7 @@ export class GrepTool implements AgentTool<typeof searchSchema, GrepToolDetails>
 					const scope = await resolveToolSearchScope({
 						rawPaths: searchablePaths,
 						cwd: this.session.cwd,
+						agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 						internalUrlAction: "search",
 						settings: this.session.settings,
 						localProtocolOptions: this.session.localProtocolOptions,
diff --git a/src/tools/index.ts b/src/tools/index.ts
index d52292e668abe128fada59a77f9aea9336030dcf..74f93317890831aed705617b57ca1855ba122fde 100644
--- a/src/tools/index.ts
+++ b/src/tools/index.ts
@@ -156,6 +156,8 @@ export interface DeferredDiagnosticsEntry {
 export interface ToolSession {
 	/** Current working directory */
 	cwd: string;
+	/** Agent directory supplied by the SDK; standalone tools use their settings root. */
+	agentDir?: string;
 	/** Additional workspace directories beyond cwd (multi-root), forwarded to subagents. */
 	additionalDirectories?: string[];
 	/** Whether UI is available */
diff --git a/src/tools/learn.ts b/src/tools/learn.ts
index 74704a2b4e5e09d274e4693597b9ab5a7de4b149..3baffc9ce6eff0f491603afe4569d0cd3661b2e8 100644
--- a/src/tools/learn.ts
+++ b/src/tools/learn.ts
@@ -80,7 +80,7 @@ export class LearnTool implements AgentTool<typeof learnSchema> {
 			}
 		} else if (backend === "local") {
 			const result = await localBackend.save?.(
-				{ agentDir: this.session.settings.getAgentDir(), cwd: this.session.settings.getCwd() },
+				{ agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(), cwd: this.session.cwd },
 				{ content: params.memory, context: params.context, source: "coding-agent-learn", importance: 0.8 },
 			);
 			if (!result || result.stored === 0) {
diff --git a/src/tools/path-utils.ts b/src/tools/path-utils.ts
index 9dfba14dc673cc173b59d1002d205a96c94b5665..46650aa0efad8d24525a6077024c9d8015592de8 100644
--- a/src/tools/path-utils.ts
+++ b/src/tools/path-utils.ts
@@ -1487,6 +1487,7 @@ export interface ResolvedExternalSearchUrl {
 export interface ToolScopeOptions {
 	rawPaths: string[];
 	cwd: string;
+	agentDir?: string;
 	/** Verb used in the "Cannot {action} internal URL without a backing file: …" message. */
 	internalUrlAction: string;
 	/** Collect absolute paths flagged immutable by their internal-URL handler. */
@@ -1599,6 +1600,7 @@ export async function resolveToolSearchScope(opts: ToolScopeOptions): Promise<To
 		}
 		const resource = await internalRouter.resolve(rawPath, {
 			cwd,
+			agentDir: opts.agentDir,
 			settings: opts.settings,
 			signal: opts.signal,
 			sessionFile: opts.sessionFile,
diff --git a/src/tools/read.ts b/src/tools/read.ts
index 3d323041c05b774377124205afea54a6bc4b59d6..04e41a91f4ae00bb98acf1fa4304c046c0776394 100644
--- a/src/tools/read.ts
+++ b/src/tools/read.ts
@@ -2447,6 +2447,7 @@ export class ReadTool implements AgentTool<typeof readSchema, ReadToolDetails> {
 		// Resolve the internal URL
 		const resource = await internalRouter.resolve(url, {
 			cwd: this.session.cwd,
+			agentDir: this.session.agentDir ?? this.session.settings.getAgentDir(),
 			settings: this.session.settings,
 			signal,
 			sessionFile: this.session.getSessionFile() ?? undefined,
diff --git a/src/session/artifacts.ts b/src/session/artifacts.ts
--- a/src/session/artifacts.ts
+++ b/src/session/artifacts.ts
@@ -4,7 +4,7 @@
  * Artifacts are stored in a directory alongside the session file,
  * accessible via artifact:// URLs.
  */
-import * as fs from "node:fs/promises";
+import fs from "node:fs/promises";
 import * as path from "node:path";
 import { replaceFileAtomically } from "../utils/atomic-file";
 
@@ -41,20 +41,20 @@
  *
  * Returns the verified UTF-8 byte count.
  */
-export async function writeArtifact(path: string, content: string): Promise<number> {
+export async function writeArtifact(path: string, content: string, signal?: AbortSignal): Promise<number> {
+	signal?.throwIfAborted();
 	const expectedBytes = Buffer.byteLength(content);
 	const tempPath = `${path}.tmp-${crypto.randomUUID()}`;
 	try {
-		const writtenBytes = await Bun.write(tempPath, content);
-		if (writtenBytes !== expectedBytes) {
-			throw new Error(`Artifact write incomplete: wrote ${writtenBytes} of ${expectedBytes} bytes`);
-		}
+		await fs.writeFile(tempPath, content, { signal });
 		const file = Bun.file(tempPath);
 		if (file.size !== expectedBytes) {
 			throw new Error(`Artifact size mismatch: found ${file.size} of ${expectedBytes} bytes`);
 		}
 		await file.slice(0, Math.min(expectedBytes, 1)).arrayBuffer();
+		signal?.throwIfAborted();
 		await replaceFileAtomically(tempPath, path);
+		signal?.throwIfAborted();
 	} catch (error) {
 		await fs.rm(tempPath, { force: true });
 		throw error;
@@ -150,9 +150,10 @@
 	 * @param toolType Tool name for file extension (e.g., "bash", "read")
 	 * @returns Artifact ID (numeric string)
 	 */
-	async save(content: string, toolType: string): Promise<string> {
+	async save(content: string, toolType: string, signal?: AbortSignal): Promise<string> {
+		signal?.throwIfAborted();
 		const { id, path } = await this.allocatePath(toolType);
-		await writeArtifact(path, content);
+		await writeArtifact(path, content, signal);
 		return id;
 	}
 
diff --git a/src/session/session-manager.ts b/src/session/session-manager.ts
--- a/src/session/session-manager.ts
+++ b/src/session/session-manager.ts
@@ -2125,9 +2125,10 @@
 		return (await this.#artifactManagerForSession()?.allocatePath(toolType)) ?? {};
 	}
 
-	async saveArtifact(content: string, toolType: string): Promise<string | undefined> {
+	async saveArtifact(content: string, toolType: string, signal?: AbortSignal): Promise<string | undefined> {
+		signal?.throwIfAborted();
 		const manager = this.#artifactManagerForSession();
-		if (manager) return manager.save(content, toolType);
+		if (manager) return manager.save(content, toolType, signal);
 
 		// Non-persistent session: keep an in-memory copy so spill truncation works.
 		this.#inMemoryArtifacts ??= new Map();
diff --git a/src/session/session-maintenance.ts b/src/session/session-maintenance.ts
--- a/src/session/session-maintenance.ts
+++ b/src/session/session-maintenance.ts
@@ -634,12 +634,16 @@
 	 * Mutates the branch in place, persists via `rewriteEntries`, replays the
 	 * rebuilt context through the agent, and tears down provider sessions that
 	 * cache message identity — same rewrite contract as {@link dropImages}.
+	 * Cancellation stops before branch mutation; an admitted rewrite finishes
+	 * before cancellation is reported.
 	 *
 	 * No-op (zero counts) when nothing is eligible.
 	 */
 	async shake(mode: ShakeMode, opts: { config?: ShakeConfig; signal?: AbortSignal } = {}): Promise<ShakeResult> {
+		opts.signal?.throwIfAborted();
 		if (mode === "images") {
 			const { removed } = await this.#host.dropImages();
+			opts.signal?.throwIfAborted();
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, imagesDropped: removed, tokensFreed: 0 };
 		}
 
@@ -667,6 +671,7 @@
 			this.#host.agent.replaceMessages(sessionContext.messages);
 			this.#host.resetAdvisorRuntimes("shake");
 			this.#host.closeCodexProviderSessionsForHistoryRewrite();
+			opts.signal?.throwIfAborted();
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, thinkingBlocksDropped: removed, tokensFreed: 0 };
 		}
 
@@ -685,7 +690,8 @@
 			return { mode, toolResultsDropped: 0, blocksDropped: 0, tokensFreed: 0 };
 		}
 
-		const artifactId = await this.#saveShakeArtifact(regions);
+		const artifactId = await this.#saveShakeArtifact(regions, opts.signal);
+		opts.signal?.throwIfAborted();
 		const replacements = regions.map((region, index) => this.#shakeElidePlaceholder(region, index, artifactId));
 
 		const hasRemoteReplacementHistory = getOpenAiRemoteCompactionPayload(latestCompaction) !== undefined;
@@ -730,6 +736,7 @@
 		this.#host.agent.replaceMessages(sessionContext.messages);
 		this.#host.resetAdvisorRuntimes("shake");
 		this.#host.closeCodexProviderSessionsForHistoryRewrite();
+		opts.signal?.throwIfAborted();
 
 		return {
 			mode,
@@ -747,23 +754,14 @@
 		return `[shaken ~${region.tokens} tokens]`;
 	}
 
-	/**
-	 * Concatenate the original region contents into one session artifact so the
-	 * agent can read them back via `artifact://<id>`. Returns `undefined` when
-	 * the session is not persisted or the write fails — callers degrade to a
-	 * bare placeholder.
-	 */
-	async #saveShakeArtifact(regions: ShakeRegion[]): Promise<string | undefined> {
+	/** Save the original regions before replacing any journal content. */
+	async #saveShakeArtifact(regions: ShakeRegion[], signal?: AbortSignal): Promise<string | undefined> {
 		const parts: string[] = [];
 		for (let i = 0; i < regions.length; i++) {
 			const region = regions[i];
 			parts.push(`### region ${i + 1} (${region.label}, ~${region.tokens} tok)`, "", region.originalText, "");
 		}
-		try {
-			return await this.#host.sessionManager.saveArtifact(parts.join("\n"), "shake");
-		} catch {
-			return undefined;
-		}
+		return this.#host.sessionManager.saveArtifact(parts.join("\n"), "shake", signal);
 	}
 
 	/**
diff --git a/dist/types/session/artifacts.d.ts b/dist/types/session/artifacts.d.ts
--- a/dist/types/session/artifacts.d.ts
+++ b/dist/types/session/artifacts.d.ts
@@ -13,7 +13,7 @@
  *
  * Returns the verified UTF-8 byte count.
  */
-export declare function writeArtifact(path: string, content: string): Promise<number>;
+export declare function writeArtifact(path: string, content: string, signal?: AbortSignal): Promise<number>;
 /**
  * Manages artifact storage for a session.
  *
@@ -56,7 +56,7 @@
      * @param toolType Tool name for file extension (e.g., "bash", "read")
      * @returns Artifact ID (numeric string)
      */
-    save(content: string, toolType: string): Promise<string>;
+    save(content: string, toolType: string, signal?: AbortSignal): Promise<string>;
     /**
      * Check if an artifact exists.
      * @param id Artifact ID (numeric string)
diff --git a/dist/types/session/session-manager.d.ts b/dist/types/session/session-manager.d.ts
--- a/dist/types/session/session-manager.d.ts
+++ b/dist/types/session/session-manager.d.ts
@@ -240,7 +240,7 @@
         id?: string;
         path?: string;
     }>;
-    saveArtifact(content: string, toolType: string): Promise<string | undefined>;
+    saveArtifact(content: string, toolType: string, signal?: AbortSignal): Promise<string | undefined>;
     getArtifactPath(id: string): Promise<string | null>;
     saveDraft(text: string): Promise<void>;
     consumeDraft(): Promise<string | null>;
diff --git a/src/session/agent-session.ts b/src/session/agent-session.ts
--- a/src/session/agent-session.ts
+++ b/src/session/agent-session.ts
@@ -8996,6 +8996,9 @@
 				break;
 			}
 			if (event.type === "error") {
+				if (event.reason === "aborted") {
+					throw new DOMException(event.error.errorMessage || "Ephemeral turn aborted", "AbortError");
+				}
 				throw new Error(event.error.errorMessage || "Ephemeral turn failed");
 			}
 		}
